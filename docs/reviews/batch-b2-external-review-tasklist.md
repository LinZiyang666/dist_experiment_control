# Batch B2 independent external review tasklist

Date: 2026-07-27
Reviewer: independent external reviewer
Base: unstaged and untracked working tree layered on the existing index at `HEAD=808552d` (`main`)
Initial boundary: 144 tracked files (`+1438/-770`) plus 9 untracked files.
Initial tracked diff SHA-256:
`9ac7aabbaf8c2c6513e469b7ad4c7b8c480d232cfa41e2934ff02b6c04d60a58`.

## Boundary, authority, and workflow

- [x] Freeze every unstaged/untracked path and its role; treat the existing index as the review base and
  do not silently review or certify older staged work.
- [x] Read `CLAUDE.md`, the WHAT/HOW authority chain, relevant architecture/operations/testing documents,
  the Batch B2 plan, the structural-audit source items, and prior external-review workflow examples.
- [x] Independently map every implementation change to B5–B9 scope, plan commitments, permanent
  non-goals, user-visible behavior, and release/deployment blast radius.
- [x] Re-check the boundary before certification and record reviewer-only additions or concurrent drift.

## B5 — NATS configuration assembly and deployment behavior

- [x] Audit the package move from `natscluster`/`natsreconcile` into `natsconf`: import boundaries,
  exported API compatibility, action/error semantics, and absence of stale production call sites.
- [x] Audit `RenderDesired` intent semantics for preserve, force-standalone, force-clustered, and
  standalone-if-lone across standalone/clustered live input, zero/one/many peers, and invalid intents.
- [x] Verify all production assembly sites share exactly one configuration construction path without
  changing caller-specific safety gates, especially already-standalone no-op, confirmation, partial-mesh,
  plan-only, reset-JS, leader/role, and cutover monitor behavior.
- [x] Verify JetStream/store-dir fail-closed behavior, account/domain removal, allow-listed directives,
  secrets-directory identity, route mTLS harvest/override, monitor harvest, websocket preservation,
  passthrough ordering, and byte-for-byte output stability.
- [x] Adversarially inspect both structural scanners for compound-literal/assignment/alias/helper blind
  spots, vacuity, generated files, tests masquerading as production, and future bypasses.
- [x] Validate config goldens are independent characterizations rather than snapshots generated by the
  implementation under test; add counterexamples where a claimed invariant is not behaviorally pinned.
- [x] Check docs and operator recovery guidance against actual render/reconcile behavior and failure
  classification.

## B6 — test naming migration and provenance

- [x] Reproduce the process-name regex/count and inventory every rename/deletion/addition; verify there
  are no collisions, package changes, build-tag loss, helper loss, or unintentional test-function drift.
- [x] Verify the four tunnel review files were consolidated without losing any verb, fence dimension,
  in-flight REGISTER behavior, DB-independence property, synchronization helper, or race/leak cleanup.
- [x] Audit `legacy_process_named_list.go`, the freeze gate, synthetic self-checks, exact residual ledger,
  and the rule that removed entries must fail rather than becoming permanent exemptions.
- [x] Verify every migrated test retains meaningful provenance; audit `// origin:` validation for
  fabricated paths, self-reference, weak prose, duplicate/incorrect attribution, and path traversal.
- [x] Compare pre/post `(directory, test function)` multisets and all E2E `-run` selections; investigate
  any delta rather than trusting the plan's counts.

## B7 — reconcile authority, loop liveness, and bounded observation

- [x] Audit authority evaluation/caching per sweep, standalone nil-authority semantics, leadership
  changes, pass ordering, registration panics, fake-clock determinism, and no-goroutine/no-timer contract.
- [x] Verify duration/high-water/overrun accounting under success, error, recovery, clock anomalies,
  cadence boundaries, never-run passes, and admin/CLI serialization/rendering compatibility.
- [x] Audit `GoEvery` for exact cadence, first-iteration timing, panic/error propagation, cancellation,
  loop exit, callback placement, data races, and honest `LastIter`/`Iters` reporting.
- [x] Trace every leader-tick JetStream observation and prove all potentially blocking calls receive a
  real deadline; mutation-check the AST guard for wrappers, aliases, multiline calls, and false matches.
- [x] Verify observation timeout is not silently converted into a measured replica count or false
  convergence/health conclusion, and that all changed status fields preserve backward compatibility.
- [x] Confirm promised loop consolidation was either delivered or honestly declined with code-backed
  reasoning; do not accept plan bookkeeping as proof.

## B8 — operation transitions and transfer-ledger disposition

- [x] Audit the three pointer fields for nil/preserve, nonzero/write, and explicit-zero/clear semantics;
  enumerate every production call site and verify intended columns and SQL parameter ordering.
- [x] Verify generated SQL remains valid, deterministic, injection-safe, mixed-version compatible, and
  guarded against accidental barrier/topology/catchup column coupling.
- [x] Exhaustively review `ledgerRowDisposition` as a state machine over site, row kind, live tracker,
  age, outbox ownership, census-known/unknown, malformed/unreadable/temp rows, and exact-terminal priority.
- [x] Trace both directory passes and all callers to prove the pure decision function received honest
  inputs and its result is applied safely under scan races, partial I/O failure, repeated passes, and
  crash/restart windows.
- [x] Re-run prior transfer exactly-one-terminal counterexamples and test unreadable outbox rows,
  missing parents, conflicting terminal bytes, callback failure, and durable retirement interactions.
- [x] Adversarially inspect the line-level census/guard for parser blind spots, renamed helpers,
  multiline conditions, vacuous counts, and future unsafe synthesis paths outside the guarded function.

## B9 — shared harnesses and leak-gate strength

- [x] Audit every `OpenDB`, `StartNATS`, `SilentLog`, leak assertion, CA, free-port, and wait-helper
  consolidation for exact behavior, cleanup ownership, timing, logging, permissions, and failure semantics.
- [x] Audit `test/clusterharness` as a shared public test API: certificate identity/SAN/validity,
  randomness/errors, listener races, port allocation TOCTOU, timeout diagnostics, and caller assumptions.
- [x] Verify the strengthened tunnel leak test exercises at least five simultaneously meaningful
  sessions, closes all client/server/NATS resources, and cannot pass through the ±2 noise floor.
- [x] Audit the leak-assert AST rule for nested subtests, helper indirection, `range` forms, constants,
  dynamic bounds, early breaks/continues, assertions inside loops, dead exemptions, and synthetic
  non-vacuity controls.
- [x] Verify matrix filter expansion really runs all intended goroutine/fd gates under `-race` and does
  not silently omit, duplicate, or destabilize unrelated concurrency tests.

## Cross-cutting code, tests, and documentation

- [x] Review every non-test implementation diff line-by-line for races, lock ordering, stale state,
  context misuse, error loss, resource leaks, unsafe zero values, misleading names/comments, and dead code.
- [x] Review every changed/new test for false goldens, implementation mirroring, soft-pass branches,
  brittle source-text coupling, timing flakes, missing cleanup, race blindness, and mutation honesty.
- [x] Re-run line-keyed/AST ratchets (`cfgdb`, error codes, ACL, raft timing, naming/origin/leak guards)
  and verify baseline changes are justified rather than adjusted to green.
- [x] Check wire/schema/API/JSON/text output compatibility, toolchain/build tags, gofmt, `git diff --check`,
  vet, lint, and repository dependency/confinement rules.
- [x] Verify documentation claims, counts, commands, and simcluster evidence against the final source;
  record doubts where policy or intent remains ambiguous.

## Verification and deploy tier

- [x] Run focused unit tests for each B5–B9 lane and classify every red.
- [x] Run affected packages and all concurrency/leak paths with `-race`, including repeated
  `test/concurrency` rounds.
- [x] Run all-tag vet/compile coverage, `make test`, `make lint`, and the sole full matrix
  `make e2e-parallel` on a stable tree.
- [x] Inspect simcluster host/server state and mandate; rebuild from the final source, prove binary
  identity/freshness, and independently run drills `10-grow-to-3`, `20-forcesingle-natsconf`,
  `41-shrink-to-standalone`, and `43-migrate-live-data`.
- [x] Preserve exact deploy-tier verdicts, gaps, assertion counts, logs, and cleanup state; do not use
  prior/internal drill runs as certification evidence.

## Deliverables

- [x] Write an external review report whose first line is exactly `Fail` or `Pass`, with severity-ranked
  findings, doubts, recommendations, scope, and exact verification evidence.
- [x] Mark every task complete or explicitly blocked with evidence; stage all files, including reviewer
  tests/tasklist/report, and verify there are no unstaged or untracked paths.

## Completion evidence

- Reviewer-only changes: six behavioral/structural counterexamples across
  `cmd/tether/natsconf_takeover_renders_test.go`,
  `internal/broker/{alert_webhook_beat,clusterstatus,xfer_unreadable_outbox_row}_test.go`,
  `internal/natsconf/config_assembly_baseline_test.go`, and
  `test/determinism/leak_assert_shape_test.go`; this tasklist and the external-review report.
- Focused result: all six counterexample groups fail deterministically; the affected package set passes
  when those exact reviewer tests are skipped.
- Static/toolchain result: `make lint` = `0 issues`; all-tag `go vet` and compile-only `go test` pass;
  gofmt and `git diff --check` are clean.
- Race result: affected package race suite passes with the reviewer reds skipped; `test/concurrency`
  passes three repeated race rounds.
- Full gates: `make test` and `make e2e-parallel` fail only on the reviewer counterexamples;
  the parallel runner represents 15/15 top-level tests and `TestAllPhases` passes.
- Deploy tier: rebuilt `tether-sim:dev` from current source and verified image/staged binary SHA-256
  `cbf04dcfff5272687bc6f63d16f4d0c7edd62c4fd29437bb6d8cc9f45c1f6f88`.
  Drills: `10-grow-to-3` GREEN (19), `20-forcesingle-natsconf` GREEN (16),
  `41-shrink-to-standalone` INCOMPLETE (31 pass, two recorded proactive-rehome gaps),
  `43-migrate-live-data` GREEN (38). All isolated drill containers were cleaned.
- No concurrent product drift was observed after the initial boundary freeze. Final staging verification
  is recorded in the review report.

---

## 主进程处置（2026-07-27，外审之后）

审查任务清单本身**不改动**——它是审查者的工作记录。以下是对它每个 Deliverable 的响应状态。

- [x] **逐条回复**：7 个 finding + 5 条 doubts 全部在 `batch-b2-external-review.md` 内**原位缩进回复**，
      含两项未采纳/部分采纳的证据（B2-1 的拓扑判据合并、B2-6 的 go/types）。
- [x] **修复**：5 MAJOR + 2 MINOR 全部实现，按报告给的 Recommended fix order。
- [x] **六个反例全部转绿并保留为永久负控**，一个都没有削弱或删除。
      唯一被改写的测试是**内审**写的 `TestWebhookBeatDoesNotCountUndeliveredEvents`，
      它与 B2-4 的结论直接冲突；按**它自己注释里预留的路径**反转为
      `TestWebhookRejectedDeliveryIsLiveButNotDelivered`，并保留其原始关切（`accepted == 0`）。
- [x] **每条新增守卫都做了变异验证**（注入它声称能抓的缺陷、确认变红、用 `Edit` 还原）：
      B2-1 的启用判据门、B2-2 的跨生产者版本门、B2-5 的锚点+次数推导、B2-6 的 helper 参数形状。
- [x] **三门重跑全绿** + 全 tag `go vet` + 四个 drill 从**重建镜像**重跑（等值构建证明二进制身份）。
- [ ] **`41-shrink-to-standalone` 仍为 INCOMPLETE 且不请求豁免**——已在报告疑问 5 下声明、归因，
      并说明为何它先于本批存在、为何不应在本批顺手改 agent 重连时序。**是否放行由外审决定。**
- [x] **不 `git add`**：暂存是外部审查者的工作（本项目既有约定），本轮修复全部留在工作树。

---

## 外部重新审查（2026-07-27）

本节只审查维护者回复后相对上轮已暂存基线新增的工作树层；不把维护者自报的测试结果、
变异验证或 simcluster 结果当成外部证据。冻结边界：24 个已跟踪文件、4 个未跟踪测试，
`git diff` SHA-256 为
`31939450dbd40babe888637250284d10de9aa5469de118d2729c4b4b84a77c59`。

- [x] 逐条复核 B2-1…B2-7 的实现、回复论证、原外审反例是否保留且未削弱，并检查修复引入的
      wire/schema、并发、恢复幂等、CLI 语义和拓扑回归。
- [x] 对 B2-5 的 AST 漏洞门添加/运行带标签的嵌套 `break`、基线变量遮蔽等独立变异；
      对 B2-6 添加/运行 import alias、Config type alias/defined type、跨函数同名变量等独立变异。
- [x] 逐行审查 4 个未跟踪测试，确认断言基于外部契约而非复制实现；检查已有外审测试未被删除、
      跳过、改名规避或降低强度。
- [x] 运行七组原始反例、新增对抗测试、受影响包、race 重复轮、`git diff --check`、gofmt、lint、
      全 tag vet/compile、`make test` 与唯一完整矩阵 `make e2e-parallel`。
- [x] 独立读取 simcluster mandate/host 状态；从最终树重建并验证镜像身份，重跑要求的四个 drill，
      保留 GREEN/INCOMPLETE/RED 原始语义，未经明确豁免不得把 INCOMPLETE 记为发布通过。
- [x] 在外审报告顶部更新最终 `Fail`/`Pass`，记录已关闭/仍开放 finding、疑惑、建议、精确命令与证据；
      完成全部清单后把所有文件加入暂存，并确认无未暂存或未跟踪路径。

### 外部复审完成证据

- 结论：2 MAJOR、2 MINOR 仍开放；B2-2/B2-3/B2-7 关闭，B2-4 核心关闭但留一个 MINOR；
  drill 41 未豁免 INCOMPLETE，整体 `Fail`。
- 独立红测：disabled-JS 脱簇/破坏性 warning、leak 三类控制流、Config import/type alias、
  JetStream map extract-local、webhook 撕裂快照。排除这些精确红测后，受影响包与 race 全绿。
- 静态：lint 0 issues、diff-check/gofmt、全 tag vet/compile 全绿；concurrency 三轮 race 全绿。
- 全门：`make test` 仅上述独立反例红；完整矩阵 15/15、`TestAllPhases` 绿，四个失败 unit
  均由上述反例解释。
- Deploy：三方 SHA-256 均为 `c0bc510c…963d1`；10/20/43 GREEN（19/16/38），
  41 INCOMPLETE（31 pass、2 gap），461s，无偏差，所有隔离容器清理。
- 交付：报告保持首行 `Fail`；所有文件由外审统一加入暂存，最终以
  `git diff`/`git ls-files --others --exclude-standard` 均为空核验。
