# C6 Plan (FINAL) — Expose/Rehome 可观测补全 + 状态命名对齐 + 恢复命令别名

> Stage-A output. 9-agent workflow (4 lens drafters → 4 cross-critiques → 1 synth; all Opus 4.8; full raw `tasks/wlrxnjps5.output`). Main process sole finalizer. The synth verified ground truth: `decideProxyEvents` (proxy_cluster.go:100) has **0 production callers**; `proxy_node_ready` exists nowhere; `PlanReassignHome` is 3-arg/no-`now`; `cluster.LitTime(t)` is the sibling stamp helper (plain string, no error — `LitText` returns a tuple); `migrateExposes` swallows `ErrNotFound` then false-emits `expose_rehomed`; `computeHealth` `FaultTolerance==0` → `healthDegraded` (exit 1) before topo checks; `observeOnce` already scans `ActiveAlerts` once.

## 0. Objective + acceptance

Additive, lower-risk phase finishing 建议5 (rehome observability) + 建议6 (status-name alignment) + 建议7 (recovery aliases). **No proto/ACL/subject change; one additive migration (0017); zero behavior change for non-cluster brokers.**

Acceptance: 5 new rehome events live + secret-free (alongside existing `expose_rehomed`); the C5 proxy-event family made genuinely LIVE (BD1); `last_rehome_at`+`reconnects` on the homes view; `cluster status --homes` aggregates exposes + `__proxy__`; 5 states via additive `health_label` (legacy `health`/`exit_code`/`schema_version` byte-stable, exit 0/1/2/3 unchanged); `cluster recovery {diagnose,force-single,rejoin prepare}` aliases the existing offline commands behavior-identically.

## 1. Binding decisions (from synth §1, all ADOPTED)

| # | Decision |
|---|---|
| **BD1** | **WIRE `decideProxyEvents`→`pubSysEvent` in `reconcileProxySession`** (one change-keyed emit) so `sub_render_empty`/`proxy_no_ready_nodes`/`proxy_partial` go LIVE — closes a real C5 honesty gap (the pure fn + tests exist but were never called). **Do NOT invent `proxy_node_ready`** (never existed; "node ready" = the `proxy_partial`→full transition + absence of `proxy_node_unready`). |
| **BD2** | One merged edit per `PlanReassignHome` call site: `PlanReassignHome(db, port, home, now)`; events wrap the single 4-arg call. proxy site `b.cfg.Now()`, drain site `a.now()`. Update plan.go comment + d6_plan_test shape assertions. |
| **BD3** | `last_rehome_at` = `cluster.LitTime(now.UTC())` (sibling of `revoked_at`; plain string). Events carry no rehome-time field beyond envelope `ts`; cross-correlate on `epoch`, not timestamp. |
| **BD4** | `last_rehome_at` = **persisted replicated column (migration 0017)**, stamped INSIDE the CAS-guarded UPDATE (survives failover; an in-memory cache blanks after election; zero extra raft writes). |
| **BD5** | `reconnects := epoch` (derived — only `PlanReassignHome` bumps epoch +1 monotone CAS; a 2nd column is provably always equal). No new column. |
| **BD6** | Fold `ClusterAdmin.onRehome` → one `emitEvent func(kind string, fields map[string]any)` (= `b.pubSysEvent`). |
| **BD7** | `broker_down_rehome_summary` honest fields: `down_broker`, `proxy_homes`(int, leader will rehome), `exposes_stranded`(int — regular exposes NOT auto-rehomed on crash). Single `ActiveAlerts` scan in `observeOnce`, reused for prevDown + orphan-clear. |
| **BD8** | Capture `skipped bool` in the drain `Propose` closure; on `ErrNotFound` skip emit nothing (fixes the pre-existing `expose_rehomed` false-emit). |
| **BD9** | M3 stuck-not-ready = ALLOCATE not rehome → emit only `rehome_stalled{target_not_ready}` (change-keyed) before the rotate; no `home_reassign_succeeded`, no `last_rehome_at` stamp (a full re-alloc resets reconnects=0/last_rehome_at=''). |
| **BD10** | Status naming = **additive `health_label`** (hyphenated 5-state); `health`/`exit_code`/`statusSchemaVersion=1` byte-stable. **NOT-HA = exit 1** (shares with DEGRADED-WRITABLE, disambiguated by label; no exit 4). |
| **BD11** | `--homes` = **new `OpClusterHomes` + `ClusterHomesReport`** (own `schema_version`), routed before the leader gate; do NOT touch `ClusterStatusReport`. Carries `is_leader_view`. |
| **BD12** | Refactor `proxyStatusNodesCluster`'s reason build into shared `b.proxyReadyFor(sid,nid,home)`; `--homes` + `proxy status` call it. Expose rows use `exposeReadyReason` gating `public_host!=""` (Ready ⇒ public_url!=""). |
| **BD13** | Recovery aliases = pure CLI re-grouping into the existing `escape` group (fresh constructor instances — a cobra cmd can't have two parents); top-level `force-single`/`recover`/`restore` unchanged; `EnablePrefixMatching` stays unset; `recovery diagnose` requires `--self-id`. |
| **BD14** | One shared test helper exporting the secret-substring set + the field allow-list, referenced by both the event guard + the homes guard. |

## 2. New events

All via `b.pubSysEvent(kind, fields)` (leader-only drivers: `driveProxyReconcile`/`migrateExposes` under `DrainNode`/`observeOnce` all gate `IsLeader()`). `reason` is a CLOSED ENUM, never `err.Error()` (raw → logger only). Change-key helper `internal/broker/rehome_events.go` (`emitRehomeOnce(port,mark,kind,fields)` / `clearRehomeMark(port)` / `classifyRehomeErr`) backed by `b.rehomeEvt sync.Map` (idle-zero-writes; reaper re-enters ~5s). Events: `home_reassign_started` (before Propose), `home_reassign_succeeded` (after Apply; drain skips if `skipped`), `home_reassign_failed` (Propose err → classified reason), `rehome_stalled` (proxy `pickProxyRehomeTarget==""`→`no_eligible_target` + M3→`target_not_ready`; drain `ErrNoMigrationTarget`→`no_eligible_target`), `broker_down_rehome_summary` (once per broker_down false→true). **Secret-free**: every payload a HAND-BUILT scalar map (never struct-splat), reads only `.Port/.HomeBroker/.Epoch/.SID/.NID/.Name`; all emits BEFORE `pushProxyDirectiveWithHome`. Guard `TestRehomeEventsNeverLeakSecret` (key-set ⊆ allow-list). **BD1**: `reconcileProxySession` computes `proxyEventCounts` + calls `decideProxyEvents(prev,cur)` + emits each, per-`(sid,kind)` change-key (`b.proxyEvt sync.Map`).

## 3. Status naming alignment (Option A, schema-stable)

`ClusterStatusReport.HealthLabel string` (additive, no omitempty). `healthLabel(health, voters)`: HEALTHY_HA→HEALTHY-HA; DEGRADED + voters≤2→NOT-HA else DEGRADED-WRITABLE; QUORUM_LOST→READ-ONLY; FORCE_SINGLE→FORCE-SINGLE; else "". `computeHealth` signature UNCHANGED (zero churn to its 4 test files). **NOT-HA=exit 1 contract-safe**: `FaultTolerance==0` branch returns `healthDegraded`(exit 1) before topo checks, and `ProjectQuorum(v,false).FaultTolerance==0 ⟺ v≤2`, so `(DEGRADED, v≤2)` is the only DEGRADED-at-v≤2 producer. Offline path (cluster.go:219/256) sets HealthLabel inline (FORCE-SINGLE/READ-ONLY; clean snapshot "" → online-socket-only for NOT-HA/DEGRADED-WRITABLE/HEALTHY-HA, documented). Render `label := HealthLabel; if ""{ label=Health }` (legacy fixtures green); card headline re-keys off health_label. Update stale Short. Leave `clusterVerdict` prose untouched + a guard asserts it agrees with NOT-HA. Do NOT touch the ctl/remote summary (no voter count) or add `--explain`.

## 4. `cluster status --homes`

Migration `0017_port_alloc_last_rehome.sql`: `ALTER TABLE port_allocations ADD COLUMN last_rehome_at TEXT NOT NULL DEFAULT ''`. `PlanReassignHome` stamps it inside the CAS WHERE (stale-epoch no-op never stamps). `OpClusterHomes` + `ClusterHomesReport{SchemaVersion,IsLeaderView,Homes[],Errors,Partial}` + `HomeEntry{Kind,SID,NID,Name,Port,HomeBroker,Epoch,Reconnects(==Epoch),PublicURL,ReadyReason,Ready,LastRehomeAt}`. Backend `homesReport` on ClusterAdmin (injected in wireClusterLate like healthPoll), routed pre-leader-gate (single mode→CodeClusterNotEnabled). `b.buildHomesReport()` (`internal/broker/homes.go`): RODB JOIN port_allocations LEFT JOIN cluster_nodes → materialize-then-enrich; proxy rows → shared `b.proxyReadyFor`, expose rows → `exposeReadyReason` (no_home/catching_up[phase≠VOTER‖cert==""‖public_host==""]/tunnel_down[!reachable]/ready); scan error → Errors+Partial (never silent-empty). CLI `--homes` (mutually exclusive with --offline/--remote/--watch), no exit-code contract, human table one EPOCH column (reconnects in --json only).

## 5. Recovery aliases

New `cmd/tether/cluster_recovery.go`; `addGrouped(newClusterRecoveryCmd(), "escape")`. `recovery diagnose --offline --self-id X` → delegates to `forceSingleGuided` (read-only peer-probe); `recovery force-single …` → fresh `newClusterForceSingleCmd()` (same --confirm-peers-dead/never-`--yes`/typed-confirm); `recovery rejoin prepare …` → fresh `newClusterRecoverCmd()` Use="prepare" under `rejoin` parent (same --dump-divergent/--emit-manifest/typed-confirm). Each alias = fresh constructor instance (cobra single-parent). Top-level unchanged; EnablePrefixMatching unset (recover/recovery coexist). `--offline` on diagnose = inert doc-parity marker. GAP-1 keep --self-id required (fix proposals example). GAP-2 printed next-steps reference top-level names (accepted; runbook table canonical).

## 6. File-level change list

NEW: `internal/storage/migrations/0017_port_alloc_last_rehome.sql`, `internal/broker/rehome_events.go`, `internal/broker/homes.go`, `cmd/tether/cluster_recovery.go`, `cmd/tether/cluster_recovery_test.go`. EDIT: port/plan.go (+now, stamp, comment, d6_plan_test), broker.go (rehomeEvt+proxyEvt sync.Map), proxy_reconcile.go (events+4-arg+M3 stalled+BD1 wire), clusterdrain.go (4-arg+skipped+lifecycle+stalled), observability.go (single scan+summary), clusteradmin.go (onRehome→emitEvent+homesReport), clusterwrite.go (wire), clusterstatus.go (healthLabel+route OpClusterHomes), adminsock/protocol.go (op+report+HealthLabel), cmd/tether/cluster.go (Short+--homes+offline label+fallback+recovery group), cmd/tether/cluster_status_card.go (cardHeadline). DOC follow-up: cluster-runbook.md (label/exit + recovery mapping + last_rehome provenance + summary asymmetry), usage.md, proposals example fix, v2-automation-program §C6 done + C5-gap note.

## 7. Adversarial test list

Events: TestRehomeProxyEmitsStartedSucceeded, TestRehomeStalledNoTargetEmittedOncePerTick, TestRehomeStalledTargetNotReady, TestRehomeFailedClassifiesNotLeader, TestRehomeEventReannouncesAfterRecovery, TestMigrateExposesEmitsLifecycleAndLegacy, TestMigrateExposesNoTargetStalled, TestMigrateExposesSkipNoFalseSuccess, TestBrokerDownRehomeSummaryOncePerTransition, TestBrokerDownSummaryReArmsAfterClear, TestObserveOnceSingleActiveAlertsScan, TestProxyEventsWiredLive (BD1), TestRehomeEventsNeverLeakSecret (BD14). Status: TestHealthLabelMatrix, TestNotHAFaultToleranceEquivalence, TestComputeHealthNotHABranchMasksDegradation, TestStatusReportHealthLabelWired, TestVerdictAgreesWithLabelAtNLE2, TestJSONSchemaStableHealthLabel, TestOfflineStatusHealthLabel, TestRenderShowsHyphenatedLabelWithFallback, TestExitCodeContractUnchangedAcross5States. Homes: TestPlanReassignHomeStampsLastRehome, TestPlanReassignHomeLastRehomeReplicaDeterministic, TestMigration0017AdditiveDefaultEmpty, TestBuildHomesReportExposeAndProxy, TestHomesReadyReasonMatchesProxyStatus, TestExposeReadyReasonGatesPublicHost, TestHomesReportSecretFree, TestHomesReportSingleModeClusterNotEnabled, TestHomesReportPartialOnReadError, TestHomesReportIsLeaderView, TestStatusHomesFlagMutualExclusion, TestStatusReportSchemaUnchanged, + TestD6Matrix extension (gated: --homes epoch++/reconnects==epoch/fresh last_rehome_at, persists across failover). Recovery: TestRecoveryForceSingleMatchesTopLevel, TestRecoveryRejoinPrepareMatchesRecover, TestRecoveryDiagnoseReadOnlyAndBlocksLivePeer, TestRecoveryDiagnoseRequiresSelfID, TestRecoveryForceSingleNeverYes, TestRecoveryRejoinPrepareTypedConfirmRefusesNonTTY, TestRecoveryTreeShapeAndGroups, TestRecoverVsRecoveryNoPrefixAmbiguity. Hard gate: make test + make e2e + make lint green; -race + NumGoroutine/fd leak gate on proxy-reaper/observe/rehome touches.
