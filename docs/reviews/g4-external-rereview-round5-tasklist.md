# G4 external re-review round 5 tasklist

- [x] Re-read the developer's round-4 response and reconstruct the new unstaged diff.
- [x] Re-check R4-B1 remediation: `verifyClusterSeam` full expected tuple and driver call site.
- [x] Re-run external seam regressions for wrong `raft_addr`, incomplete seam, and wrong `data_dir`.
- [x] Add independent coverage for wrong-but-nonempty `secrets_dir` and `nats_conf_path`.
- [x] Run focused Go checks, shell syntax checks, and diff checks.
- [x] Inspect current tether access to `weilandserver` and simcluster status.
- [x] Write the round-5 external review report with Pass/Fail, doubts, and verification.
- [x] Add all files to the git index after the report is complete.
