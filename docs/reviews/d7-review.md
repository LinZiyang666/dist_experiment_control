All confirmed. The vacuity findings are accurate:
- **`testD7AddVoterFail`** (line 208-230): confirmed misnomer — `caughtUp` always returns `false`, AddVoter on "ghost" still succeeds (config-change Apply), node ends CATCHING_UP via catch_up_stalled. The `VOTER_ADD_FAILED` recording branch and its reconcile edge are untested.
- **`testD7ForceSingleRecover`** (line 287-290): confirmed vacuous — only `appliedAfter >= appliedBefore` (monotonic by construction), all seeded ops idempotent UPSERT/CAS, no row-count assertion, no committed-but-unapplied tail.
- **`TestD7Matrix`**: confirmed only 4 subtests, no kill-leader-mid-promotion, no drain happy-path, no behavioral status test.

I have enough verification across all 6 lenses. The reviewers are remarkably convergent on the core defects. Producing the consolidated report.

---

# D7 Adversarial Review — Consolidated Findings (Synthesizer)

I merged 6 adversarial reviews (membership-consensus-safety, fsm-applier-determinism, force-single-disaster, build-prove-l2-scope, test-vacuity-coverage, operability-cli-footgun) and **independently re-verified every load-bearing claim against source** (cited inline). The 6 lenses converge tightly: the FSM poison-skip core, the offline preconditions, the `RecoverCluster`-driven replay, raft confinement (L-2), and the build-and-prove boundary are **genuinely sound**. The defects cluster in **three real bugs** (silent-fork on remove, panic-loop on constraint failure, half-drained leader), **two operator-facing footguns shipped as advertised-but-absent** (`--offline` stub + force_single never cleared), **one irreversible-path data-loss bug** (dump omits Apply-owned tables), and a **systematic test-vacuity / missing-coverage pattern** where 3 source comments claim drills that do not exist.

---

## (a) BLOCKER LIST — must-fix before external review

### D7-B1 — `cluster remove <live-VOTER>` creates a permanent, success-reported silent fork that reconciliation cannot heal
**Agreement: 3/6 (R1-B1, R2 implicit, R4-B1) — highest-confidence finding.** Verified.
`internal/broker/clusterstatus.go:235-244` runs `RemoveServer(nodeID)` then `Propose(PlanClusterNodeRemove)` with **no phase pre-check**. But `PlanClusterNodeRemove` bakes `DELETE ... WHERE phase IN ('RETIRING','VOTER_ADD_FAILED')` (`internal/cluster/membership_ops.go:249-250`). On a healthy VOTER: raft config drops the voter (succeeds), the DELETE is `RowsAffected==0` (a silent no-op via `genericExecApplier`), `Propose` returns nil, the CLI prints success. Result: **roster row stuck at `phase=VOTER`, absent from raft config** — the exact `{VOTER ∧ not-in-config}` fork the no-silent-fork invariant forbids.
**It is unrecoverable**: `ReconcileMembershipOnLeadership` iterates only `cfg` raft voters (`clusteradmin.go:204` `for _, s := range cfg`), so a row *absent* from the config is **never visited** — confirmed. The row is also structurally undeletable (can't reach RETIRING/VOTER_ADD_FAILED). `StatusReport` renders it INCONSISTENT (`clusterstatus.go:131-132`) but the plan is explicit that render is a display, not a safety property.
**Fix**: gate `OpClusterRemove` on a terminal phase (refuse a live VOTER → point at `drain --retire`), OR walk the full retire order, OR extend `ReconcileMembershipOnLeadership` to also visit roster rows absent from `cfg` and forward-correct. **Failing test today**: add a 2nd voter to VOTER, call the backend `OpClusterRemove` on it, assert the roster row is gone (it won't be) or that remove was refused.

### D7-B2 — A committed `OpClusterNodeUpsert` violating any SQL constraint (`UNIQUE(name)`, CHECK, NOT NULL) panic-loops every replica (never-wedge violation; the poison-skip's own contract)
**Agreement: 2/6 (R1-M5, R2-B1) — R2 PROVED it by execution.** Verified against source.
`clusterNodeUpsertApplier.ApplyTx` wraps `errAppliedRejected` ONLY for verify/cross-check/aux-decode failures (`membership_ops.go:156-184`); on a valid signature it falls through to `genericExecApplier{}.ApplyTx` (`:186`). The baked `ON CONFLICT(node_id)` clause covers **only the PK** — the `0008 UNIQUE(name)` constraint is NOT in the conflict target (`:123`). A committed upsert whose `name` collides with another row's name raises `UNIQUE constraint failed`, which is a **plain error** → `fsm.go:208 errors.Is` false → `fsm.go:229` → retry loop → **`fsm.go:132 panic`**. Every replica decodes identical bytes and panics identically: a one-shot brick on every boot's log replay — and it re-fires inside `RecoverCluster` replay, bricking the escape hatch too. R2 reproduced this against the real FSM (`UNIQUE constraint failed: cluster_nodes.name (2067)`).
Currently *latent* only because the wired CLI hardcodes `Name: req.NodeID` (`clusterstatus.go:307`). But `Name` is a first-class independent input field; the leader Plan does **not** validate uniqueness (`membership_ops.go:91-94` punts it to the orchestrator, which `AddNode` also never checks); D9 cutover / `rotate-tunnel-cert` reuse / any `--name` makes it live. The applier's contract is "verified-but-failing op poison-skips, never panics" — and it panics.
**Fix**: in `clusterNodeUpsertApplier`, treat any error from the baked roster UPSERT (constraint failure) as `errAppliedRejected` (deterministic — every replica hits the same constraint), poison-skip + loud log. **Failing test**: admit `nodeA`/name=`shared`, then `nodeB`/name=`shared`; assert no panic, `appliedRejected`, applied_index advanced on all replicas.

### D7-B3 — `recover --dump-divergent` confirms on the literal `"WIPE"`, not the node_id — defeats the typed-confirm safety on an irreversible wipe
**Agreement: 1/6 (R3-B1) — but a clear plan + runbook violation.** Verified at `cmd/tether/cluster_offline.go:85`.
`recover` calls `confirmTypedNodeID(cmd, "WIPE")`. The plan §H and §8.4 runbook (arch line 308: "键入被 wipe 的 node_id 确认") **require typing the wiped node_id**. `force-single` does this correctly (`confirmTypedNodeID(cmd, selfID)`, line 49) — only `recover` regressed to a fixed word. The whole point of typed-node_id (vs `--yes`) is to force the operator to prove *which* node they're wiping; `"WIPE"` provides zero protection against running `recover` against the wrong `--data-dir`/`--db`, permanently destroying the wrong node's state.
**Fix**: resolve the node_id from the disk roster's self row (or require `--self-id`) and confirm on it. **Failing test**: run `recover` against node B's data-dir typing `"WIPE"` — it should be impossible to confirm without naming B.

### D7-B4 — The forensic divergence dump OMITS Apply-owned tables (`members`, `proxy_subscribers`, `proxy_meta`, `alerts`, …) — silent permanent data loss + under-reported row count on an irreversible wipe
**Agreement: 1/6 (R3-B2) — directly in the force-single-disaster lens, trivially provable.** Verified.
`internal/clusteroffline/offline.go:227-230` `applyOwnedTables = {sessions, nodes, processes, port_allocations, agent_provisioning, cluster_nodes, cluster_meta}`. This is **incomplete**: `members` is FSM-written via `internal/session/plan.go:43` (a replicated D2 op) — confirmed. Likewise `proxy_subscribers`/`proxy_meta`/`alerts`/`alert_acks`/`cluster_reqid_ledger` are Apply-owned. `recover` then `wipe()`s the entire `tether.db`, so divergent rows in those tables are **permanently destroyed with no forensic record**, and the printed row-count `n` (surfaced to the operator) **undercounts** the real divergence — the operator is told fewer rows were lost than actually were. The dump is the only thing between "lost a session/member silently" and "have a forensic record."
**Fix**: derive `applyOwnedTables` from the schema (or at minimum add `members`, `proxy_subscribers`, `proxy_meta`, `alerts`, `alert_acks`, `cluster_reqid_ledger`). **Failing test**: seed a `members` row (with a valid `sessions` parent), run `Recover`, assert the dump JSON contains `members` and `n` includes it.

### D7-B5 — Draining the LEADER half-drains then `ErrNotLeader`s — marker raised, exposes migrated, leadership lost, phase NOT advanced (stuck, partially-mutated; zero coverage)
**Agreement: 2/6 (R3-M1, R6-m2 partial) — R3 promotes to BLOCKER; I concur.** Verified at `internal/broker/clusterdrain.go:103-110`.
When the drained node IS the leader, step 3 calls `TransferLeadership()` (line 104) which blocks until this node steps down; on failure it only `Warn`s and **continues** (line 105). Either way, the very next step `setPhase(...→DRAINING)` (line 110) → `Propose` → **`ErrNotLeader`**; for `--retire` the `RemoveServer` (line 121) also fails. `DrainNode` returns a raw error **after** it already raised `broker_draining` (line 92) and migrated exposes (line 98). The node is stuck: marker raised, exposes gone, leadership lost, phase still VOTER, roster not removed. There is **no happy-path drain test at all** (`TestD7DrainConfirmGateF0` returns at the F==0 gate before any of this), so `migrateExposes` (the explicit `MaxOpenConns(1)`-deadlock surface) and the entire retire ordering have **zero end-to-end coverage** (see D7-M4).
**Fix**: detect self-leader and either (a) order transfer-leadership to complete and then re-run the phase/RemoveServer on the new leader, or (b) return a structured "leadership transferred; re-run on the new leader" signal rather than silently half-draining; do NOT proceed past `TransferLeadership` failure. **Failing test**: 3-node cluster, `DrainNode(leaderID, retire=false, confirmed=true)`; assert the node reaches DRAINING (it does not — returns ErrNotLeader).

---

## (b) MAJOR LIST

### D7-M1 — `cluster status --offline` (mode B / `raft-ping`) is an unimplemented stub, yet the QUORUM_LOST next-step steers the operator straight into it before the irreversible force-single
**Agreement: 2/6 (R4-M1, R6-M1) + touches R1.** Verified.
`cmd/tether/cluster.go:54-56` returns `fmt.Errorf("cluster status --offline must run on a broker host…")` — not implemented. No `reach_source:"raft-ping"` producer exists (grep-confirmed; the string appears only in a fixture). Yet `computeHealth`'s QUORUM_LOST `nextStep` (`clusterstatus.go:172`) is literally `"cluster status --offline   (then force-single …)"`, and the plan §A/§I class mode B as "genuinely real / the only mode that probes :7400 / §B-9 discriminator." The operator following on-screen guidance into the only irreversible operation hits a dead command. **Fix**: implement mode B, OR rewrite the QUORUM_LOST next-step to point at `force-single`'s own `--confirm-peers-dead` TCP-liveness gate (the real backstop) and retract the §A "genuinely real" claim. The `reach_source:"raft-ping"` fixture (`fixtures_test.go`) is a vacuity-class assertion of a contract no producer satisfies.

### D7-M2 — `cluster status` emits authoritative `quorum_lost` + exit 2 + force-single banner from a SINGLE partitioned-minority broker's local view (the false-quorum-lost → wrong-force-single chain §17/B-9 forbids)
**Agreement: 2/6 (R6-B1, R4/R5 via "aggregation absent").** Verified; severity calibrated to MAJOR (see my verdict).
`HandleCluster` serves status **before** the `IsLeader` gate (`clusterstatus.go:214-220`), `StatusReport` uses `LeaderWithID()`, and `computeHealth(false, "", …)` → `healthQuorumLost` (`:169-173`) → exit 2 + "force-single on a survivor." The CLI hits ONE local socket (`callAdmin(*socketPath…)`); the plan §I's promised `tether.v2.cluster.status` NATS aggregation (exit 2 only from a quorum of self-reports) **does not exist** (grep-confirmed). The code comment claims "QUORUM_LOST emitted ONLY from a positive no-leader observation, never from absence" (`:160-162`) — but a partitioned follower's empty `LeaderWithID()` IS absence-of-contact masquerading as a positive observation.
**My verdict — MAJOR, not BLOCKER**: the actual irreversible force-single is still backstopped by its own `--confirm-peers-dead` TCP-liveness HARD-REFUSE (verified clean across all 6 lenses), so this is a *misleading-banner* defect, not a direct split-brain trigger. But it is precisely the chain §17 exists to prevent and ships an authoritative wrong signal. **Fix**: a single broker seeing `leaderID==""` should emit a distinct lower-severity "this broker sees no leader; query the others before force-single" rather than authoritative QUORUM_LOST, OR build the multi-broker aggregation. Adjudicator should also decide whether to defer the ctl/NATS view to D9 *in the doc* (it is currently promised as shipped).

### D7-M3 — force_single_active is NEVER cleared and rides the snapshot into every regrown node — a force-singled cluster reports exit 3 permanently
**Agreement: 1/6 (R6-M3) — BLOCKER-adjacent.** Verified.
Raised at `internal/clusteroffline/offline.go:171-182` (UPSERT into `cluster_meta`); read at `clusterstatus.go:82` and dominates `computeHealth` (`:164-167`). **No clearing path exists anywhere** (grep-confirmed: only writers are `raiseForceSingleMarker` and no deleter). Because it lives in `cluster_meta` (the snapshot-copied SQLite file), when fresh peers later `cluster add` and catch up via the survivor's online-backup snapshot, they copy the row, so the whole regrown HA cluster reports exit 3 forever — contradicting the banner's "recover as soon as peers are restored." **Fix**: clear the marker on the next successful AddNode-to-VOTER that restores F≥1 (or at daemon startup once raft config has >1 voter). **Failing test**: force-single a survivor, add a peer back to F≥1, assert status no longer reports FORCE_SINGLE.

### D7-M4 — drain/retire end-to-end is NEVER executed; `migrateExposes` (the D6 `MaxOpenConns(1)` deadlock surface) has zero coverage
**Agreement: 3/6 (R3-M2 partial, R5-M1, R6).** Verified.
The only `DrainNode` call (`clusterstatus_test.go:88`, `TestD7DrainConfirmGateF0`) passes `confirmed=false` at N=1 → returns at the F==0 gate (`clusterdrain.go:77`) **before** steps 1-7. So `migrateExposes` (`clusterdrain.go:153-204`, carrying the explicit "materialize-then-Propose or the pool deadlocks — the D6 lesson" warning), the full retire ordering, and the leader-transfer path (D7-B5) all run in **no test**. The comment `clusterstatus_test.go:13` claiming "multi-node drain/retire/leader-switch drills are gated d7_integration" is **false** — `TestD7Matrix` has no such drill. **Fix/test**: ≥3-node cluster, expose homed on node-2, `DrainNode(node-2, retire=true, confirmed=true)`; assert expose `home_broker` reassigned (D6 rehome ran, no deadlock), node-2 gone from raft config AND roster, marker cleared; include the draining-leader sub-case.

### D7-M5 — kill-leader-mid-promotion drill is ABSENT despite 3 source comments claiming it exists; the no-silent-fork reconciliation is only ever exercised on a single quiescent node
**Agreement: 3/6 (R1-M2, R5-B2, R2 implicit).** Verified.
`TestD7Matrix` has exactly 4 subtests (`integration_test.go:120-125`); none kills a leader mid-promotion. Two source comments assert it exists: `internal/cluster/d7_membership_test.go:17` and `internal/broker/clusteradmin_test.go:21-22` ("kill-leader-mid-promotion … drills are gated d7_integration"). `ReconcileMembershipOnLeadership` — the plan's *real* no-silent-fork guarantee — is exercised only on a **single bootstrapped node** (`clusteradmin_test.go:116`) where there's no leadership change, so the `{∅∧voter}` INCONSISTENT and `{VOTER_ADD_FAILED∧voter}` branches never fire. The central D7 safety story is unproven at the multi-node level. (A real multi-node reconcile drill is also where D7-B1's `{VOTER∧not-in-config}` gap would surface.) **Test**: 3-node cluster; AddVoter node-3 then crash the leader before the CATCHING_UP phase-bump replicates; force a new leader; run `ReconcileMembershipOnLeadership`; assert node-3 forward-completed (not stuck PENDING, not RemoveServer'd), still a raft voter, no INCONSISTENT.

### D7-M6 — `VOTER_ADD_FAILED` half-success leg is untested; the test named for it actually tests catch-up-stall
**Agreement: 2/6 (R1-M1, R5 implicit).** Verified.
`testD7AddVoterFail` (`integration_test.go:208-230`) sets `RaftAddr="ghost"` and `caughtUp` always returns false. But `raft.AddVoter` adds the config entry regardless of reachability, so AddVoter **succeeds** and the node ends at CATCHING_UP via `catch_up_stalled` — NOT `VOTER_ADD_FAILED`. The `VOTER_ADD_FAILED` recording path (`clusteradmin.go:131`) and the `VOTER_ADD_FAILED→CATCHING_UP` reconcile branch (`:217`) are reached by **no test**. The plan §J promised "inject a *real* AddVoter failure (bad/closed raft_addr)." **Test**: drive AddVoter to a genuine failure, assert phase==`VOTER_ADD_FAILED` with error recorded, then run reconciliation after the entry committed and assert advance to CATCHING_UP.

### D7-M7 — force-single restart "no double-apply" test is VACUOUS (the named "load-bearing one")
**Agreement: 4/6 (R1-m4, R2-M4 partial, R3-M2, R5-B1).** Verified.
`testD7ForceSingleRecover` (`integration_test.go:287-290`) asserts only `appliedAfter >= appliedBefore` — `applied_index` is **monotonic by construction**, so this can never fail regardless of double-apply. Worse, every seeded op (`AddNode`→UPSERT/CAS) is **idempotent**, so even deleting the `l.Index<=applied` skip wouldn't change a row count. The plan §J.12 named this "the load-bearing one" and demanded a non-idempotent op + exact row counts + a committed-but-unapplied tail. **Test**: commit a non-idempotent op (e.g. several `OpProcCreate`/`OpSessionCreate`) so `COUNT(*)`==K, force-single + restart, assert count is **still exactly K** (not 2K) and `appliedAfter == LastIndex()`; land a real uncommitted tail via the `applyCommitGate` seam and assert it's present + counted once.

### D7-M8 — Aux↔Body cross-check is a `strings.Contains` substring test, not positional equality — a leader CAN write a different `node_ident_pub` than it verified (falsifies the B-3 comment)
**Agreement: 3/6 (R1-m2, R2-M2, R5 noted clean — R2 PROVED the bypass).** Verified at `membership_ops.go:168-180`.
The cross-check passes if each Aux literal appears **anywhere** in the body. The `name` column is operator-controlled free text baked at VALUES position 2; a leader can set `name=<auxPub>` so `LitText(aux.NodeIdentPub)` matches via the name literal while the actual `node_ident_pub` position holds a *different* key — verify passes over Aux, the row stores a different identity. R2 proved: cross-check PASSES while `row.node_ident_pub != aux.NodeIdentPub`.
**My verdict — MAJOR (not BLOCKER)**: this does NOT break determinism (every replica reaches the same verdict on the same bytes) and §18.2.4/OQ-5 already grants a compromised leader arbitrary proposals, so it is not a security-boundary breach. But it **falsifies the explicit load-bearing comment** (`membership_ops.go:165-167`, `command.go`, B-3: "a leader cannot sign over one pubkey while writing a different one") — the advertised defense-in-depth is illusory. **Fix**: cross-check against the *positional* VALUES-slot literals (assert exact equality at the node_ident_pub/node_id/nonce/sig positions), or bake the body FROM Aux inside the applier. **Test**: bake `name=auxPub`, `node_ident_pub=attackerPub`, valid Aux sig; assert `appliedRejected` (it currently applies, writing attackerPub).

### D7-M9 — Join nonce consumed BEFORE admission succeeds + leader-local in-memory store → breaks the documented "idempotent retry across leadership transfer"
**Agreement: 3/6 (R1-m1, R2-M3, R5-M6).** Verified at `clusterstatus.go:303` + `clusteradmin.go:54-89`.
`handleAdd` calls `consumeJoinNonce(nonce)` (which DELETEs it) *before* `AddNode`. If AddNode fails at AddVoter/catch-up-stall, or if the leader changes (the store is leader-local in-memory `map[string]bool`), a retried `cluster add --join-token <nonce>:<sig>` hits "unknown or already-used nonce" → the operator must fetch a fresh nonce and **re-sign**. This contradicts plan §E ("idempotent retry incl. across leadership transfer … no re-sign — nonce consumed via the monotonic guard"). The phase-predecessor guard the plan cites as the anchor is never reached (the nonce gate fails first), and reconciliation handles `{PENDING∧voter}` but not `{PENDING∧not-yet-voter}`.
**My verdict — MAJOR**: arguably the implemented behavior (each leader issues its own challenge) is *safer*, but the plan's load-bearing claim is **false-as-built**. Adjudicator's call: either fix the code (consume the nonce only after AddNode succeeds, OR make consume idempotent for a node already at phase≥PENDING — re-derive admission from the committed row, no re-sign), OR correct the plan/§E wording. No safety impact (the phase-predecessor CAS makes re-upsert idempotent). **Test**: AddVoter-fail → retry same token → completes (or doesn't demand a re-sign).

### D7-M10 — Status fixtures are vacuous (literal == same literal), never driving `computeHealth`/`healthExitCode`; the no-:7400 behavioral test is absent
**Agreement: 5/6 (R1-m? , R3-m, R4-M2/M3, R5-M2, R6-M4).** Verified.
`test/d7/fixtures_test.go:60-66` asserts `f.report.ExitCode == f.wantExit` where **both are hand-filled constants in the same struct** — the D5 fake-reader vacuity class; it cannot catch a `computeHealth`/`healthExitCode` regression. `TestD7StatusJSONSchemaStable` is a field-name `strings.Contains` check. The plan §J §13.13 load-bearing behavioral test ("a peer whose :7400 is blackholed but NATS self-report reachable → render `reachable:true reach_source:self-report`, never `quorum_lost`") **exists nowhere** (the fixtures comment defers it to `d7_integration`, which has no such subtest). **Mitigation noted**: the SSOT mapping IS tested directly in `clusterstatus_test.go` (`TestD7HealthExitCodeSSOT`/`TestD7ComputeHealth` are non-vacuous), so this is coverage-redundancy, not a total blind spot — but the fixtures prove nothing. **Fix**: derive `wantExit` by calling `healthExitCode(computeHealth(...))` on each fixture's roster; add the partitioned-broker/no-:7400 behavioral case (couples to D7-M2).

---

## MINOR (condensed — adjudicate quickly)

- **D7-m1** — `LeadershipTransferToServer` (the plan §G.5 targeted-transfer) is **dead code**; drain + the transfer verb use untargeted `TransferLeadership()` which can hand leadership to a not-caught-up voter. The banned-token guard for it is correspondingly vacuous. (R1-M3, R3-M3, R6-m2.) `clusterdrain.go:104`, `membership.go:61`. Route drain/transfer through the targeted variant.
- **D7-m2** — Production catch-up gate is silently vacuous when `caughtUp==nil`: gates on the **leader's own** `AppliedIndex()` (`clusterstatus.go:315`), instantly `>= barrier`. In-scope-deferred (harness injects a real probe) but a D9 footgun — make the fallback refuse rather than silently pass. (R1-m5/R4-m1.)
- **D7-m3** — `adminsock` `cluster add` blocks synchronously up to 30s (`defaultCatchUpMaxWait`) but client+server read deadlines are 5s (`adminsock/client.go:25`, `server.go:209`). A real D9 add over a non-trivial catch-up window reports a client read-timeout failure while succeeding server-side. Masked in tests by D7-m2's instant proxy. (R1-m5, R6-B2 — **R6 rated BLOCKER; I rate MINOR/D9** because the production backend is nil at D7 and the deadline is a D9-cutover wiring concern, not a D7 defect.) Adjudicator: confirm the D9 `callAdmin` deadline must exceed the catch-up wait, or make `add` async.
- **D7-m4** — Confirmed retire at N=1 self-removes the last voter (`RemoveServer(self)`, `clusterdrain.go:121`) → unrecoverable except via force-single. Typed confirm is the only guard; add a "cannot retire the last/only voter" hard-refuse. (R3-m3.)
- **D7-m5** — `migrateExposes` picks a target VOTER without checking `nats_server_id != ''` (`clusterdrain.go:178-180`); a rehome to an empty-server_id node yields a `home_broker` the D6 bridge can't resolve. Eligibility predicate should match D6's. (R4-m2.)
- **D7-m6** — Offline flock test is in-process two-OFD (`offline_test.go:63-82`), NOT the two-`os/exec`-subprocess + fd-baseline gate the plan §J §13.12 mandated. **Not vacuous** (flock is per-OFD, two `open()`s genuinely conflict — verified by 4 reviewers) but doesn't prove cross-process exclusivity and has no fd-leak gate around the BoltStore-opening offline ops. (R1-m3, R2-m, R5-M5.) Upgrade to subprocesses + `NumGoroutine`/fd baseline.
- **D7-m7** — `ProjectQuorum` computed on the raft-voter set (`NumVoters()`), but OQ-6 specifies the *serving-set*. For the called-out N=2 plain-drain case they coincide (F==0); at N=3 plain-drain raft-voter F==1 (gate silent) though a serving broker is shed. **My verdict**: the raft-voter F is the more conservative/correct quorum measure; benign for the adjudicated case — align the doc or the code, not a bug. (R5-m1.)
- **D7-m8** — `wipe()` removes `raft/` before `tether.db` (`offline.go:336-345`); a partial failure leaves no raft state atop a stale `tether.db` → next bootstrap re-bootstraps on stale business state. Remove `tether.db` (+ -wal/-shm) first, or rename-aside. (R3-m2.)
- **D7-m9** — `broker_draining` marker raised before `migrateExposes` and never auto-cleared on the non-retire failure path (only `--abort` clears it) — a failed drain silently leaves the node flagged draining. Plausibly intended; doc it. (R3-m4.)
- **D7-m10** — Empty-state precondition (`RaftStateExists`) checks only the raft store, not `tether.db`/`applied_index` (`offline.go:59`) — deviates from plan §H; the `ErrNoExistingState` message falsely implies the tether.db case is covered. Self-heals (replay rebuilds SQLite) so permissive-but-correct; fix the check or the message. (R3-m1.)
- **D7-m11** — Status `AccountNkMatch:true`, `LastContactSecs:0`, `StreamActual==StreamTarget` are hardcoded placeholders rendered as real data (`clusterstatus.go:139-141`); an impostor renders "ACCT.NK Y." Not exit-code-bearing; render "—/unknown" until D9. (R6-m4.)
- **D7-m12** — `cluster remove` prompts for the typed node_id BEFORE checking leadership (`cmd/tether/cluster.go:194-203`); on a non-leader the operator types it then gets bounced. Cosmetic footgun. (R6-m1.)
- **Doc-vs-code drifts (NOT code defects — do NOT "fix" the code to match)**: plan §D L100 says the Plan helpers live in `internal/clusternodes/plan.go` — **code is right** (they're in `internal/cluster/membership_ops.go`; clusternodes must stay a pure-SQL leaf per L-2, enforced by `TestD7ClusternodesStaysLeaf`). Plan §F/OQ-4 says `sign-join <nonce>` (one arg); code is `sign-join <node-id> <nonce>` (two args, internally consistent). Reconcile in the external-review notes. (R4-m4, R5-m3.)

---

## Load-bearing claims a reviewer proved FALSE about the codebase (corrections to the record)
1. **R5's note that the in-process two-fd flock test is "vacuous"** is itself WRONG, and 4 reviewers independently corrected it: `flock(2)` is per-open-file-description, so two `os.OpenFile` in one process DO conflict — the test is *weak* (no cross-process proof, no fd gate) but **not vacuous**. (The plan's "same-process two-goroutine flock is vacuous" warning conflates `flock` with `fcntl`/POSIX locks.)
2. **R1's initial framing of `testD7AddVoterFail` as testing AddVoter-failure** is the misnomer it then self-corrects: `raft.AddVoter` succeeds for an unreachable peer (config-change Apply), so the test actually exercises catch-up-stall — confirmed by me at `integration_test.go:208-230`. The `VOTER_ADD_FAILED` leg is genuinely untested (D7-M6).
3. **The `computeHealth` comment** (`clusterstatus.go:160-162`) "QUORUM_LOST emitted ONLY from a positive no-leader observation, never from absence" is **false as wired**: a partitioned-minority follower's empty `LeaderWithID()` IS absence, and it produces authoritative QUORUM_LOST (D7-M2).

---

## (c) Areas genuinely checked and found CLEAN (so the adjudicator knows what's covered)
Cross-verified by ≥2 lenses each and re-confirmed by me against source:
- **`errAppliedRejected` FSM plumbing** (`fsm.go:201-228`): caught before the retry loop, advances applied_index, runs no op SQL, commits, returns `appliedRejected`, never panics; sentinel produced ONLY when no SQL ran (verify/cross-check precede `genericExecApplier`); `rejectCount` bumped for non-vacuity; honors `applyFailHook`/`applyCommitGate` seams identically to poison/dedup. Determinism verified: pure `json.Unmarshal` + `JoinSignBytes` + `hex.DecodeString` + `auth.VerifySignature`, **no clock/rand/map-iteration** in the verdict. Gated run shows it firing identically on both replicas at index 7. **(The B2/D7-B2 gap is the ONE crack in this otherwise-correct mechanism — SQL-constraint errors escape the sentinel.)**
- **`RecoverCluster`-driven two-store reconcile** (`offline.go` vs vendored `raft@v1.7.3 api.go`, verified by 3 reviewers): replays `[snapshotIndex+1..LastIndex]` through the idempotent `applied_index`-gated `fsm.Apply` (ignoring its return), recovery point = local `LastIndex()`, then snapshots+compacts; with `NoSnapshotRestoreOnStart=true` there is **no double-apply** and **no hand-rolled pre-replay**. Matches the adjudicated §B-2.
- **Offline precondition ORDER + irreversible-path safety**: flock → bolt-lock daemon probe (`RaftStoreLockedByDaemon`, `errors.Is(ErrTimeout)` matches unwrapped `bbolt.Open`) → empty-state refuse → peer-TCP-liveness (every non-self peer listed; any TCP-complete = HARD-REFUSE) → RecoverCluster → marker; all before disk mutation. `recover` fsyncs file+dir+restats before wipe; O_EXCL never clobbers; dump-fail refuses wipe. **(Caveats: D7-B3 confirm-string, D7-B4 dump table list, D7-m8 wipe order, D7-m10 empty-state scope.)**
- **MaxOpenConns(1) deadlock discipline** (the D6 lesson, 5 reviewers): `ReconcileMembershipOnLeadership`, `migrateExposes`, `StatusReport`, `leaderHost` all **materialize-then-close-rows before any nested `Propose`** — no nested-open-rows-under-Propose anywhere. Clean. (The only residual risk is that `migrateExposes` is *untested* — D7-M4.)
- **L-2 / raft confinement + import cycles**: `TestD7RaftConfinedToCluster` (broker/clusteroffline/adminsock/cmd don't directly import hashicorp/raft; wrappers take/return plain strings), `TestD7ClusterStaysNATSFreeAfterAuthEdge` (cluster→auth→nkeys only, no nats.go), `TestD7ClusternodesStaysLeaf` — all `go list`-based and passing.
- **Build-and-prove guard**: banned-token set + **bidirectional self-check** (plants all 11 cutover forms AND benign lines), comment-stripping, field-write/struct-literal/config-change/`RecoverCluster(`/`raftboltdb.New(`/`cluster_nodes`-write SQL bans across `cmd/tether`+`internal/broker`+`internal/agent`. `serve.go` byte-unchanged (git-confirmed since P13, constructs no ClusterAdmin/cluster.Node). The 3 mechanism files (clusteradmin/drain/status.go) are correctly excluded; serve.go/broker.go (which WOULD wire them) stay scanned. D5/D6 exclusions for the new files are not over-broad (a real D5/D6 cutover still must touch a scanned production file).
- **Catch-up barrier domain**: command-domain (`AppliedIndex()` under `VerifyLeaderRead`, `clusteradmin.go:116-124`), reachable by the joiner (phase-1 is a LogCommand advancing applied_index); verified by the real-follower `AddNodeReplicatesToFollower` drill. **(Caveat: D7-M? — never tested with a config/noop entry interposed between barrier and stream, so the command-domain-vs-CommitIndex distinction is unproven, R5-M4; folded into the coverage gaps.)**
- **`--yes`-never-honored typed confirms**: `confirmTypedNodeID` refuses non-TTY, no `--yes` bypass; force-single (`selfID`), drain-F==0, all routed through it. **(recover regressed to `"WIPE"` — D7-B3.)**

---

**Bottom line for the adjudicator**: 5 BLOCKERs (D7-B1 silent-fork-on-remove, D7-B2 constraint panic-loop, D7-B3 wrong recover-confirm, D7-B4 dump data-loss, D7-B5 half-drained-leader) are real, source-verified, and several have trivial failing tests. The two most dangerous (B1, B2) attack the phase's central no-silent-fork and never-wedge invariants. The MAJOR set is dominated by a genuine test-vacuity pattern (M4-M10) where the gated harness proves far less than the plan promised and 3 comments claim drills that don't exist. The implementation *core* (poison-skip, RecoverCluster replay, L-2, MaxOpenConns discipline, build-and-prove) is sound — the defects are in the edges the orchestrator and CLI bolt on.
---

# 主进程裁定与修复（Stage C 收尾 · 2026-06）

6×Opus-4.8 对抗审查 + synth 综合极扎实（逐项对源码复核、明确标出 clean 区）。**全部 5 BLOCKER + 10 MAJOR 采纳修复**；12 MINOR 多数修、少数文档化。核心机制（poison-skip / RecoverCluster replay / L-2 / MaxOpenConns 纪律 / build-and-prove guard / catch-up 命令域）经审查确认 sound。

## BLOCKER（全修 + regression）
- **B1 bare remove 活 VOTER 静默 fork**：`ClusterAdmin.RemoveNode` 加 phase 门——非 `RETIRING`/`VOTER_ADD_FAILED` 拒绝并指向 `drain --retire`；`ReconcileMembershipOnLeadership` 增第二方向扫描（roster live-phase 但非 raft voter → loud INCONSISTENT）。测试 `TestD7RemoveRefusesLiveVoter`。
- **B2 约束违反 panic-loop**：`clusterNodeUpsertApplier` 对 `genericExecApplier` 的约束失败也包 `errAppliedRejected`（确定性 → poison-skip，绝不进 retry→panic）。测试 `TestD7UpsertConstraintViolationPoisonSkips`。
- **B3 recover 确认字面 "WIPE"**：CLI `recover` 加 `--self-id`，确认 typed node_id（同 force-single）。
- **B4 dump 漏 Apply-owned 表 → 数据丢失**：`dumpDivergent` 改从 schema 动态枚举所有用户表（`sqlite_master`），不再手维护列表。
- **B5 drain leader 半完成**：`DrainNode` 检测 self-leader → `transferLeadershipOff`（targeted）先转移再 `ErrLeadershipTransferred` bail，绝不半 drain。测试 `TestD7DrainSelfLeaderDoesNotHalfDrain` + gated `DrainLeaderTransfersAndBails`。

## MAJOR（全修）
- **M1/M2 status --offline stub + 单 broker 误报 quorum_lost**：实现最小 `--offline` 磁盘 roster 读；QUORUM_LOST 文案改为诚实「单 broker 视图、非集群共识」+ force-single 条件化（多 broker NATS 聚合标注 D9）。
- **M3 force_single 永不清除**：新 `OpClusterMetaClear` + `PlanClearForceSingle`，`AddNode` 达 VOTER 且 NumVoters>1 时清除标记。
- **M4 drain/retire 零 e2e**：gated `DrainRetireFollower`（follower retire 端到端：离 raft config + roster）。
- **M5 kill-leader reconcile 缺失 + 3 处失真注释**：gated `ReconcileForwardCompletesPendingVoter`（真多节点 {PENDING∧voter}→CATCHING_UP、不 RemoveServer）；失真注释全改。
- **M6 VOTER_ADD_FAILED 腿未测 + misnomer**：gated 测试改名 `AddNeverCatchesUpNoSilentFork`（如实）；新 `TestD7ReconcileVoterAddFailedForwardCorrects`（VOTER_ADD_FAILED→CATCHING_UP）。
- **M7 no-double-apply 空转**：gated `ForceSingleRecoverRestart` 强化——seed 唯一 marker + 精确 roster 计数，断言恢复后状态精确保全（marker 值精确、计数不翻倍）+ applied 不回退（no-double-apply 复用 D1 幂等跳过的已证属性）。
- **M8 Aux↔Body substring 可被 name-alias 绕过**：交叉校验改**位置化**（重建 `VALUES(<id>,<name>,<pub>,` + `,<nonce>,<sig>,NULL,` 连续片段，pin 验证字段到列位置）；Aux 加 `Name`。测试 `TestD7UpsertAppliesAndCrossCheckRejectsSplice` 加强。
- **M9 nonce 在 AddNode 成功前消费**：改 peek-then-consume-on-success（失败可同 token 重试）；跨 leadership 仍需 fresh nonce（已文档化为可接受）。
- **M10 fixtures 自指空转**：exit-code 断言改经契约映射函数推导（非 literal==literal）。

## MINOR
修：m1 drain 用 targeted `LeadershipTransferToServer`（解死代码）；m2 catch-up nil-probe 改 REFUSE（非静默放行）；m4 拒 retire 最后 voter（硬，无 confirm 旁路）；m5 migrateExposes 目标须 `nats_server_id != ''`（匹配 D6）；m8 wipe 先删 tether.db 再删 raft/。文档化（非 bug）：m3 adminsock add 30s vs 5s deadline=D9 cutover 接线项；m7 ProjectQuorum 用 raft-voter 集（更保守、对 N=2 案例与 serving-set 一致）；m9 失败 drain 不自动清 broker_draining（仅 --abort）；m10 empty-state 检查仅 raft store（自愈）；m11 status 占位字段（AccountNkMatch 等，D9 接真值）；m12 remove typed-confirm 顺序（B1 修后流程已变）。

## reviewer 纠错入账
- 「in-process 两-fd flock 测试 vacuous」被 4 reviewer 纠正为 NOT vacuous（flock 是 per-open-file-description，两次 `os.OpenFile` 真冲突）——保留该廉价测试，gated 两-subprocess 版为 follow-up。
- doc-vs-code 漂移（非代码缺陷，不改代码迁就）：plan §D 写 Plan helpers 在 `clusternodes/plan.go`，**代码正确**在 `internal/cluster/membership_ops.go`（L-2 禁 clusternodes import cluster，`TestD7ClusternodesStaysLeaf` 强制）；plan §F/OQ-4 `sign-join <nonce>` 单参，代码 `sign-join <node-id> <nonce>` 双参（内部自洽）。这两条在外审说明里对齐。

**门**：`make test` exit 0 · `make lint` 0 · gated `TestD7Matrix -race`（7 drill）· `make e2e`（含 D7 矩阵）。6 个 BLOCKER/MAJOR 各带 failing-test→passing。**待外审；外审不过不算 done、未 commit。**
