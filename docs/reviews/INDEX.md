# docs/reviews — 索引

> 每个 phase 收尾（CLAUDE.md §3 step 7）在此追加一行。归档判据见该条款：
> `docs/` 顶层只放"下一次改代码时还会被读"的活文档；描述的工作一旦完成，它就是过程产物，`git mv` 进这里。
>
> 存量的 389 份**不回填**——条款从此刻起生效，回填一次并不会让下一次自动发生，而条款会。
> （389 = `git ls-files docs/reviews | grep -c '\.md$'`，即本增量之前已跟踪的份数。写出命令而不只写数字：
> 本轮闭合核验反复抓到的一类缺陷就是「公布了一个没人能重新导出的数字」，包括我自己publish 的好几个。
> 顺带三个口径都不同，容易混：顶层 `ls docs/reviews/*.md` = 368，递归 `find` = 392，已跟踪 = 389。）

| 日期 | 增量 | 一句结论 | plan |
|---|---|---|---|
| 2026-08-29 | proxy-lifecycle · 把 agent 的 SS proxy 生命周期从 per-session `runCtx` 解耦 | weilandserver 对外服务了一个**死出口 7h40m**，而 broker 全程报 `READY=true`：SS server 被锚在**每个 session 都会重写**的 `runCtx` 上，一次普通 NATS 重连即杀死数据面，而 `p.srv` 仍指着尸体、register 回包按设计是 keyset-only，于是永远撞 `default:` 分支（`SetKeys err="server stopped"` 5416 条／4.3 MB）；那句 READY 谎言还是 agent 自己经 exact-equal re-ACK 发出去的。修法是**不接受 caller 的 ctx**（而非"不要 ctx"，见下），使停止者成为闭合四项：权威 OFF／fail-closed／teardown-then-rebuild／agent 退出；顺带修掉 `Start` 对已停止 server 返回 `(oldPort,nil)` 的假成功、以及 upstream 不在关闭集导致的无界 `Stop`。零 wire 改动、agent-only、双向可回滚。**首轮外审 Fail**（4 Major 各带独立反例）逐条修复：F1 `Stop` 无法取消 in-flight DNS（改为 Server **自有** `stopCtx`）／F2 hard revoke 漏关 upstream 半边／F3 持久化 `LocalPort` 冲突使自愈永久变暗／F4 single-broker 非重连 corpse 无自愈边，**复审 Pass**（无 Blocker/Major，drill 73 GREEN 46 断言、数据面 ~26s AUTO-RECOVERED）。教训两条都关于**我否决别人的方式**：①F4 推翻了我的内审驳回——我读了 `repairProxy` 的 Fix D 抑制条件却漏读它**上面**的 CONVERGENCE-FIRST 早退，而 corpse 恰好完全匹配后者，我用这个漏读否决了三个独立 lane 并把错误写进代码注释；②外审刚指出"测函数不测接线"，我为 F4 写的测试**立刻重犯**（直接调 helper，删掉 `heartbeatLoop` 里的接线照样绿）。plan §3「SS server 不接受任何 ctx」也被证明**过度推广**：对生命周期所有权成立，被我推广成了对取消能力的禁令，而后者恰是修 hang 所需。变异账本 **23 条，其中 4 条一开始打不红**（含两个我自己的空转守卫：特权端口 1、恒等式 ctx 行）。⚠️ 仍 OPEN：`Stop` 在 `p.mu` 下的**总** teardown 预算未成显式契约；#33 维持 CANDIDATE | [plan](proxy-lifecycle-plan.md) · [内审](proxy-lifecycle-review.md) · [外审 Fail→复审 Pass](proxy-lifecycle-external-review.md) · [外审 tasklist](proxy-lifecycle-external-review-tasklist.md) · [复审 tasklist](proxy-lifecycle-external-rereview-tasklist.md) |
| 2026-08-11 | g75-g78 · 部署默认值收口（#75 strict `--config-check` / #76 units enable / #77 journald cap / #78 proxy participate opt-out） | 四条部署短板一并收口，全程**有序更新**（v0.5.0 起不再拆-重建）。#75 给出无副作用 `serve --config-check`（strict YAML + 与真实 `serve` 同源的 `broker.ValidateConfig`，不开 DB / 不 bind / 不写文件）；#76 install.sh `systemctl enable`（不 start）+ `--no-enable`；#77 journald `SystemMaxUse` 按盘三档 + marker ownership（不认领 operator 文件）；#78 agent.yaml `proxy.participate=false` FOLD 进 `nodes.proxy_capable`（**不新增 raft 字段**，N-1 零值=全参与）+ single-mode 收敛 free。首轮外审 **Fail** 的 F1–F10 逐项采纳；二次独立外审又发现 NATS pool 跨 transport 组合与非 IP `%zone` 两个 parser parity 缺陷，授权直修并保留差分守卫后 **Pass**。deploy-tier 对 32/78 做可恢复负向验证；最终 `-race`/`make test`/`make gates`/99-unit e2e/simcluster hermetic 全绿，重建镜像 live 93 无 assert/setup/product red。⚠️ 仍 OPEN:#29/#34 多-broker cluster home eligibility(独立后续增量)、drill 78 Arm C post-TLS WARN 与 drill 93 webhook lease-jitter runtime-guard 诚实 `not_covered` | [plan](g75-g78-deploy-defaults-plan.md) · [内审](g75-g78-deploy-defaults-review.md) · [首轮外审 Fail→整合](g75-g78-deploy-defaults-external-review.md) · [二次外审 Pass](g75-g78-deploy-defaults-external-rereview.md) · [二审 tasklist](g75-g78-deploy-defaults-external-rereview-tasklist.md) |
| 2026-08-06 | smalldisk · 让小盘 broker 上的 tier-B 传输可用 | racknerd 的 tier-B 五个月发不出去：一个**空的** 8 GiB OBJ_xfer 桶按 legacy cap 预留了 10.33 GiB store 的 77%，任何 create 都撞 10047。修法是把桶**按需**定尺（一次最大传输 + 分块余量，而非磁盘比例）、预留量按会话数×副本数缩放并绑到 `jsstream` 导出常量、以及 **resolve-before-create**（现存桶走 `STREAM.INFO`，不经额度检查）。教训两条都关于**「名字相同的两个东西」**：先把 go.mod 的 nats-server v2.14.0（嵌入式测试 server）当成生产的 v2.10.22，再把 `/jsz` 的 `max_storage`（**服务器级**）当成 `AccountInfo.Limits.MaxStore`（**账户级**，生产恒为 -1）——第二次让收敛整条成了生产死代码，被三个内审 reviewer 独立抓到。外审 PASS 但直接修了 6 Major（量纲混用、收敛误缩并清空 `Placement`、同桶并发撞 `DiscardNew`、payload 上限未扣分块开销、准入忽略现存桶真实容量、pull 的大小只有 agent 知道），主进程复核后全部采纳，并补了外审**未留守卫**的 F3/F4（三条变异原本零失败）。⚠️ 收敛因 F2 的安全裁决在**每台真实 broker 上都不触发**，现网仍需一次运维缩桶 | [plan](smalldisk-plan.md) · [外审 PASS](smalldisk-external-review.md) · [tasklist](smalldisk-external-review-tasklist.md) |
| 2026-08-05 | h1 · ps 有界化 + 存储 GC + 可靠 proc 事件 + ctl 存活 + 日志上限 | 2026-08-04 `tether ps` 全员超时事故的根治：ports 回包有界（500 上限 + 截断位）、`respondLogged` 单一出口把吞掉的 `ErrMaxPayload` 变成 `reply_too_large`、FREED/EXITED 行走 raft 的保留期 GC、agent 侧持久化 proc 事件 courier、ctl 存活 reaper 收僵尸 PTY、broker/agent 日志进程内封顶 + panic sink。外审 Fail（1 Blocker 4 Major 2 Medium）逐条修复；复审又直接改了实现（R1/R2 两个 Major）并**推翻了我误报的 gotcha #75**（drill 80 用错会话的节点造假红）。主进程复核后全部采纳，并**延伸修掉 R1 自己漏掉的那半**：端口读失败仍会撤销全部隧道，其回归测试关掉整个 DB 导致端口路径从未被执行。教训入档：F3 的根因是日志流映射被复制十几份——所以修的是「只能有一份」这个结构（`drills/lib/logs.sh` + 闸门），不是那十几处 | [plan](h1-plan.md) · [内审](h1-review.md) · [外审+复审 Pass](h1-external-review.md) · [tasklist](h1-external-review-tasklist.md) |
| 2026-07-29 | 线二 · 质量闸门加固 | 装齐 S3 剩余闸门；实测推翻 S3 两条核心裁决（maintidx 非注释免疫、exhaustive 的 `default:` 逃生门）；lint 从 139 降到 0。**内审 59 块 + §5/§6 十四条全部处置**，其中三条影响生产行为（`node upgrade --all` 会把打错的 `--url` 继续扇给车队、incident bundle 静默丢审计正文、PTY 耗尽的 ENOSPC 落在终态支）。闭合核验推翻过我自己一次「全部完成」的结论——分母 53 是错的（真值 59），且三条我从未看见 | [line2-plan.md](line2-plan.md) · [line2-review.md](line2-review.md) · [外审](line2-external-review.md) · [复审 Pass](line2-external-rereview.md) |
| 2026-08-02 | upgrade follow-ups + gotcha #72（有界 teardown） | 三件遗留（DNS fidelity 门、专用 success drill 33、gotcha #72）一并收口。#72 的根因是 `nc.Close()` 排在 cancel 前、而 `doReconnect` 持 `nc.mu` 跨无 deadline 的 DNS/TLS/ws 写——现场卡了 10m58s。teardown 改为 S1–S5 有界 ladder（cancel-first → 限时 close → 粘性毒化 → self-exec/exit 91），恢复上界 ≈60s。外审直接改了实现（3 Blocker 起步的两轮共 13 条），主进程逐条复核后全部采纳：**F3 那轮我只修了两个 observer 站点中的一个**，`session()` 自己在 finalizer 发布前还留着一次 `ConnectedUrl`。5 条新守卫的变异验证由主进程补做，其中去掉 sibling 身份判据会让共享二进制被第三方降级成 OLD。**#72 仍 OPEN**（WSS 面在 simcluster 不可构造） | [plan](gotcha72-teardown-plan.md) · [drill plan](upgrade-success-drill-plan.md) · [内审](upgrade-followups-review.md) · [外审 Pass](upgrade-followups-gotcha72-external-review.md) |
| 2026-08-01 | upgrade-safety（N-1 更新规范 + 升级安全） | requirements §6.7 改写为 N-1 窗口（join 门为显式豁免）；wire 字段 append-only 账本入 gates；agent 升级状态机（冒烟纪元校验 / prev 槽 / marker / boot shim 预算 / register 提交 / 自动回退，host flock 一个升级域 + 四件套提交证明 + 有序 fsync）；ctl `--wait` fail-closed + `--all` 金丝雀。内审 30 条全处置；外审首轮 Fail（1 Blocker 6 findings）逐条修复，复审 2 Major 4 Minor 由外审者直接修复、主进程审查通过，最终 Pass。教训入档：管道 `\| tail` 吃退出码曾制造一轮假全绿 | [plan](upgrade-safety-plan.md) · [内审](upgrade-safety-review.md) · [外审](upgrade-safety-external-review.md) · [复审](upgrade-safety-external-rereview.md) · [终审 Pass](upgrade-safety-external-final-review.md) |
| 2026-08-19 | 克隆凭证实例（同凭证多实例的租约命名） | 一个镜像烤进凭证后被同时启动多份，此前在 broker 眼里是**同一个 agent**——每条 exec 扇出给所有实例、回复无法归属。给每个**进程**一个 crypto/rand 实例 id（26 字符、永不落盘、经环境跨 `syscall.Exec` 传递），broker 据此裁决节点名：先到者保留原名，之后拿 `-02`/`-03`。不读任何机器信息（`/etc/machine-id` 在参考 pod 里是空的、`boot_id` 每内核共享，两条都已在现网证伪）。两轮 25 专家内审（第二轮 91 findings / 9 BLOCKER）+ 三轮外审。**deploy-tier drill 抓到了 hermetic 测试结构上够不到的东西**：三道硬闸全绿之后 drill 83 报出双执行回来了——现任的 register→subscribe 周转在容器主机上约 2s，而我为修一个无关的红把 `leaseGrantWindow` 压到了 1s；每个 hermetic 测试都在一个快进程里 register 完立刻 subscribe，没有一个能看见那个窗口。外审三轮各自在我上一轮的**修法**里找到新洞，其中两个是我自己引入的攻击面（`ConfiguredNID` 客户端可控却被当权威、`PreviousNID` 同样可控且直接驱动进程行迁移）。终审外审者直接改了实现（X1–X6），主进程复核后全部采纳，并**修掉他们引入的一个 fleet 级回归**：X4 把 `proposeOrForward` 换成裸 `Propose`，丢掉了 `ErrNotLeader` 映射——一次常规 leader 切换就会让全车队 agent 把它读成永久拒绝并退出进程 | [plan](cloned-credential-instances-plan.md) · [内审](cloned-credential-instances-review.md) · [内审 round2](cloned-credential-instances-review-round2.md) · [外审](cloned-credential-instances-external-review.md) · [复审](cloned-credential-instances-external-rereview.md) · [终审 Pass](cloned-credential-instances-external-final-review.md) |
| 2026-09-01 | remote-fs stale-health（gotcha #81）+ spawn runtime 隔离 | timan107 上一个跑了 18 天的 agent 在 NFS 挂死后 `exec nvidia-smi` 恒定 `remote_fs_spawn_timeout`、`--safe` 无效、重启才好：spawnsafe 的挂载健康判定 `stHealthy` 是**终态**（无 TTL、无再验证，`applyMounts` 按 signature 原样继承），所以"探测时活、之后才死"的盘永远不被剔出 `$PATH`，整套 v0.3.3 保护静默退化成两条死线。修法：证据驱动作废（spawn 停滞本身就是"某条 healthy 判定过期"的证据，4 个站点）+ healthy-only 惰性 TTL 兜底两类零证据负载 + 换指针重新武装；dead 绝对 sticky；零 wire、零新键。**三轮外审，前两轮 Fail**：首轮抓到我按 `-run` 正则**分组**跑变异互相掩蔽（一条恒等式测试因此漏网）、闸门只是 arm-level 布尔 + 线性 Lock 计数；复审抓到裸 `go`/`goto`/if-initializer 三个绕过，以及我的 drill 取证代码本身是坏的（全局变量在 `assert_ok` 子 shell 里丢失，产出一轮 5/5 假红）。**终审外审者直接改了实现并找到真正的根因**：Go 的 `CLONE_VFORK` 让父线程在 RawSyscall 里被内核挂起直到子进程 execve 完成，runtime 不知情，子 execve 在死 FUSE 上 D 住后下一次 GC 的 stop-the-world 冻住整个 agent——这同时解释了 #82（agent S 态停摆 + 两个 pre-execve D 态子进程）与 drill 62 反复的 `rc=124`；我此前三次归因（1S-3 → 1S-2 → 游移）全都指错了层，因为"放弃 goroutine"在结构上碰不到一个不可抢占的线程。现由 `internal/spawnexec` 经 `/proc/self/exe` helper 原地 `syscall.Exec` 隔离风险 execve，agent 只等可取消 pipe。外审者第一版 helper 在 helper 内二次 `cmd.Start` + 只给三个包手写 `TestMain`，裸跑时 `*.test` 递归执行整套测试，留下 5,789 个 `exe`、307 GiB RSS，触发主机 global OOM——已改为 `init()` 统一分派 + 原地 exec + 跨重启的 64 槽 CLOEXEC 内核租约。主进程复核后全部采纳，独立复跑全部门禁与两次全新镜像 drill。教训入档：**分组跑变异会互相掩蔽**；**取证工具坏掉时必须自曝而不是冒充产品判决**；**一条命令只有一个断言时，"控制面没回"与"产品码不对"在观测上不可分** | [plan](remote-fs-stale-health-plan.md) · [内审](remote-fs-stale-health-review.md) · [外审 Fail→复审 Fail→终审 Pass](remote-fs-stale-health-external-review.md) · [tasklist](remote-fs-stale-health-external-review-tasklist.md) · [复审 tasklist](remote-fs-stale-health-external-rereview-tasklist.md) · [终审 tasklist](remote-fs-stale-health-external-rereview-2-tasklist.md) |
| 2026-09-01 | 测试体系革新（B0–B6：收据、发布链、fuzz、租约时钟、登记闸、性质测试、矩阵去重） | 一句话：让"门说的"等于"门做的"。测试身份 golden（只增不减）、矩阵单元 golden（含 whole 矩阵字面量）、发布链并入 ci.yml 并 `needs` 三闸、`make fuzz` 发现全部 29 个目标、租约路径统一注入时钟 + 随机走模型（silent 事件 + B5 双向过期 Skip）、端口/FSM/subject/AEAD/invite 性质与 fuzz、run-all.sh 接进 `make gates`（此前在干净树上是红的且无人调用）、runner 按 `go list -race` 闭包 hash 去重（40→32 单元）。**内审 Fail-not-block（67 条，19 条 plan 偏离只登了 6 条）→ 外审 Fail（4 Major：发布门漏 `failure()`、leader 门两类假阴性、sleep 门一行绕过、splitter 漏 `CommandContext`/别名）→ 复审 Pass**，复审又抓到我修法里的 R1–R3（同名 helper shadow、任意调用当观测、别名只追一层），由外审者直接修复、主进程逐行复核。教训入档：**G1 记假比洞本身更该先修**（B3 ② 守卫不可达却记为"红"）；**账本/偏离清单不完整等于外审者要重新发现一遍**；`nohup` 下跑 `make test` 会让靠 SIGHUP 的 PTY 测试假红；**"精确名单"仍可被同名一次性函数 shadow——身份要按 import 路径或从实现证明** | [plan](test-system-overhaul-plan.md) · [基线](test-system-overhaul-baseline.md) · [内审](test-system-overhaul-review.md) · [外审 Fail](test-system-overhaul-external-review.md) · [tasklist](test-system-overhaul-external-review-tasklist.md) · [复审 Pass](test-system-overhaul-external-rereview.md) · [复审 tasklist](test-system-overhaul-external-rereview-tasklist.md) |
| 2026-09-03 | 发布前全库审计 · 增量 2（session 准入 + inbox 重设计）外审 Fail → 九条全采纳 | 外审判 **Fail**：2 Blocker + 4 Major + 3 Minor，四条独立红测全部命中生产接线。**B-1 是本轮最重的一条，也是唯一需要用户裁定的**：`_TINBOX` 只保护了升级后的回复，任何匿名合法 nkey 只要**不发** `InboxCapableMarker` 就拿到 `Sub _INBOX.>`，而那里流过的 tunnel token 与全部订阅者 PSK 是**长寿凭据**、升级后依然有效——需求文档同时写着「本 release 没有 N-1 例外」和「轮换是尚未实现的后续项」，自相矛盾。我先穷尽了兼容设计并确认**做不到**：老客户端在连上之后才自选随机回复主题，静态 per-connection ACL 无法表达「只准收你自己那条」，而 `_INBOX.*`+`_INBOX.*.*` 那版收窄也不行（服务端把订阅自身的 `>` 当字面 token，`_INBOX.>` 仍被准入）。三条路交用户裁定，选定**「秘密不进共享空间」**：判据 `auth.IsPrivateInboxSubject`（白名单，判错方向的代价是把 token 发进读者集合未知的空间），register reply 与 `/sub` URL 在共享 inbox 上省略凭据但**注册照常成功**——拒绝注册是更简单的代码和更坏的结果，节点永不 ONLINE 就意味着 `node upgrade` 这条唯一修复路径也断了。**零新 wire 字段**（复用既有 `Code` + `omitempty`）。B-2 的机理值得记：原注释押错了一跳——`exec.go` 钉住的是 agent→接收方那一跳，空 SID 出现在**下一跳**（N-1 broker 转发时旧 payload 没有 `Sid` 字段），而它的输入仍是 agent 选的 PID，于是旧 broker 成了 confused deputy。M-2 是时序：注释写的「tries again on its next boot」在 followers-first/leader-last 里**没有下一次 boot**，改为 leader-only reconcile pass + marker 前只 grandfather 已有 owner（这条差点做错——撤销刻意不删 session，grandfather 若活过 marker 会静默复活每一次撤销）。**变异验证 9/9 RED**，其中两条第一轮 GREEN 不是漏洞而是双层防御，两层同时退回才变红——教训：「变异没红」的第一反应该是怀疑变异有没有到达被测对象。**闸门这轮抓了我六次**（wire-freeze 要求声明 `forwardDeps` 不上 wire、新错误码不许静默退 70、结构预算抓到第 286 个方法、maintidx 抓到两个被推过线的函数、inbox 配对门抓到裸设前缀没配 marker、promised-guard 抓到我点名了已删测试），六次我都改代码而**没有放宽任何预算**。**deploy tier 这轮是主角**：drill 修复前 **0/43 GREEN**——全部死在同一个根因（全新 broker 的准入表是空的），而 `simcluster:281` 的 `\|\| true` 把产品拒绝洗成了「auth setup 失败」这个错误诊断；顺带查出 `cluster add`/`unlock`/`upgrade` 等六个集群操作全在同一道门后面而 `cluster-runbook.md` 只字未提，已补前置块。**drill 修好后剩 8 条偏离，定性花了三步**：①含修复那轮的偏离集是不含修复那轮的**严格子集**且逐条同断言 ⇒ 不是本轮修复引入的；②`expected-verdicts.tsv` 上次校准早于本增量，于是拿 **HEAD 建 git worktree**（工作树一行未动）、用 HEAD 的源码烘镜像、跑 HEAD 的 drill——那才是该表被校准时的状态；③6 条 HEAD 就红（登记表过期），**2 条是真回归**。只动 nats-server 版本这一个变量即可复现：**tether 代码造成零条部署层回归，两条全部来自内审时把 pin 对齐到 v2.14.5**。机理两层，第二层与版本无关且更重要：boot 路径对 JS 只探**一次**、落空即 exit 70，`Restart=always` 把**瞬时可自愈**状态变成死循环（实测 `NRestarts` 137、ready 行不增），而那条 fatal 自己的文案写着「TRANSIENT … self-heals」并同时建议运维「systemctl stop」——**产品能说出自己的瞬时态，却选择立刻退出，并把「关掉它」当成诊断**。用户裁定两件都做：pin 三处降回 v2.10.22（升级的原始理由「让 deny 装载」已随 `_TINBOX` 作废，剩下的只有「测的==发的」，两个方向都满足）+ boot 探测改**有界**轮询（有界而非无限：被 force-single 踢出去的节点永不恢复，必须仍能走到那条可操作 fatal），并把诊断挪到**等待开始时**打印而不是 90 秒后随 fatal 出现——那段文字的作用正是告诉运维等待是否可能有用，只在 fatal 时给出等于在它失去意义的那一刻才送达。两条 drill 复验 GREEN；副作用是 drill 50 的 K-#64c reality-tie 因诊断提前打印而**重新变回确定性**。教训：**过期的登记表长得跟回归一模一样**，区分二者只能拿基线 commit 建 worktree 重跑，不能读表 | [内审](prerelease-audit-review.md) · [外审 Fail + 逐条回复 + deploy-tier 续](prerelease-audit-external-review.md) · [tasklist](prerelease-audit-external-review-tasklist.md) |
| 2026-09-04 | 发布前全库审计 · 收尾（三轮外审 Fail→Pass，审查者获授权直接改码，主进程复核） | 用户授权外审者**直接改实现**到可放行，再由主进程按 §4 角色边界逐条复核——**能改实现的仍然只有主进程**。他的六条修复我采纳五条（leader-local 与 forwarded session create 收敛到同一个 committed-DB planner，补上了我上一轮「同一不变量两个入口只守住一个」的洞；`setup-go@v5→v6`，因为 v6 起才读 `toolchain` 指令而本仓 `go 1.26.0` 是**显式零补丁**——v5 装的恰好是 go1.26.0，正是本仓栽过 31 条可达 stdlib 漏洞的那个坑；`go mod tidy`；spawnsafe churn 测试的伪慢探测（512 容量通道被 feeder 预填，所谓「慢探测」从未慢过）；`proxy sub rm` 文案），**一条采纳机理但重写实现**。**我在他之上修了三处**：①他的 durable-terminal 查询改成按原始序列**反向走查**，无 consumer/无时钟两点对，但 history 流装的是 `audit.>`，call/proc/port 与 transfer 共用——反向走查会取回每条消息体，而它唯一的提前退出（本 transfer 的 start 行）缺失的场景**恰恰**是 publish 正在失败、ledger 行留存不掉的场景，于是每 5 分钟的 reap pass 同步重走整条 1 GiB 流、堵住 reconcile goroutine：**一个 consumer 泄漏换来一次 reconciler 停摆，且发生在最需要它的时刻**。改为服务端 `next_by_subj` 向前走查 + 30s 活性预算，并加**成本**守卫（数 `$JS.API.STREAM.MSG.GET` 次数，变异回旧形状 200 次 vs 阈值 8）。②他修了 `proxy sub rm` 的三处文案却只留下一条**单串黑名单**守卫、只扫其中一处——换成从 cobra 树推导动词的闸门，变异注回他够不到的站点（broker 产品回复串）新闸门变红、**他的黑名单同一变异下是绿的**。③**推翻我自己上一轮的因果**：我曾判定 drill 95 的 `T2c` 红是「90s boot wait 与 90s poll 正面相撞」并据此把预算改成 30s，还把这个故事写进了代码注释和一条比例守卫。取证（drill 运行中直读 brk1 journal+slog）证明：drill 95 杀的是 tether-broker 而非 nats-server，**JetStream 从未缺席、这条 wait 一次都没进过该路径**（SIGKILL→ready 实测 2.8s，全程 0 条 `still waiting`）；真实原因是我在同机与 drill 并发跑了整包 `go test` 并留着三个 11 小时的残留实例。预算改回 90s，比例守卫换成「等待期间必须持续出声」的行为守卫——**一条理由是假的守卫比没有守卫更坏：它靠常绿活着，还会把假话教给下一个读它的人**。教训入档：**没有 deploy-tier 证据就不要把因果写进代码注释**；**跑 drill 时别在同一台机器上并发跑 go test，它会把健康的产品报成失败**（本轮为此白跑了两轮 drill）。四硬闸全绿（e2e-parallel `ALL PASS` 3m51s）；首轮 p2/p13 两条负载敏感红已定性（同码空闲 30/30、15/15）并**如实登记不掩盖**，其中 p2 我尝试的「排序」修法被自己的测量推翻（contested probe 实测 17ms 而非注释假设的 200ms 预算），已还原并留作后续增量的输入 | [plan](prerelease-audit-plan.md) · [内审](prerelease-audit-review.md) · [外审 Fail×3 → Pass + 主进程复核 §12](prerelease-audit-external-review.md) · [tasklist](prerelease-audit-external-review-tasklist.md) |
