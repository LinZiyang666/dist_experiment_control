# assert-identity.baseline.tsv — the per-drill MULTISET of claims (drill<TAB>primitive<TAB>description as
# source text) that tests/assert-identity.sh --check demands EQUALITY against. Regenerate in the SAME change
# that intentionally moves a claim, and say why in that change's plan/review:
#     cd test/simcluster && sh tests/assert-identity.sh > tests/assert-identity.baseline.tsv
# 1610 rows at HEAD a3431a1 (2026-09-19, simcluster-speed M0) — per-drill counts verified equal to
# tests/kept-sites.baseline.tsv on the same tree, so the two gates start from one snapshot.
# 2026-09-19 M0-#33 (same change): 73-proxy-cluster-ha REHOME [#33] assert_ok (a both-outcomes pass) → not_covered
#   gap with the measurement and the agt_conn_on trace in its text; reason in expected-verdicts-log.md ## 73 and
#   docs/reviews/simcluster-speed-plan.md §8.1d. Count-neutral (kept-sites unchanged); identity changed on purpose.
# 2026-09-19 X14 (same change): 98-stuck-redial-recovery — 3 rows out (IMPACT-gates-recovery, RECOVERY-heartbeat,
#   RECOVERY-landed) / 2 rows in (RECOVERY conjunction at the injection watermark, SERVER-SIDE drop evidence);
#   reason in the drill header + docs/reviews/simcluster-speed-plan.md §8.1c. kept-sites 14->13 trade row records it.
# 2026-09-19 96 arm split (same change): 96-mid-flight-chaos — 2 rows out / 2 rows in. D0a reworded ("brk2 rejoins raft
#   after the #58 arm restarted it" → "a bare check races the last joiner's raft catch-up": the D arm now runs on its
#   own fresh N=3, no #58 arm precedes it); the F arm's cross-arm-residue not_covered ("96-F double fault … did not
#   fully recover from arm D") → the D arm's END-STATE not_covered ("post-partition full recovery … not observed
#   within 360s of the heal") — the same 360 s window, measured where the residue is produced instead of gating
#   another arm. Count-neutral (kept-sites 67 unchanged; _gap_drill_level is ONE site called by every arm).
#   Reason: docs/reviews/simcluster-speed-plan.md §5.5 "96 首拆" + §8.4.
# 2026-09-19 96.D R-CTX (same change): +1 row, 96-mid-flight-chaos assert_ok "D7 R-CTX: switch the ctl back to $SID …" —
#   the D arm's end-state measure (and the pre-split F precondition before it) read `node ls` under the ctl's
#   ACTIVE session, which D3/D4b's `session create canary2/canary3` had silently moved; two solo D runs answered
#   "(no nodes)" on a healthy 3-VOTER cluster. kept-sites 96: 67 -> 68 (upward; no trade). Reason: expected-verdicts-log.md ## 96.
# 2026-09-19 74 arm split (same change): 74-rebalance-on-return — 3 rows reworded / 3 rows added. Reworded: the two
#   "74 B-negctrl + C-negctrl THIS RUN" gaps → "74 B-negctrl THIS RUN" (C no longer inherits B's negative control) and
#   "74 destructive arms (SKEW/RETURN/A/B/C) THIS RUN" → "(SKEW/RETURN/A/B)" (C is its own unit). Added: C-negctrl-fixture +
#   C-negctrl-fixture-pre (C builds the `reg` pre-control it used to inherit) and the matching "74 C-negctrl THIS RUN" gap.
#   kept-sites 59 -> 62 (upward). Reason: docs/reviews/simcluster-speed-plan.md §5.6 / §8.4; expected-verdicts-log.md ## 74.
# 2026-09-19 internal review round 1 R3-1 (same change): the scanner now also reads drills/lib/*.sh, keyed `lib/<name>` —
#   +8 rows for lib/setup-forcesingle (claims that setup_forcesingle_n2 executes inside five drills and that neither this
#   gate nor kept-sites could see). No existing row changed.
# 2026-09-19 internal review round 1 R1-F1 (same change): 67-transient-js-refusal +2 product_red rows (#84 injection face and
#   #84 post-recovery face). The 09-19 revision had booked the Put-leg stall as a not_covered gap; plan X27 rules it a
#   product_red under a new number. kept-sites 67: 25 -> 27. Reason: expected-verdicts-log.md ## 67, gotcha #84.
# 2026-09-19 internal review round 1 R1-F3 (same change): 98-stuck-redial-recovery +1 row, "inject self-proof C: at the watermark
#   instant $CUT_BROKER's /connz STILL holds agt1's client connection" — closes the vacuity window between the /connz discovery
#   and the DROP (a connection that had already moved would satisfy the recovery conjunction at once). kept-sites 98: 13 -> 14.
# 2026-09-19 internal review round 1 R3-6 (same change): 32-install-lifecycle +8 rows — the H-7' client-liveness retrofit arm
#   (plan X12): a pre-liveness nats.conf is kept byte-identical on re-run, its .new carries the two ping keys read from
#   install.sh's single definition, the KEPT report names them, and a conf with OTHER values is reported as a value
#   difference (R3-5). kept-sites 32: 54 -> 62.
# 2026-09-19 #84 image #8 receipt (same change): 67-transient-js-refusal — the post-recovery product_red REWORDED (1 out /
#   1 in): the judgement is now on duration (>=100 s, next attempt succeeds) whatever the refusal says, and the text
#   quotes the first attempt's face; the image-#8 stall came back worded transient and the text-only predicate let it
#   pass. Count-neutral (kept-sites 67 unchanged). Reason: gotcha #84, plan §8.5c.
# 2026-09-19 Block 2b 74 solos (same change): 74-rebalance-on-return C-still-skewed REWORDED (1 out / 1 in): the edge is
#   also valid when the auto path already fired INSIDE the return window (proxy_auto_rebalanced advanced past its
#   baseline). The "still at 0" form raced the feature under test — brk2 rejoined in 7 s, the auto move landed before
#   the line ran, and the drill invalidated its own edge (74C1, plan §8.5b). Count-neutral. Reason: plan §8.5b.
# 2026-09-19 S2 triage of the 2026-09-03 "stale table" reds (same change): 52-credential-rotation A8d/A8e and
#   81-admin-evict-session-rm B3 REWORDED (3 out / 3 in; 81's B3 also assert_refuses → assert_ok with a two-part
#   predicate). All three read a startup refusal from the wrong stream: since h1 the broker's exit error goes to
#   journald and the agent's to agent.boot.err (fd 2 dup2), while the oracles read the slog / the captured stdout —
#   the product text was unchanged. Count-neutral (kept-sites unchanged). Reason: plan §8.5b′.
# 2026-09-19 #33 evidence flip (same change): 73-proxy-cluster-ha REHOME `[#33]` not_covered → `[#33 FIXED by #80]` assert_ok
#   (1 out / 1 in, count-neutral): 7/7 samples delivered the mechanism receipt (connection on the killed broker, re-registered
#   on a survivor, AUTO-RECOVERED 16–29 s). 94-agent-reconcile B5 REWORDED (DOC-25 closed: the reconnect register now
#   prints its counts). Reason: gotcha #33 / #87, plan §8.5b′, expected-verdicts-log.md ## 73.
# 2026-09-20 round-2 review dispositions (same change), rows now keyed `<drill>.<arm>` / `<drill>._shared` for the two
#   arm-split drills (74, 96) — plan X7 / §5.5 arm rows, delivered after R1-F2 / R3-F3 showed a claim moved between arms
#   was invisible to this file and to kept-sites; the pre-existing `74-…` / `96-…` rows are the same claims under their
#   arm keys. Claim changes in the same regeneration:
#   67: +1 product_red (injected face judged on DURATION ≥150 s, R1-F3) · +1 product_red (slow-success first
#       CONTROL(after) attempt, R1-F4) · +1 not_covered (a 1-attempt `refused` refusal is not the bounded-retry tooth,
#       R1-F3) · CONTROL(after) recovered line reworded (attempt-1 seconds). 
#   73: `[#33 FIXED by #80]` single conjunctive assert → `[#33 FIXED]` hard claim (AUTO-RECOVERED ∧ ≤90 s) + a gated
#       mechanism assert `[#33 mechanism: pool reconnect]` (only when conn==home at kill time) + a not_covered gap for
#       the tunnel-only path (R6-4 / R3-F13 / R1-F5 / R6-1).
#   74: `_gap_drill_level` text reworded to face 1 only (R6-3: the rc=64 face is #86, auto-rebalance DOES fire).
#   96: D0f PREMISE (#89) assert_ok + a runtime-guard not_covered when the premise is red at D6b; A2 post-restart
#       no-orphan record reclassified gap → runtime-guard (same class as the pre-restart record, R1-F6).
#   98: +1 assert_ok (self-proof C′ re-asserted at the watermark instant, R1-F7).
#   Reasons: docs/reviews/simcluster-speed-review-round2.md.
#   73 (same change, after the round-2 -j5 re-run): Q-xcheck reworded — the vended endpoint is polled into agreement for
#       60 s before the assert (3/14 one-shot mismatches were a leg still serving through the previous home).
00-skeleton	assert_ok	up 1 broker + 1 agent + 1 ctl
00-skeleton	assert_ok	init brk1 (standalone → N=1 cluster)
00-skeleton	assert_ok	N=1 healthy floor (leader + DEGRADED)
00-skeleton	assert_ok	session $SID + ctl login
00-skeleton	assert_ok	agent-join agt1
00-skeleton	assert_ok	node ls lists agt1 ONLINE
00-skeleton	assert_ok	seed tier-A (1 KiB) + tier-B (20 MiB) files on ctl
00-skeleton	assert_ok	tier-A push ctl→agt1
00-skeleton	assert_ok	tier-A pull agt1→ctl
00-skeleton	assert_ok	tier-A round-trip sha256 matches
00-skeleton	assert_ok	tier-B push ctl→agt1 (JS Object Store)
00-skeleton	assert_ok	tier-B pull agt1→ctl
00-skeleton	assert_ok	tier-B round-trip sha256 matches
10-grow-to-3	assert_ok	up 3 brokers + 1 agent + 1 ctl
10-grow-to-3	assert_ok	init brk1 (N=1)
10-grow-to-3	assert_ok	grow brk2 (N=2)
10-grow-to-3	assert_ok	grow brk3 (N=3)
10-grow-to-3	assert_ok	every voter's tether-broker active after grow (#23 guard)
10-grow-to-3	assert_ok	3 VOTERs
10-grow-to-3	assert_ok	all 3 reachable
10-grow-to-3	assert_ok	streams at R=3 (stream_actual==stream_target==3)
10-grow-to-3	assert_ok	clustered JS meta present (≥3-node)
10-grow-to-3	assert_ok	joiner brk3 roster NOT hollow (row count == leader's)
10-grow-to-3	assert_ok	no FK-panic / hollow-voter in joiner brk3 tether-broker journal
10-grow-to-3	assert_ok	transfer leadership $CUR→$TGT (--wait, run on the current leader)
10-grow-to-3	assert_ok	status --json is the AUTHORITATIVE leader view after the move (is_leader_view=true, leader=$TGT)
10-grow-to-3	assert_ok	session + ctl login
10-grow-to-3	assert_ok	agent-join agt1
10-grow-to-3	assert_ok	docker-kill follower $F (raft node drops → quorum 2/3)
10-grow-to-3	assert_ok	survivors still elect a leader at quorum 2/3
10-grow-to-3	assert_ok	control plane alive: node ls still lists agt1
10-grow-to-3	assert_ok	control-plane WRITE commits at quorum 2/3 (session create via raft)
11-grow-gaps	assert_ok	up 3 brokers
11-grow-gaps	assert_ok	init brk1 (N=1)
11-grow-gaps	assert_ok	grow brk2 reaches VOTER via \`tether cluster add\`
11-grow-gaps	assert_ok	grow ran through tether cluster add (trailer names it, not workarounds)
11-grow-gaps	assert_ok	#4 move-not-delete: former-N1 brk1 JS store moved to jetstream.grow-bak.<epoch> (never deleted)
11-grow-gaps	assert_ok	#8 GONE: no pre-mesh 'still in flight'/'is BLOCKED' give-up (non-blocking approve)
11-grow-gaps	assert_ok	#3 GONE: no 'no cluster block to harvest' / 'reconcile … not converged' (secrets-dir fallback)
11-grow-gaps	assert_ok	no GREW-VIA-WORKAROUNDS trailer (the manual grow is retired)
11-grow-gaps	assert_ok	gate A: cmd_grow does NOT hand-run cluster init/join/reconcile-nats/JS-reset (cluster add owns them)
11-grow-gaps	assert_ok	gate A: cmd_grow DOES invoke tether cluster add
11-grow-gaps	assert_ok	brk3 is a fresh broker with no raft state (never grown)
11-grow-gaps	assert_ok	add the cluster seam to fresh brk3 (NO init)
11-grow-gaps	assert_refuses	#I1 INVARIANT: cluster-mode serve refuses a fresh joiner with no raft state (fail-closed; cluster add does the init)
12-ghost-voter	assert_ok	docker-kill brk2 (provably dead)
12-ghost-voter	assert_ok	stop brk1 broker daemon (an operator stop — #23 Restart=always does NOT revive it)
12-ghost-voter	assert_ok	offline force-single brk1 (auto-prunes the abandoned brk2)
12-ghost-voter	assert_ok	restart nats-server + broker (provisioning)
12-ghost-voter	assert_ok	#12 FIXED: brk2 auto-pruned from the roster (no ghost VOTER — the RED deadlock premise is gone)
12-ghost-voter	assert_ok	#12 FIXED: recovery node remove of the pruned brk2 reports 'no such roster node' (not a deadlock refusal)
12-ghost-voter	assert_ok	batch-C: the OFFLINE force-single path creates NO force_single_finalize op (the retry op is ONLINE-only, and only on a failed prune)
13-inbroker-reconcile-perm	assert_ok	up 3 brokers
13-inbroker-reconcile-perm	assert_ok	init brk1 (N=1)
13-inbroker-reconcile-perm	assert_ok	grow brk2 (N=2, clustered)
13-inbroker-reconcile-perm	assert_ok	grow brk3 reached VOTER (N=3)
13-inbroker-reconcile-perm	assert_ok	/etc/tether DIRECTORY is root-owned (Option B: Caddyfile stays root-only)
13-inbroker-reconcile-perm	assert_ok	/etc/tether/nats.d is tether-owned (the #22 fix — reconciler-writable subdir)
13-inbroker-reconcile-perm	assert_ok	every voter broker.yaml pins nats_conf_path at nats.d/ (no old-path seam straggler)
13-inbroker-reconcile-perm	assert_ok	#22 fixed: grow's reconcile nats --all shows NO natsconf permission-denied reason
13-inbroker-reconcile-perm	assert_ok	grow trailer does NOT carry #22 (auto path converged post-fix)
13-inbroker-reconcile-perm	assert_ok	derived well-formed reconcile args (account pub A…, broker nkey U…, peer triple)
13-inbroker-reconcile-perm	assert_ok	#22 fixed: a User=tether nats.conf write into the tether-owned nats.d/ SUCCEEDS
20-forcesingle-natsconf	assert_ok	docker-kill brk2 (provably dead)
20-forcesingle-natsconf	assert_ok	stop brk1 broker daemon (an operator stop — #23 Restart=always does NOT revive it)
20-forcesingle-natsconf	assert_ok	offline force-single brk1 auto-de-clusters + prunes (prints 'de-clustered to standalone')
20-forcesingle-natsconf	assert_ok	#20 FIXED: nats.conf auto-de-clustered to STANDALONE (no cluster{} block)
20-forcesingle-natsconf	assert_ok	#20 FIXED: force-single --reset-js MOVED the clustered JS store aside ITSELF (jetstream.force-single-bak.* present — the product verb, not the sim's old mv)
20-forcesingle-natsconf	assert_ok	restart nats-server + broker (operator provisioning; the JS reset was the PRODUCT's job above)
20-forcesingle-natsconf	assert_ok	#20 FIXED: tier-B push WORKS at N=1 after auto-de-cluster (standalone JS, no 503)
20-forcesingle-natsconf	assert_ok	#12 FIXED: abandoned brk2 pruned from the roster (no ghost VOTER)
20-forcesingle-natsconf	assert_ok	T1/B1 setup: stop the broker before fault-injecting its post-rewrite durable state
20-forcesingle-natsconf	assert_ok	T1/B1 setup: delete marker+epoch to reproduce the exact crash window after raft rewrite
20-forcesingle-natsconf	assert_ok	T1/B1 setup: plant the durable intent an INTERRUPTED online force-single would have left
20-forcesingle-natsconf	assert_ok	T1/B1 non-vacuity: intent EXISTS and marker+epoch are both ABSENT before restart
20-forcesingle-natsconf	assert_ok	T1/B1 setup: start brk1 so the intent is met on a fresh leadership acquisition
20-forcesingle-natsconf	assert_ok	T1/B1 precondition: brk1 comes back up and has a leader (this drill's de-clustered N=1 is bootable, unlike drill 22's clustered survivor)
20-forcesingle-natsconf	assert_ok	T1/B1: an INTERRUPTED recovery forward-completes from its on-disk intent — the trigger is the INTENT, not the force_single_active marker it may never have written
20-forcesingle-natsconf	assert_ok	T1/B1: force_single_active is reported afterwards, so cluster status shows the emergency and the ctl destructive gate stays CLOSED
20-forcesingle-natsconf	assert_ok	T1/B1: the exact epoch from the pre-rewrite intent was restored (no fresh token minted)
20-forcesingle-natsconf	product_red	T1/B1 NOT recovered: brk1 restarted cleanly and has a leader, yet the planted force-single intent was still on disk 90s later — an interrupted recovery does not forward-complete, so the post-rewrite window remains unrecoverable (external review B1)
21-smalldisk-tierb	assert_ok	up 1 broker (JS filesystem capped to 4g tmpfs) + agent + ctl
21-smalldisk-tierb	assert_ok	init brk1 (N=1)
21-smalldisk-tierb	assert_ok	session + ctl login
21-smalldisk-tierb	assert_ok	agent-join agt1
21-smalldisk-tierb	assert_ok	no max_file_store in nats.conf (cap is tmpfs, not the forbidden subkey)
21-smalldisk-tierb	assert_ok	control plane alive: node ls lists agt1
21-smalldisk-tierb	assert_ok	tier-B push SUCCEEDS on the small disk (disk-aware OBJ_xfer; was #21 RED: 8 GiB hardcode → 10047)
21-smalldisk-tierb	assert_ok	tier-B file present on agt1 (round-trip completed, not a queued no-op)
22-forcesingle-online	assert_ok	survivor brk1 is the raft leader (control-source ops require it)
22-forcesingle-online	assert_ok	DRY (healthy) reports would-NOT-proceed (leader contact) + no mutation
22-forcesingle-online	assert_ok	DRY (healthy) is zero-mutation: MainPID unchanged, no force_single_active
22-forcesingle-online	assert_refuses	GATE-a healthy REAL arm refuses (quorum_not_lost / leader contact)
22-forcesingle-online	assert_refuses	YES-off: offline --yes rejected (Tier-2, NO --yes override)
22-forcesingle-online	assert_refuses	YES-online (#36 FLIPPED, R14): online --yes is Tier-2-rejected IDENTICALLY to offline ('NO --yes override' / 'cannot run unattended'), not the old online-gate divergence
22-forcesingle-online	assert_ok	Arm-0③ brk2 raft :7400 refused (peer provably dead)
22-forcesingle-online	assert_refuses	PROT set-raft-addr refused during quorum loss (no leader / election in progress)
22-forcesingle-online	not_covered	Arm-0 uninterrupted-dwell production-fidelity (TAMED: the survivor restarted but the dwell still satisfied — a container-timing artifact)
22-forcesingle-online	assert_ok	GATE-d real arm mints a non-empty token after the dwell
22-forcesingle-online	assert_ok	GATE-d wrong token is refused with exact arm_expired code
22-forcesingle-online	assert_ok	GATE-d captured token is stale after a failed consume attempt (single-shot clear)
22-forcesingle-online	assert_ok	GATE-d second real arm mints a fresh token
22-forcesingle-online	assert_ok	GATE-d token older than the 60s TTL is refused with exact arm_expired code
22-forcesingle-online	assert_ok	POS commit: online force-single (pty-fed typed confirm) succeeds in-process
22-forcesingle-online	assert_ok	POS PRIMARY oracle: the PREVIOUSLY-REFUSED set-raft-addr now SUCCEEDS (raft writable at quorum-of-1)
22-forcesingle-online	assert_ok	POS in-process discriminator: brk1 MainPID UNCHANGED across the commit (== $PID0, no restart)
22-forcesingle-online	assert_ok	POS force_single_active banner: cluster status exits 3 / reports force-single
22-forcesingle-online	assert_ok	POS R3: nats.conf STAYS clustered (online never de-clusters; offline-only #20)
22-forcesingle-online	assert_ok	batch-C: the abandoned peer's roster row is GONE right after commit (the prune is synchronous — the runbook's next step depends on it)
22-forcesingle-online	assert_ok	batch-C: a CLEAN recovery creates NO force_single_finalize op (it is the failure-branch retry, not the happy path)
22-forcesingle-online	assert_ok	batch-C/B1: a CLEAN recovery leaves NO force-single intent file behind
22-forcesingle-online	assert_bug	#35 online force-single dwell structurally unreachable (brk1 restarted, dwell never satisfied)
22-forcesingle-online	assert_setup	#35 fixture reset
22-forcesingle-online	assert_setup	#35 fixture provision fresh N=2 containers
22-forcesingle-online	assert_setup	#35 fixture initialize brk1
22-forcesingle-online	assert_setup	#35 fixture grow brk2
22-forcesingle-online	assert_setup	#35 fixture pin brk1 leader (already-leader is success)
22-forcesingle-online	assert_ok	#35 fixture: brk2 is provably dead before survivor restart
22-forcesingle-online	assert_ok	#35 trigger: manual systemctl restart succeeds
22-forcesingle-online	assert_ok	#35 fixture validity: manual restart changes MainPID ($PIDb→${PIDm:-missing})
22-forcesingle-online	product_red	#35 reproduced: ≥2 automatic restarts in a 36s window (NRestarts=$NRa), ≥2 distinct crash PIDs carry the exact startup JetStream-unavailable/EJECTED diagnostic, and EVERY 3s DRY sample stayed non-proceeding
30-rolling-upgrade	assert_ok	grow_to_3 (N=3 HA cluster, SINGLE no-retry attempt — 30 owns #31; GROW-ATTEMPTS logged above must be 1/retry=0)
30-rolling-upgrade	assert_ok	session lab + ctl login (owner)
30-rolling-upgrade	assert_ok	broker-local ctl login on leader $LDR (cluster upgrade + cluster unlock are ctl-over-NATS ops, unlike the admin-socket rebalance)
30-rolling-upgrade	product_red	#31 (owner: R7a release-failure shape / R7b lease+TTL) — 'cluster add' reported SUCCESS and every node reached VOTER, yet the cluster_grow_active marker is STILL held afterwards. While it is held, join/retire/upgrade are all refused, so a grow that says it worked leaves membership fenced. Probed directly via cluster unlock --dry-run (zero mutation)
30-rolling-upgrade	assert_ok	#31-remedy tether's OWN out clears it: cluster unlock --grow --force (rc=0 ONLY when a post-clear re-probe confirms the marker is gone — cluster_unlock.go:236,279-305)
30-rolling-upgrade	assert_ok	#31-remedy-effect an INDEPENDENT zero-mutation re-probe now reports the grow lock NOT held (the clear is real, not just a printed claim)
30-rolling-upgrade	assert_ok	[R7a] NO grow lock leaked after 'cluster add' — cluster_grow_active was released AND the release confirmed (a KEPT invariant since R7a; before it, a silent best-effort release routinely left the marker set and fenced membership)
30-rolling-upgrade	assert_setup	[env: OQ-6 SUPPLY] declare $DECL_NID as $FIRSTHOP's co-located agent in broker.yaml + restart the broker (provisioning: the sim builds the HOST shape; every upgrade action below is tether's)
30-rolling-upgrade	assert_setup	[env: OQ-6 SUPPLY] start the DECLARED co-located agent $DECL_NID on broker host $FIRSTHOP (out of the root-owned /usr/local/bin/tether — the re-exec-only path a real co-located agent uses)
30-rolling-upgrade	assert_setup	[env: OQ-6 SUPPLY] start an UNDECLARED co-located agent nid=$OBS_NID on broker host $OTHERHOP (node_id==nid convention ⇒ presence must be OBSERVED from the session node list)
30-rolling-upgrade	assert_ok	P3-(a)-nid DECLARED: $FIRSTHOP's row names agent_nid=$DECL_NID — the broker.yaml declaration was consumed; this nid differs from the node_id, so it CANNOT have come from the fallback convention
30-rolling-upgrade	assert_ok	P3-(a)-assumed $FIRSTHOP is NOT an assumed pairing (assumed=false ⇒ self-declared, node_versions.go:56-60)
30-rolling-upgrade	assert_ok	P3-(b) OBSERVED: $OTHERHOP correlates to nid=$OBS_NID via the node_id==nid convention (assumed=true — the operator is never shown a silently-wrong pairing)
30-rolling-upgrade	assert_ok	P3-(c) ABSENT: the leader $LDR reports agent_ver='?' — no agent has EVER registered under its node_id, which is the only thing that makes a host state (c) rather than a merely-down agent
30-rolling-upgrade	assert_ok	P3-(a) $FIRSTHOP whole-host BASELINE is $VCUR on both legs — the post-roll assertion therefore measures a real advance
30-rolling-upgrade	assert_ok	P3-(b) $OTHERHOP whole-host BASELINE is $VCUR on both legs
30-rolling-upgrade	assert_ok	[env: PRIVILEGED STAGING — operator stages the new binary; cluster upgrade does NOT] install tether-next → /usr/local/bin/tether on each broker
30-rolling-upgrade	assert_ok	staged on-disk sha256 matches on every broker (--expect-sha256 anchor; the co-located agents' re-exec target is this SAME staged file)
30-rolling-upgrade	assert_refuses	no --account-seed → refused (each broker verifies an account-signed trigger)
30-rolling-upgrade	assert_refuses	no --backup-taken → refused (first-boot migration is one-way; restore is the only recovery)
30-rolling-upgrade	assert_ok	cluster upgrade --dry-run plans EVERY broker host (exactly 3 UPGRADE steps — a count, not a keyword grep that any prose could satisfy)
30-rolling-upgrade	assert_ok	cluster upgrade --dry-run roll ORDER: the leader's UPGRADE is the LAST upgrade line (followers-first/leader-last; Stage-C cluster-1)
30-rolling-upgrade	assert_ok	PLAN-(c) the agentless leader $LDR's step is marked '[broker-only — no co-located agent on this host]' — P3's state (c) rendered where the operator can see it BEFORE the roll (cluster_upgrade.go:renderUpgradePlan)
30-rolling-upgrade	assert_ok	PLAN-(a) the DECLARED-agent host $FIRSTHOP is NOT broker-only — a whole-host step (broker + agent legs)
30-rolling-upgrade	assert_ok	PLAN-(b) the OBSERVED-agent host $OTHERHOP is NOT broker-only either — presence inferred from the node list still yields a whole-host step
30-rolling-upgrade	assert_ok	dry-run left every broker's RUNNING version on v-cur (no host re-exec'd; on-disk was already staged to NEXTVER so this checks the SELF-REPORTED running release, not the binary on disk)
30-rolling-upgrade	assert_setup	[env] stop the DECLARED co-located agent $DECL_NID on the FIRST hop $FIRSTHOP (constructing P3 state (b): declared, therefore PRESENT, but not answering)
30-rolling-upgrade	assert_ok	start background raft write-probe (phase-1 window) in ctl1
30-rolling-upgrade	assert_ok	PROBE-ISOLATION the ctl's ACTIVE session is still '$SID' after the probe started writing — the probe creates sessions, and 'session create' rewrites the active-session pointer of whatever HOME it runs in. If it shared the ctl's HOME, every session-scoped oracle below (the whole AGENT half of node ls --brokers) would silently answer for an empty throwaway session and report agent_ver='?'
30-rolling-upgrade	product_red	#31 acquire-lock window re-fenced the roll AFTER the lock was probed clear (owner: R7b lease+TTL) — the real cluster upgrade HALTED at acquire-upgrade-lock with 'a cluster membership operation (join/retire) is in flight', although cluster unlock reported the grow lock cleared moments earlier
30-rolling-upgrade	not_covered	30 PHASE-1 P3 (b)-state HALT + the whole-host roll mechanism THIS RUN
30-rolling-upgrade	assert_ok	PHASE-1 [P3 (b)] the roll must HALT on a DECLARED-but-DOWN co-located agent, NEVER report success — it returned rc=0, i.e. it silently treated $FIRSTHOP as broker-only and called a half-upgraded host done. That is the P3 regression
30-rolling-upgrade	assert_ok	PHASE-1 [P3 (b)] the roll HALTED at the first hop $FIRSTHOP naming the AGENT leg ('HALTED at $FIRSTHOP' + 'agent re-exec refused'/'agent_no_responders') — a declared agent stays PRESENT while it is down, so the roll stops loudly instead of quietly skipping half the host
30-rolling-upgrade	assert_ok	PHASE-1 the roll failed (rc=${_ROLL_RC:-?}) for a reason that is NEITHER the expected P3 agent-leg HALT at $FIRSTHOP NOR the #31 acquire-lock fence — an undocumented failure, surfaced rather than laundered into one of the known shapes (see the verbatim roll.log above)
30-rolling-upgrade	assert_ok	PHASE-1 per-hop [1/3] the halted hop $FIRSTHOP's BROKER daemon did advance to $NEXTVER (its reload succeeded; the HALT came afterwards, on the agent leg)
30-rolling-upgrade	assert_ok	PHASE-1 per-hop [2/3] $OTHERHOP is UNTOUCHED (still $VCUR) — the roll advances ONE host at a time; a roll that fanned out would have moved it too
30-rolling-upgrade	assert_ok	PHASE-1 per-hop [3/3] the leader $LDR is UNTOUCHED (still $VCUR) — leader-last held even on the failure path
30-rolling-upgrade	assert_ok	PHASE-1 SKEW $FIRSTHOP now reports skew=true — broker at $NEXTVER, its co-located agent still at $VCUR. This is #19's whole-host verdict doing its job: the host is NOT done, and the operator is told so by name
30-rolling-upgrade	assert_ok	PHASE-1 SKEW $FIRSTHOP is NOT whole-host-at-target (the half-upgraded host must not read as done on either version)
30-rolling-upgrade	assert_ok	PHASE-1 CONTINUITY the raft write-probe saw NO not_leader / 503 / no-responders ACROSS the failed roll — a HALT must leave a SAFE partial state, i.e. writable, not a wedged cluster
30-rolling-upgrade	assert_ok	UNLOCK-precond the HALTed roll LEFT the roll lock HELD (cluster_upgrade_drive.go deliberately does not release on HALT, so membership stays fenced while the cluster sits half-rolled)
30-rolling-upgrade	assert_ok	UNLOCK-halt-admits the DEFAULT clear (no --force) SUCCEEDS against a HALTed roll's lock, and CONFIRMS it: the orchestrator expired its own lease on the way out, so nothing is renewing it and 'fix and re-run to resume' is true NOW instead of after LockLeaseTTL. rc=0 still requires the post-clear re-probe (cluster_unlock.go:236,279-305 — 'probably cleared' is explicitly not good enough)
30-rolling-upgrade	assert_ok	UNLOCK-effect an INDEPENDENT zero-mutation re-probe now reports the roll lock NOT held
30-rolling-upgrade	not_covered	30 UNLOCK the default clear's REFUSAL of a LIVE lease, and --force's override of one
30-rolling-upgrade	assert_setup	[env] restart the DECLARED co-located agent $DECL_NID on $FIRSTHOP (repairing state (b); the roll is resumed by re-running it, which is the documented recovery)
30-rolling-upgrade	assert_ok	PHASE-2 the repaired agent is back ONLINE before resuming
30-rolling-upgrade	assert_ok	PHASE-2 the repaired agent came back running the STAGED binary ($NEXTVER), so $FIRSTHOP is whole-host at target BEFORE the resumed roll — the operator's restart did that, not cluster upgrade
30-rolling-upgrade	assert_ok	start background raft write-probe (phase-2 window) in ctl1
30-rolling-upgrade	assert_ok	PHASE-2 the resumed roll COMPLETED (rc=0). Under R8 that is more than 'every host reported the target': driveUpgrade also gates rc=0 on the leader's agent-confirmed home verdict and on never having lost the roll lock mid-roll
30-rolling-upgrade	assert_ok	PHASE-2 [UNLOCK TERMINAL PROOF] the resumed roll got PAST acquire-lock — no 'membership operation in flight' anywhere in its log. This, not the unlock command's own success message, is the proof membership was really unfenced
30-rolling-upgrade	assert_ok	PHASE-2 [whole-host] every broker's RUNNING (self-reported) version == $NEXTVER after the roll
30-rolling-upgrade	assert_ok	PHASE-2 [MECHANISM — the roll's OWN agent leg] cluster upgrade re-exec'd $OTHERHOP's co-located agent and declared that host '(broker+agent) at $NEXTVER' in its own output. Without this the drill would only be reading END STATES, and an end state can be reached without the roll: a restarted daemon picks up the staged binary by itself. THIS is the whole-host mechanism actually executing
30-rolling-upgrade	assert_ok	PHASE-2 [IDEMPOTENT RESUME] the resumed roll SKIPPED $FIRSTHOP as 'already at target' — a re-run does not re-restart daemons that are already current (cluster_upgrade_drive.go skips the agent leg only when it is ALREADY at target), which is what makes 'fix and re-run to resume' safe advice
30-rolling-upgrade	assert_ok	PHASE-2 [WHOLE-HOST (a)] $FIRSTHOP is done by #19's ONLY criterion: broker AND its DECLARED co-located agent $DECL_NID are both at $NEXTVER (OQ-6 — never once asserted before this batch, because the sim ran no co-located agent)
30-rolling-upgrade	assert_ok	PHASE-2 [WHOLE-HOST (b)] $OTHERHOP is done on both legs too, with its agent discovered by OBSERVATION (nid==node_id) rather than declaration
30-rolling-upgrade	assert_ok	PHASE-2 [WHOLE-HOST] $FIRSTHOP's skew is CLEARED (skew=false) — the half-upgraded state phase 1 pinned is gone, on the same oracle that reported it
30-rolling-upgrade	assert_ok	PHASE-2 [WHOLE-HOST] $OTHERHOP reports no skew either
30-rolling-upgrade	assert_ok	PHASE-2 [(c) BROKER-ONLY] the agentless leader $LDR reached $NEXTVER on its broker leg and STILL reports agent_ver='?' — a broker-only host is done with no agent leg at all, and the roll never invented one
30-rolling-upgrade	assert_ok	PHASE-2 [re-exec] tether-broker MainPID UNCHANGED across BOTH rolls (syscall.Exec in place, not a kill/restart) — brk1=$MP1 brk2=$MP2 brk3=$MP3
30-rolling-upgrade	assert_ok	PHASE-2 CONTINUITY the write-probe saw NO not_leader / 503 / no-responders across the completing roll (M1, independent phase-2 window — it cannot pass on phase-1's samples)
30-rolling-upgrade	not_covered	30 the G5 roll MECHANISM (per-hop advance, whole-host dual-version, skew, cluster unlock recovery, PID-preserving re-exec, write continuity) THIS RUN
30-rolling-upgrade	not_covered	30 (with stated reasons — Stage-C mandate-2/cluster-2): (b) Arm 30-C N=2 write-fence NEGATIVE CONTROL
30-rolling-upgrade	not_covered	30 (with stated reasons — Stage-C mandate-2/cluster-2): (c) mid-run HALT/resume of the LEADER hop
31-node-upgrade-fleet	assert_ok	up 1 broker + 2 agents + 1 ctl
31-node-upgrade-fleet	assert_ok	init brk1 (N=1 cluster)
31-node-upgrade-fleet	assert_ok	SETUP broker.upgrade.url_allow += artifact prefix (labeled operator config) + broker restart
31-node-upgrade-fleet	assert_ok	session lab + ctl login (owner)
31-node-upgrade-fleet	assert_ok	agent-join agt1
31-node-upgrade-fleet	assert_ok	agent-join agt2
31-node-upgrade-fleet	assert_ok	SETUP artifact serving tether-next (ART_URL/ART_SHA populated)
31-node-upgrade-fleet	assert_ok	SETUP inject instance CA into agt1 trust store (so the fetch is trusted — isolates #28)
31-node-upgrade-fleet	assert_ok	SETUP agt1 agent.upgrade.url_allow += artifact prefix (labeled operator config) + agent restart + ONLINE
31-node-upgrade-fleet	assert_ok	P28-i  precondition: agt1 can FETCH+TRUST the artifact URL (so the flip reaches the sha gate, not download_failed)
31-node-upgrade-fleet	assert_ok	P28-ii precondition: broker WHITELISTED the URL (so the request forwards, not broker url_not_allowed)
31-node-upgrade-fleet	assert_ok	P28-iii precondition: agt1 ONLINE + owner=caller (rules out node_offline/not_owner)
31-node-upgrade-fleet	assert_refuses	#28 FLIPPED (R15): the CONFIGURED artifact URL PASSES the agent local allow-list — it now fails on the WRONG sha (sha256_mismatch), NOT url_not_allowed_local (was: unconfigurable → always url_not_allowed_local)
31-node-upgrade-fleet	assert_refuses	#28 non-vacuity: the agent allow-list is LIVE, not disabled — a broker-whitelisted-but-agent-denied URL (VALID sha) is still refused url_not_allowed_local AT THE AGENT gate (broker-side url_not_allowed would MISS this sig → FAIL LOUD)
31-node-upgrade-fleet	assert_refuses	N1 broker url_not_allowed (off-allowlist URL + VALID sha → not the sha gate)
31-node-upgrade-fleet	assert_refuses	N2 broker sha256_invalid (malformed sha, not 64-lowercase-hex)
31-node-upgrade-fleet	assert_ok	N3 not_owner (a joined member, not the session owner, is refused node upgrade)
31-node-upgrade-fleet	assert_ok	F1 --all target set (node ls --json ONLINE filter) includes agt1 (ONLINE)
31-node-upgrade-fleet	assert_ok	F1b --all target set includes agt2 (ONLINE) — a real 2-node fleet
31-node-upgrade-fleet	assert_ok	F2-setup stop agt2 daemon → make it OFFLINE/STALE
31-node-upgrade-fleet	assert_ok	F2 agt2 goes OFFLINE/STALE in the broker view
31-node-upgrade-fleet	assert_ok	F2b OFFLINE agt2 is EXCLUDED from the --all target set (listOnlineNIDs ONLINE-only)
31-node-upgrade-fleet	assert_ok	F2c ONLINE agt1 stays in the --all target set
31-node-upgrade-fleet	assert_ok	F3 --all dispatched to ONLINE agt1 → aborted on the agent's url_not_allowed_local gate (isConfigError aborts the fleet; reached an agent, not a no-op)
31-node-upgrade-fleet	assert_ok	F3b --all did NOT dispatch to the OFFLINE agt2 (enumerated out — the abort names agt1, never agt2)
31-node-upgrade-fleet	assert_ok	F4-setup restart agt2 daemon
31-node-upgrade-fleet	assert_ok	F4-setup2 agt2 back ONLINE (2-node fleet again)
31-node-upgrade-fleet	assert_ok	F4a --all --timeout 0 announces the canary hold-back ('canary: agt1' with the rest held) — proves 2-node enumeration reached the canary split
31-node-upgrade-fleet	assert_ok	F4b canary transient failure ABORTS the fleet non-zero: rc!=0 AND 'canary agt1 failed, 1 node(s) untouched' (a canary that cannot even dispatch must hold the fleet — upgrade-safety plan §5)
31-node-upgrade-fleet	assert_ok	F4c agt2 was NEVER dispatched (NO line mentioning agt2 at all — a success line must fail this too, not just skipped/failed)
31-node-upgrade-fleet	not_covered	31 (external-review M4, honest — rescoped after drill 33 took single-node ownership): a SUCCESSFUL fleet-wide --all rollout (healthy canary COMMITTED + fan-out + two-node summary)
32-install-lifecycle	assert_ok	fresh bare container systemd up (NOT provisioned)
32-install-lifecycle	assert_ok	ctl --dry-run as sim (zero download/chmod/write)
32-install-lifecycle	assert_ok	agent --dry-run as sim (needs dummy broker/session/nid)
32-install-lifecycle	assert_ok	broker --dry-run as root (skip-download)
32-install-lifecycle	assert_ok	dry-run ZERO write (CONTENT+metadata manifest byte-identical after 3 dry-runs — sha256/mode/owner/link, not just path names; M5)
32-install-lifecycle	assert_ok	broker install (real, skip-download uses baked binaries)
32-install-lifecycle	assert_ok	never-start invariant: no tether process after install (install.sh starts nothing)
32-install-lifecycle	assert_ok	ownership: /etc/tether root-owned AND /etc/tether/nats.d tether-owned (real install.sh layout)
32-install-lifecycle	assert_ok	manifest is CONTENT-sensitive (mutating a byte of an existing tracked file MOVES the digest — the old path-name md5 could not detect this; M5)
32-install-lifecycle	assert_ok	#76 broker units are ENABLED for boot (nats-server tether-broker caddy all is-enabled)
32-install-lifecycle	assert_ok	#76 enabled-but-NOT-started: still no tether process (enable is a symlink, not a start — K.0 §2 amended)
32-install-lifecycle	assert_ok	uninstall before the --no-enable check
32-install-lifecycle	assert_ok	#76 broker install --no-enable
32-install-lifecycle	assert_ok	#76 --no-enable leaves the units DISABLED (the operator opted out)
32-install-lifecycle	assert_ok	reinstall broker (default, re-enables) for the #77 + uninstall checks below
32-install-lifecycle	assert_ok	#76 default reinstall re-enabled the units
32-install-lifecycle	assert_ok	H-7' fixture: strip the two ping keys from the installed nats.conf (a pre-liveness conf)
32-install-lifecycle	assert_ok	H-7' a re-run over a pre-liveness nats.conf exits 0 (KEPT, not overwritten)
32-install-lifecycle	assert_ok	H-7' the kept nats.conf is BYTE-IDENTICAL after the re-run (sha256 unchanged — the DRD-F1 keep policy holds for this file)
32-install-lifecycle	assert_ok	H-7' the .new beside it carries exactly the two client-liveness lines in the load-bearing shapes (quoted duration, bare integer), read from install.sh's single definition
32-install-lifecycle	assert_ok	H-7' the KEPT report names the two lines to merge and says the conf predates the keys (the operator merges exactly those, nothing else)
32-install-lifecycle	assert_ok	H-7' fixture: a nats.conf carrying the keys with OTHER values (ping_interval: \"45s\")
32-install-lifecycle	assert_ok	H-7' the KEPT report names the VALUE difference (carries ping_interval=45s) instead of staying silent because a ping_interval line exists (R3-5)
32-install-lifecycle	assert_ok	H-7' cleanup: restore the shipped ping values so the checks below see the stock conf
32-install-lifecycle	assert_ok	#77 journald drop-in exists (/etc/systemd/journald.conf.d/60-tether.conf)
32-install-lifecycle	assert_ok	#77 drop-in SystemMaxUse == the tier the drill INDEPENDENTLY derives from df /var/log (not trusting the product's number)
32-install-lifecycle	assert_ok	uninstall before the operator-setting check
32-install-lifecycle	assert_ok	#77 pre-seed an explicit operator SystemMaxUse in journald.conf
32-install-lifecycle	assert_ok	reinstall broker with the operator setting present
32-install-lifecycle	assert_ok	#77 our drop-in is NOT written when the operator already set SystemMaxUse (operator setting respected)
32-install-lifecycle	assert_ok	#77 cleanup: remove the seeded journald.conf + reinstall clean for the uninstall check
32-install-lifecycle	assert_ok	#76 uninstall DISABLES the units before removing them (no dangling boot symlinks)
32-install-lifecycle	assert_ok	#77 uninstall removed our journald drop-in
32-install-lifecycle	assert_ok	reinstall broker idempotent after the full-symmetry uninstall
32-install-lifecycle	assert_ok	uninstall broker → tether-broker.service removed
32-install-lifecycle	assert_ok	reinstall broker idempotent (clean second install)
32-install-lifecycle	assert_ok	reinstall still never-starts
32-install-lifecycle	assert_ok	agent install (as sim) targets ~/.local/bin (user-writable BIN_DIR; node upgrade atomic-replace target; S0-layout)
32-install-lifecycle	assert_ok	AGENT-real install.sh --role agent REAL-downloads+extracts the S0-artifact tarball → a real binary at ~/.local/bin/tether (NOT --skip-download; exercises place_binary atomic-place, not a stub)
32-install-lifecycle	assert_ok	AGENT-real the placed binary RUNS ('tether version' — proves a genuine extraction, not a dry-run no-op)
32-install-lifecycle	assert_ok	AGENT-real never-start: install.sh started nothing after the real agent install
32-install-lifecycle	assert_ok	AGENT-real uninstall REMOVES the placed binary (~/.local/bin/tether gone — a real binary-placement + removal boundary)
32-install-lifecycle	assert_ok	CTL-real install.sh --role ctl REAL-downloads+extracts the S0-artifact tarball → ~/.local/bin/tether (ctl's OWN place_binary path; as sim, /usr/local/bin unwritable → ~/.local/bin)
32-install-lifecycle	assert_ok	CTL-real the placed ctl binary RUNS ('tether version' — genuine extraction, not a dry-run no-op)
32-install-lifecycle	assert_ok	CTL-real never-start: install.sh --role ctl started nothing (ctl is a user tool — installs no service at all)
32-install-lifecycle	assert_ok	CTL-real uninstall REMOVES the placed ctl binary (~/.local/bin/tether gone)
32-install-lifecycle	assert_ok	84-up 1 broker + 1 agent + 1 ctl (fresh sim topology for the §8.4 running-broker upgrade)
32-install-lifecycle	assert_ok	84-init brk1 (N=1 cluster: auth_callout + JS bootstrap)
32-install-lifecycle	assert_ok	84-session lab + ctl login (owner)
32-install-lifecycle	assert_ok	84-agent-join agt1 (onboards + tunnel to brk1)
32-install-lifecycle	assert_ok	84-baseline expose biz on brk1 serves the sentinel (business DATA plane LIVE pre-upgrade)
32-install-lifecycle	assert_ok	84-pre-version brk1 runs v-cur (NOT $NEXTVER yet — anti-vacuity for the flip below)
32-install-lifecycle	assert_ok	84-stop systemctl stop tether-broker (business goes DOWN)
32-install-lifecycle	assert_ok	84-down expose biz curl → connection-refused (broker stopped; business genuinely down, curl exit 7 — not a half-open listener)
32-install-lifecycle	assert_ok	84-[env PRIVILEGED] swap /usr/local/bin/tether → tether-next (root-owned bin; operator installs it, cluster/node upgrade does NOT — a distinct labeled step)
32-install-lifecycle	assert_ok	84-staged-sha the on-disk binary is now the staged tether-next (sha matches vendor/tether-next)
32-install-lifecycle	assert_ok	84-integrity PRAGMA integrity_check == ok (broker-ops §8.4 line 535 — the tether.db is intact across the binary swap)
32-install-lifecycle	assert_ok	84-start systemctl start tether-broker (the NEW binary boots + runs G.2 reconcile)
32-install-lifecycle	assert_ok	84-converge-persist original expose biz serves the sentinel AGAIN on brk1:$PB84 (agent re-registered + state PERSISTED across the swap — G.2 reconcile; real tunneled bytes)
32-install-lifecycle	assert_ok	84-converge-write a NEW expose biz2 created POST-upgrade serves the sentinel (control-plane WRITE + data plane BOTH work on the new binary — real read-write, not a health field)
32-install-lifecycle	assert_ok	84-version-flip brk1 running (self-reported) version flipped v-cur→$NEXTVER via cluster status .release_version (a 'no upgrade happened' run would leave it on v-cur — the false-green guard the plan mandates)
32-install-lifecycle	assert_ok	84-pid-changed tether-broker MainPID CHANGED across §8.4 (a real systemctl stop/start swap, NOT drill 30's syscall.Exec in-place re-exec — version-flip ∧ PID-changed TOGETHER prove a genuine swap happened)
33-node-upgrade-success	assert_ok	up 1 broker + 1 agent + 1 ctl
33-node-upgrade-success	assert_ok	init brk1 (N=1 cluster)
33-node-upgrade-success	assert_ok	SETUP broker.upgrade.url_allow += artifact prefix (labeled operator config) + broker restart
33-node-upgrade-success	assert_ok	session lab + ctl login (owner)
33-node-upgrade-success	assert_ok	agent-join agt1
33-node-upgrade-success	assert_ok	SETUP agt1b sibling unit (own HOME/nkey, SAME binary path) + ONLINE
33-node-upgrade-success	assert_ok	DIE-GATE both units execute the same binary path AND inode (upgrade-domain premise non-vacuous)
33-node-upgrade-success	assert_ok	SETUP artifact serving tether-next (ART_URL/ART_SHA populated)
33-node-upgrade-success	assert_ok	SETUP inject instance CA into agt1 trust store
33-node-upgrade-success	assert_ok	SETUP agt1 agent.upgrade.url_allow += artifact prefix + restart + ONLINE
33-node-upgrade-success	assert_ok	SETUP agt1b agent.upgrade.url_allow (own HOME tree, unit tether-agent-b) + restart + ONLINE
33-node-upgrade-success	assert_ok	B1 syn-block agt1 -> brk1:4222 (new connections only)
33-node-upgrade-success	assert_ok	B1b self-proof: rule took effect — a NEW connection to brk1:4222 black-holes (rc 124, not refused)
33-node-upgrade-success	assert_ok	B1c established flows live: agt1 still ONLINE through its pre-fault registration
33-node-upgrade-success	assert_ok	B2 staged: ctl printed the verbatim staged line (both url gates + download + sha + smoke + flip all passed)
33-node-upgrade-success	assert_ok	B3 pending window (whole conjunction): marker pending ∧ boot_count>=1 ∧ dst==NEXT ∧ MainPID==PID0 ∧ /proc/exe==NEXT (in-place exec of the staged image, and its boot shim really ran)
33-node-upgrade-success	assert_ok	C-pre agt1b still ONLINE (its established registration is untouched by the SYN-only block)
33-node-upgrade-success	assert_refuses	C sibling agt1b refused BY THE AGENT while B pends — agent_rejected:upgrade_in_progress (a broker-side refusal carries no agent_rejected: prefix; which agent-side gate fired is not observable from ctl — see the header note)
33-node-upgrade-success	assert_ok	B4 watchdog rollback (whole conjunction, fault still armed ⇒ marker static): rolled_back ∧ Detail~'register deadline exceeded' ∧ dst==OLD ∧ .prev consumed ∧ MainPID==PID0 ∧ /proc/exe==OLD
33-node-upgrade-success	assert_ok	B5 ctl verdict: rc!=0 AND the output carries BOTH the staged line AND 'likely ROLLED BACK' naming $OLD_RELEASE (inference oracle — the truth is B4's disk/process conjunction)
33-node-upgrade-success	assert_ok	B6 heal: lift the syn-block
33-node-upgrade-success	assert_ok	B6b the rolled-back OLD binary re-registers ONLINE at OLD_RELEASE (pure product self-heal, no operator action)
33-node-upgrade-success	assert_ok	B6c broker journal carries the agent-reported rolled_back outcome (only a register carrying upgrade_state from the marker's target can emit it)
33-node-upgrade-success	assert_ok	B6d delivered terminal marker is REMOVED (one-shot delivery proof — S24 asymmetry)
33-node-upgrade-success	assert_ok	A1 ctl success: rc=0 ∧ staged line ∧ 'waiting for re-register (deadline 120s)' ∧ 'upgrade COMMITTED' naming $NEXT_RELEASE (phrases not whole lines — two COMMITTED variants exist)
33-node-upgrade-success	assert_ok	A2 in-place exec: MainPID unchanged ∧ ExecMainStartTimestamp unchanged ∧ /proc/PID0/exe sha==NEXT (same PID through THREE execs; only a real re-exec runs the new bytes)
33-node-upgrade-success	assert_ok	A3 registration/disk interlock: node ls RELEASE==NEXT ∧ ONLINE; dst==NEXT; .prev==OLD; marker committed ∧ boot_count==1 ∧ new_sha==NEXT ∧ target_nid==agt1; journal committed outcome
33-node-upgrade-success	assert_ok	A4 version-skew pin: agt1b still ONLINE at OLD_RELEASE (the sibling keeps running the old image; the four-part proof kept it from claiming A's commit)
33-node-upgrade-success	assert_ok	C2 domain released: sibling's own upgrade COMMITs (rc=0 ∧ COMMITTED ∧ agt1b ONLINE@NEXT ∧ agt1b MainPID unchanged ∧ marker target_nid==agt1b)
33-node-upgrade-success	not_covered	33 (gotcha #73, honest): a NON-tether artifact that fakes the frozen version line passes the smoke gate, then has NO boot shim — budget never ticks, the watchdog died with the old image, the marker pends forever, the node is stranded behind NAT
40-drain-retire	assert_setup	create session fixture
40-drain-retire	assert_setup	join expose-capable agent fixture
40-drain-retire	assert_ok	D-drain: cluster drain $T succeeds (rc=0)
40-drain-retire	assert_ok	D-drain FG: intermediate DRAINING phase OBSERVED before --abort
40-drain-retire	assert_ok	D-drain: cluster drain $T --abort succeeds (rc=0)
40-drain-retire	assert_ok	D-drain: $T back to VOTER after --abort
40-drain-retire	assert_refuses	drain --retire is REMOVED → steers to cluster retire
40-drain-retire	assert_ok	OPS-OBS: ops ls --json schema=cluster_ops v1
40-drain-retire	assert_ok	OPS-OBS: ops show --json schema=cluster_op v1
40-drain-retire	assert_ok	RECONCILE-plan: --manual --plan on a clustered (multi-user) conf REQUIRES --account-issuer/--broker-nkey (KEPT guard)
40-drain-retire	assert_ok	RECONCILE-plan: zero-write (nats.conf md5 unchanged after the --plan attempt)
40-drain-retire	assert_ok	RECONCILE-plan full renderer: rc=0, schema v1, all 3 routes/auth users, dry_run result present
40-drain-retire	assert_ok	RECONCILE-plan human footer says NOTHING WAS WRITTEN and gives leader-last restart order
40-drain-retire	assert_ok	RECONCILE-plan full renderer leaves nats.conf and the entire .bak set byte/mtime-identical
40-drain-retire	assert_setup	APPLY-plan fixture: write a valid desired roster on $NL
40-drain-retire	assert_refuses	APPLY-plan on non-leader refuses a non-authoritative roster view
40-drain-retire	assert_setup	ADD-dryrun fixture: install a CA-signed dry4 route certificate with SAN=DNS:dry4
40-drain-retire	assert_ok	ADD-dryrun: resolves and prints a grow plan for a not-yet-provisioned target
40-drain-retire	assert_ok	ADD-dryrun: complete DB/config/raft fingerprint is unchanged
40-drain-retire	assert_ok	OPS-CONFIRM fixture: genuine unreachable nonvoter reaches BLOCKED after its catch-up deadline
40-drain-retire	assert_ok	OPS-CONFIRM: confirm retries the BLOCKED join
40-drain-retire	assert_ok	OPS-CONFIRM: operation re-enters CATCHING_UP
40-drain-retire	assert_ok	OPS-ABORT: abort makes the in-flight operation terminal
40-drain-retire	assert_ok	OPS-ABORT: op state is ABORTED
40-drain-retire	assert_ok	OPS-ABORT: same target admits a fresh operation after abort
40-drain-retire	assert_ok	OPS cleanup: raw force-remove the explicitly staged nonvoter
40-drain-retire	assert_ok	OPS cleanup: synthetic target is absent from the authoritative roster
40-drain-retire	assert_refuses	SAFE: recovery node remove --yes rejected (NO --yes override, Tier-2)
40-drain-retire	assert_refuses	SAFE: recovery node remove WITHOUT --manual rejected (requires --manual)
40-drain-retire	assert_refuses	SAFE: machine-confirm missing env → refused (double-factor)
40-drain-retire	product_red	#31 grow-lock leak BLOCKS cluster retire [#31] — the first and only retire attempt matched 'grow of … is in progress / membership operation in flight'; retire spine blocked this run
40-drain-retire	assert_setup	R-resume: old leader returns before irreversible removal
40-drain-retire	product_red	#45 retire convergence defect: the captured ops-show document remained state=$OP_STATE last_error=$OP_ERR after 150s; terminal RETIRED was not reached
40-drain-retire	assert_ok	R-retire PRIMARY oracle: control-source WRITE succeeds on the surviving cluster (raft coherent)
40-drain-retire	assert_ok	R-hint: retire NOTE 'NOT a credential revocation' was printed (from captured retire output)
41-shrink-to-standalone	assert_setup	create the raft-replicated baseline session
41-shrink-to-standalone	assert_setup	join agt1 directly through the broker that will be retired first ($AGENT_RETIRE_TARGET)
41-shrink-to-standalone	assert_setup	fixture: restart the bound agent once without its learned discovery cache (single seed remains $AGENT_RETIRE_TARGET)
41-shrink-to-standalone	assert_ok	BEFORE: agt1 has a real direct NATS connection on the soon-to-retire broker ($AGENT_RETIRE_TARGET)
41-shrink-to-standalone	assert_ok	BEFORE: exactly 3 voters (grow_to_3 established the N=3 baseline to shrink FROM)
41-shrink-to-standalone	assert_ok	BEFORE: the raft-replicated session '$SID' exists (survival oracle baseline)
41-shrink-to-standalone	assert_refuses	S-refuse-peers: --to-standalone --confirm-single with 2+ voters HARD-REFUSED
41-shrink-to-standalone	assert_refuses	S-refuse-no-confirm: --to-standalone WITHOUT --confirm-single refused (FINAL N=1 gate)
41-shrink-to-standalone	assert_ok	S-rebind: set-raft-addr in-place rebind accepted (roster raft_addr updated; run reconcile after)
41-shrink-to-standalone	not_covered	41 SHRINK first-retire PROACTIVE fast-path #1: agt1 does not RELIABLY leave the retired-but-still-meshed $_b inside a justifiable window
41-shrink-to-standalone	not_covered	41 SHRINK first-retire PROACTIVE fast-path #2: agt1 does not RELIABLY reconnect to a remaining VOTER inside that window
41-shrink-to-standalone	assert_ok	SHRINK first-retire: agt1 stays FUNCTIONALLY reachable across the retire — a real exec crosses the post-retire command path (the correctness invariant on a meshed retire; NON-VACUITY: _agent_live is a bounded ctl exec, a dead path REDs)
41-shrink-to-standalone	assert_ok	SHRINK: reached EXACTLY 1 voter ($LDR) via cluster retire (#31 released this run)
41-shrink-to-standalone	assert_ok	SHRINK: final membership operation is explicitly captured for the N=1 de-cluster boundary
41-shrink-to-standalone	assert_ok	S-tostandalone GUARD precondition: the conf is STILL clustered (else the already-standalone refusal — which also mentions --reset-js — would bank this guard for the wrong reason and the de-cluster arm would go green without de-clustering)
41-shrink-to-standalone	assert_refuses	S-tostandalone GUARD: bare --to-standalone REFUSES on a data-bearing JS store and names --reset-js + the data impact (R16 A4)
41-shrink-to-standalone	assert_ok	S-tostandalone: reconcile --to-standalone --reset-js de-clusters the conf AND moves the clustered JS store aside (ONE product verb; the hand-rolled mv is retired)
41-shrink-to-standalone	assert_ok	S-tostandalone: nats.conf has NO cluster{} block (real de-cluster)
41-shrink-to-standalone	assert_ok	S-tostandalone: the PRODUCT moved the store aside (never deleted) — a standalone-bak.* is on disk
41-shrink-to-standalone	assert_ok	S-jsreset [operator per runbook §2.2]: restart the services the product told the operator to restart
41-shrink-to-standalone	assert_ok	S-jsreset [operator per runbook §2.2]: tether-broker is ACTIVE again after the JS-store reset + restart
41-shrink-to-standalone	assert_ok	S-final-retire: only after live standalone NATS loads, the final op reaches terminal RETIRED
41-shrink-to-standalone	assert_ok	S-final-retire: every remaining voter reports topo_observed >= topo_desired
41-shrink-to-standalone	assert_ok	S-survival setup (#48 FLIPPED, R14): agt1 ESCAPES the retired-broker island within the silence-rebuild SLA and has a REAL post-restart command path on the standalone survivor (not a stale ONLINE row)
41-shrink-to-standalone	assert_ok	S-survival setup: ctl can re-login to the original session after JS reset
41-shrink-to-standalone	assert_ok	S-survival: the pre-shrink session '$SID' SURVIVES to N=1 standalone (raft-replicated row intact across the JS reset)
41-shrink-to-standalone	assert_ok	S-restart-tierB: create payload strictly larger than 8 MiB
41-shrink-to-standalone	assert_ok	S-restart-tierB: push WORKS at N=1 standalone and reports tier=B
41-shrink-to-standalone	assert_ok	S-r3-persist: successful broker restart keeps nats.conf standalone (R3)
41-shrink-to-standalone	product_red	#31 grow-lock leak BLOCKS shrink retires [#31] — first retire attempt matched 'grow of … is in progress / membership operation'; strict drill does not retry or clear the lock (voters=$(_voters))
41-shrink-to-standalone	product_red	#45 retire STALLS at NATS_ROLLED_OUT (never reaches terminal RETIRED), then refuses the next retire 'already in flight' [#45] — signature matched; a retire-convergence stall DISTINCT from #31, blocks the shrink spine this run (voters=$(_voters))
42-rejoin-returning	assert_refuses	C diagnose: ALIVE peer detected → force-single blocked (a peer is still reachable)
42-rejoin-returning	assert_ok	C diagnose is READ-ONLY: exact leader+roster projection unchanged
42-rejoin-returning	assert_ok	brk2 provably dead (:7400 connection-refused)
42-rejoin-returning	assert_ok	B diagnose (dead-peer POSITIVE): command exits 0 and prints pasteable force-single command
42-rejoin-returning	assert_ok	B diagnose is READ-ONLY: exact leader+roster projection unchanged
42-rejoin-returning	assert_setup	H stop brk1 daemon while its on-disk roster still contains brk2
42-rejoin-returning	assert_refuses	H resnapshot refuses unless the on-disk roster is SINGLE-VOTER
42-rejoin-returning	assert_ok	OFFLINE force-single --reset-js brk1 → lone N=1 survivor (prunes dead brk2; de-clusters conf AND moves the clustered JS store aside; runs to completion, rc=0)
42-rejoin-returning	assert_ok	OFFLINE force-single de-clustered nats.conf to standalone (#20 post-condition on the FILE, not a log line)
42-rejoin-returning	assert_ok	OFFLINE force-single --reset-js MOVED the clustered JS store aside itself (jetstream.force-single-bak.* present — the product verb, not the sim's old mv)
42-rejoin-returning	assert_ok	OFFLINE force-single: the operator's systemctl restart loads the standalone conf + fresh store; both services return active (systemctl stays sim-side per Mandate ③; the JS reset was the product's job)
42-rejoin-returning	assert_setup	D pre-create brk2 forensic dump path as the data-dir owner (force O_EXCL)
42-rejoin-returning	assert_refuses	D O_EXCL: rejoin prepare with a pre-existing dump on an INTACT DB → WIPE REFUSED (safety)
42-rejoin-returning	assert_ok	D O_EXCL: raft/ and tether.db STILL present after the refused wipe (data intact)
42-rejoin-returning	assert_setup	D remove only the colliding empty forensic pathname
42-rejoin-returning	assert_ok	D positive: returning brk2 emits forensic dump + identity manifest, then wipes old timeline
42-rejoin-returning	assert_ok	D dump+manifest are non-empty 0600 and the divergent DB/raft are gone
42-rejoin-returning	assert_ok	D manifest carries brk2's complete identity, cluster mode, and no business-state replay
42-rejoin-returning	assert_ok	E init --from-manifest creates a fresh single-voter identity for brk2
42-rejoin-returning	assert_ok	E fresh DB/raft contain exactly the manifest self identity (not the old two-node roster)
42-rejoin-returning	assert_setup	I stage the transfer-audit payload on ctl1
42-rejoin-returning	assert_refuses	I GATE: the same push WITHOUT --ack-alerts is BLOCKED by the force_single_active severe gate (gateDestructive, alert_gate.go — a KEPT design guard, not a defect; this is why the fixture below must pass the override)
42-rejoin-returning	assert_setup	I create real post-force-single transfer-audit Raft entries (--ack-alerts overrides the force_single_active gate the assertion above pins)
42-rejoin-returning	assert_setup	G stop lone brk1 before offline resnapshot
42-rejoin-returning	assert_setup	I inject a stale audit_published_index=0 in the offline FSM DB
42-rejoin-returning	assert_refuses	I resnapshot without --accept-audit-loss refuses the real unpublished audit window
42-rejoin-returning	assert_ok	G+I explicit --accept-audit-loss performs the single-voter resnapshot and becomes grow-ready
42-rejoin-returning	assert_ok	G resnapshot does not resurrect a stale non-self peer from the Raft tail
42-rejoin-returning	assert_setup	G restart resnapshotted brk1 and recover a writable leader
42-rejoin-returning	assert_setup	G brk1 is leader again
42-rejoin-returning	assert_ok	F [REJOIN TERMINUS] the returning node RE-GROWS into the cluster: 'cluster add' resumed the pre-approved join op and completed the mesh/start orchestration (rc=0). This is the terminus of the whole recovery journey — force-single, rejoin prepare, init --from-manifest, resnapshot are all worthless if the node cannot come back
42-rejoin-returning	product_red	#GROW-ONTO-FORCE-SINGLE (owner: R9 product side; the deploy-tier finding this arm exists to surface) — every documented recovery step SUCCEEDED (force-single, rejoin prepare with dump+manifest, init --from-manifest, resnapshot with --accept-audit-loss, brk1 leader again) and yet the re-grow of the returning node FAILED with a force-single signature (rc=$_F_GROWRC): the survivor's JetStream was left clustered-lone-voter (503), so the returning joiner crash-loops on n1ClusteredJetStreamFatal, never meshes, and stalls the op at CATCHING_UP. A survivor that can never be grown back out of force-single means the documented escape hatch is one-way: the cluster stays permanently N=1 with no redundancy. Full 'cluster add' output logged verbatim above
42-rejoin-returning	product_red	#47 (owner: R9 product side) — the returning brk2 was admitted but never converged to VOTER: 'cluster add' left a REACHABLE joiner in CATCHING_UP (rc=$_F_GROWRC) with NO force-single root present. docs/deploy-tier-gotchas.md:521 — the subsequent grow is then blocked by the serialization lock. R7b bounded the CATCHING_UP state and R9 added the controller-loop tests, but the deploy-tier verifier for the AddVoter/isVoter branch is this drill (R9 risk 2 named it explicitly: that branch is unreachable in a hermetic single-node raft)
42-rejoin-returning	assert_ok	F [REJOIN TERMINUS] the returning node must RE-GROW into the cluster — it did not (rc=$_F_GROWRC), and the failure matches NEITHER the #47 CATCHING_UP signature NOR a force-single one. Undocumented: surfaced rather than laundered into a known defect (full 'cluster add' output logged verbatim above)
42-rejoin-returning	assert_ok	F returning brk2 is a reachable VOTER in the authoritative roster
42-rejoin-returning	assert_setup	F seed a real tier-A workload after rejoin
42-rejoin-returning	assert_ok	F post-rejoin real workload transfers through the surviving session/agent
42-rejoin-returning	assert_ok	F post-rejoin workload bytes are exact on agt1
42-rejoin-returning	not_covered	42 F post-rejoin VOTER + tier-A workload round-trip THIS RUN
42-rejoin-returning	assert_refuses	J rejoin prepare --yes rejected (Tier-2, never-escapable)
42-rejoin-returning	assert_refuses	J resnapshot --yes rejected (Tier-2)
42-rejoin-returning	assert_refuses	K resnapshot machine-confirm missing env → refused
42-rejoin-returning	assert_refuses	K CONTRAST: rejoin-prepare has NO --confirm-node-id flag (never-escapable, unlike resnapshot)
43-migrate-live-data	assert_setup	up 1 un-init P2 broker + agent + ctl
43-migrate-live-data	assert_setup	start the uninitialized P2 broker once to create tether.db
43-migrate-live-data	assert_setup	wait for a non-empty pre-migration tether.db
43-migrate-live-data	assert_setup	mint the documented single-mode auth_callout secrets
43-migrate-live-data	assert_setup	distribute the documented single-mode auth_callout secrets
43-migrate-live-data	assert_setup	A [env, broker-ops §3.4] stop services before rendering standalone auth_callout
43-migrate-live-data	assert_setup	A [env] render standalone nats.conf with auth_callout issuer/broker nkey
43-migrate-live-data	assert_setup	A [env] enable the documented single-mode auth_callout seed flag in the real service unit
43-migrate-live-data	assert_setup	A create the pre-migration live session
43-migrate-live-data	assert_setup	A join the pre-migration live agent
43-migrate-live-data	assert_setup	A provision agt1 tunnel endpoint for the live expose
43-migrate-live-data	assert_ok	A create pre-migration live expose row
43-migrate-live-data	assert_ok	A pre-migration DATA PLANE returns the exact sentinel
43-migrate-live-data	assert_ok	A create a pre-migration history row with a real exec
43-migrate-live-data	assert_ok	A pre-migration session row is readable
43-migrate-live-data	assert_setup	B stop broker for offline migration checks
43-migrate-live-data	assert_setup	B save exact pre-migration nats.conf and broker.yaml rollback anchors
43-migrate-live-data	assert_ok	B init --check: NO tether.db.bak written (zero-write, not just exit 0)
43-migrate-live-data	assert_ok	B init --check: tether.db byte-identical (md5 unchanged)
43-migrate-live-data	assert_refuses	C init --yes rejected (NO --yes override, Tier-2)
43-migrate-live-data	assert_refuses	D init machine-confirm missing env → refused
43-migrate-live-data	assert_ok	E noninteractive machine-confirm cutover succeeds without a PTY and prints the reconcile step
43-migrate-live-data	assert_setup	E execute the printed standalone N=1 reconcile inputs
43-migrate-live-data	assert_setup	E apply cluster seam, validate nats.conf, restart services
43-migrate-live-data	assert_ok	E from-existing cutover reaches an authoritative N=1 leader
43-migrate-live-data	assert_ok	E cutover: tether.db.bak is non-empty mode 0600
43-migrate-live-data	assert_ok	E cutover: broker.yaml carries the exact brk1 raft_addr cluster seam
43-migrate-live-data	assert_ok	F original session survives the cutover
43-migrate-live-data	assert_ok	F original agent reconnects ONLINE
43-migrate-live-data	assert_ok	F original expose port serves the exact same sentinel after cutover
43-migrate-live-data	assert_ok	F original history contains the pre-migration exec sentinel
43-migrate-live-data	assert_ok	G rollback [operator per runbook §4]: stop + restore DB, exact nats.conf and exact broker.yaml
43-migrate-live-data	assert_ok	G rollback ① DB byte-exact: tether.db == the pristine tether.db.bak (md5 match, not just cp exit 0)
43-migrate-live-data	assert_ok	G rollback ② cluster-mode OFF: broker.yaml no longer has the raft_addr cluster seam
43-migrate-live-data	assert_ok	G rollback ③ process restart: broker boots as working standalone with auth_callout
43-migrate-live-data	assert_ok	G rollback ④ the ORIGINAL session is readable after rollback
43-migrate-live-data	assert_ok	G rollback ④ the ORIGINAL agent reconnects ONLINE without re-provisioning
50-backup-restore	assert_setup	grow_to_2 (N=2 VOTER + JS meta formed + leader pinned brk1)
50-backup-restore	assert_setup	vault init (S0-backup-vault: 0700, empty — AFTER grow so a retry-nuke cannot reap it)
50-backup-restore	assert_setup	session $SID + ctl login
50-backup-restore	assert_setup	agent-join agt1
50-backup-restore	assert_setup	provision agt1 agent.yaml (tunnel_addr wired, S0-tunnel)
50-backup-restore	assert_setup	expose agt1:8081 --on-broker brk1 --name live (home PINNED: without it #29 makes this a ~50% coin flip — measured — and would false-RED restore; the #29 data-plane surface is drill 71's property)
50-backup-restore	assert_ok	B1 pre-disaster data plane: ctl1 curls brk1:$PX and gets the EXACT sentinel
50-backup-restore	assert_ok	B2 seed history: exec prints BACKUP-HISTORY-SENTINEL
50-backup-restore	assert_ok	B3 history carries the exec row (the audit read path works BEFORE the disaster)
50-backup-restore	assert_ok	Q1 incident export --out writes a 0600 bundle
50-backup-restore	assert_ok	Q1b incident bundle is 0600
50-backup-restore	assert_ok	Q1c incident schema + roster of 2 (real cluster shape)
50-backup-restore	assert_refuses	Q2 incident export --out refuses to clobber an existing file (O_EXCL)
50-backup-restore	assert_ok	Q3 incident export --force overwrites (the CONTROL SOURCE for Q2 — proves the verb works)
50-backup-restore	assert_ok	Q4a stage a symlink pointing at a REAL private key
50-backup-restore	assert_refuses	Q4b incident export --force still refuses to FOLLOW a symlink (O_NOFOLLOW)
50-backup-restore	assert_ok	Q4c the private key the symlink pointed at is byte-identical (the REAL result oracle)
50-backup-restore	assert_ok	Q6 incident export --since smoke
50-backup-restore	assert_ok	R1 doctor --offline is green on a healthy node (as User=tether — root would bypass the real open() check)
50-backup-restore	assert_refuses	R2 doctor --offline --conf <nonexistent> FAILS (the CONF cell's control source — that cell always did a real read)
50-backup-restore	assert_ok	R3-prep stage the six pathological --db inputs (missing / dir / empty / truncated / unreadable / corrupt-page)
50-backup-restore	assert_refuses	R3a #50 state 1/6 — a NONEXISTENT --db is FATAL (this is #50's own literal reproducer, which used to exit 0)
50-backup-restore	assert_ok	R3a-rc the #50 reproducer exits 64 (usage), not 0 — the class a calling script branches on
50-backup-restore	assert_ok	R3a-cell the DB CELL is the one reporting FATAL (summary.fatal>=1 alone would be satisfied by ANY other cell going red)
50-backup-restore	assert_refuses	R3b #50 state 2/6 — a DIRECTORY passed as --db is FATAL (os.Stat + IsDir)
50-backup-restore	assert_refuses	R3c #50 state 3/6 — an EMPTY (0-byte) file is FATAL: it opens, pings and quick_checks perfectly, so ONLY the schema probe can see it
50-backup-restore	assert_refuses	R3d #50 state 4/6 — a TRUNCATED db is FATAL
50-backup-restore	assert_refuses	R3e #50 state 5/6 — a PERMISSION-DENIED db is FATAL and is diagnosed as UNREADABLE, not as corrupt (the layer the lazy sql.Open hid best)
50-backup-restore	assert_refuses	R3f #50 state 6/6 — a CORRUPT PAGE is FATAL: the file opens, pings and carries a schema, so quick_check is the only layer that can catch it
50-backup-restore	assert_ok	R3g ANTI-VACUITY: the identical probe against the LIVE tether.db is green — the six reds above discriminate, they are not a doctor that FATALs on everything
50-backup-restore	assert_ok	DOC-27 CLOSED: the runbook §5 ONLINE-backup literal example (--out /var/lib/tether/backups/tether-\$(date +%F)) runs as User=tether on a stock install — install.sh makes LIB_DIR tether-owned, so the daemon's MkdirAll of the backup leaf succeeds
50-backup-restore	product_red	DOC-27 REGRESSION: the CORRECTED runbook §5 example \'cluster backup --out /var/lib/tether/backups/tether-\$(date +%F)\' is unrunnable again on a stock install (parent-dir/perm failure) — the doc fix regressed. Captured: $(printf '%s' 
50-backup-restore	assert_ok	D backup on the LEADER (the freshest committed state)
50-backup-restore	assert_ok	D-sig it self-reports as a LEADER-sourced online bundle
50-backup-restore	assert_ok	D-#53-silence the BACKUP end warns UNMISSABLY about the bundle's scope: it names 'BUNDLE SCOPE', says JetStream is NOT in it, and prints the runnable \`nats stream backup\` alternative (a bare header with no content and no remedy would satisfy a one-clause grep — this one cannot)
50-backup-restore	assert_ok	D2 [env, S0-backup-vault] pull the bundle off the box (= an operator scp-ing it away)
50-backup-restore	assert_ok	D3 manifest names brk1 as a LEADER-sourced bundle over a 2-node roster
50-backup-restore	assert_ok	E1 manifest keys are a subset of the known non-secret allowlist (a denylist would miss future keys)
50-backup-restore	assert_ok	E2 no PEM key / certificate / nkey seed material anywhere in the bundle
50-backup-restore	assert_ok	E3 self_cert_fp is a FINGERPRINT (sha256:<hex>), not the certificate itself
50-backup-restore	assert_refuses	F1 backup on the FOLLOWER refuses and names the leader + the escape hatch
50-backup-restore	assert_ok	F1b the refusal happened BEFORE any directory was created (refused, not half-done)
50-backup-restore	assert_ok	F2 --allow-stale-follower permits it AND labels the bundle possibly-stale
50-backup-restore	assert_ok	F2b the stale bundle self-declares follower + names the leader (never used as an identity oracle here)
50-backup-restore	assert_refuses	G1 --offline refuses while the daemon holds the raft lock
50-backup-restore	assert_ok	G2a stop brk2's broker for the offline path
50-backup-restore	assert_ok	G2b offline backup on a STOPPED daemon
50-backup-restore	assert_ok	G2c the offline bundle carries account_fp (the semantic line between offline and online)
50-backup-restore	assert_ok	G2d restart brk2 and reconverge to 2 VOTER before continuing
50-backup-restore	assert_ok	G2e N=2 VOTER restored
50-backup-restore	assert_ok	B4a create session 'zed' AFTER the backup was taken
50-backup-restore	assert_ok	B4b Z EXISTS right now (without this baseline, L2 asserts nothing)
50-backup-restore	assert_ok	B4c switch the ctl back to $SID (session create activated 'zed'; the restore rolls it away, and a ctl left pointing at a destroyed session fails every later call with an Authorization Violation — a drill artifact, not a finding)
50-backup-restore	assert_ok	H1a stop brk1's units before wiping (never wipe jetstream/ under a live nats-server)
50-backup-restore	assert_ok	H1b DISASTER: wipe brk1's /var/lib/tether (lib-volume loss; /etc/tether survives)
50-backup-restore	assert_ok	H1c the DB is really gone
50-backup-restore	assert_ok	H1d the data plane is really DEAD (curl exit 7 = refused; NOT '! curl -sf', which a 4xx would satisfy)
50-backup-restore	assert_refuses	I1 restore --yes is refused (Tier-2: no unattended override by design)
50-backup-restore	assert_refuses	I2 restore is NEVER machine-escapable: correct --confirm-node-id AND correct \$TETHER_CONFIRM_NODE_ID, non-interactive => still refused
50-backup-restore	assert_ok	I-prep push the vaulted bundle back onto brk1 [operator per runbook §5]
50-backup-restore	assert_refuses	I3 restore refuses when --confirm-node-id names a node the bundle is not for
50-backup-restore	assert_ok	I4-prep stop brk2's broker + stage brk1's bundle there
50-backup-restore	assert_ok	I4-prep2 push brk1's bundle onto brk2
50-backup-restore	assert_refuses	I4 FOREIGN BUNDLE: brk1's bundle on brk2 is refused by the un-forgeable tunnel-cert anchor (NOT by the confirm-id gate — we satisfied that one on purpose)
50-backup-restore	assert_ok	I4b brk2's own DB was not touched by the refused foreign restore
50-backup-restore	assert_ok	I4c restart brk2
50-backup-restore	assert_ok	M1-prep tamper ONE semantic manifest value (public_host), keeping the JSON well-formed
50-backup-restore	assert_refuses	M1 a torn/edited bundle is refused on the manifest-vs-state.db cross-check
50-backup-restore	assert_ok	M1b the refusal wrote NOTHING: no .pre-restore backup was created (the gate is before restore.go:175)
50-backup-restore	assert_ok	M1c repair the manifest from the vault for the real restore
50-backup-restore	assert_ok	J2 restore succeeds via a real typed confirm (the CONTROL SOURCE for I1/I2/I3/I4/M1 — 'it always refuses' is not evidence a gate exists)
50-backup-restore	assert_ok	J2b restore pruned the stale peer + reset the cursor (the ONLY stdout evidence the prune ran)
50-backup-restore	assert_ok	J2c restore preserved the prior DB and printed where
50-backup-restore	assert_ok	J2d restore printed the ORDERED next-step list (R10 P4 replaced the single 'NEXT: start tether-broker' line that #64 was made of)
50-backup-restore	assert_ok	J2e (#53-silence) the RESTORE end warns that history/audit did NOT come back, says the re-derive cursor does NOT backfill, and prints the runnable \`nats stream restore\` inverse
50-backup-restore	assert_ok	J2f (P2) restore REPORTED on the broker.cluster seam via --config (default /etc/tether/broker.yaml) — here the idempotent branch, since the seam was provisioned when the cluster was built. A --config that was parsed and dropped would print nothing at all
50-backup-restore	assert_ok	J2g the broker.cluster seam carries ALL FIVE fields on disk (data_dir/raft_addr/secrets_dir/nats_conf_path/nats_server_bin) — serve keys cluster mode on data_dir alone, so a PARTIAL seam boots SINGLE mode and hits the very FATAL the seam prevents
50-backup-restore	assert_ok	K-#64a the restore completion text LEADS with the de-cluster step and states the broker will REFUSE to start without it — the advance warning #64 said was owed (it used to appear only in the boot FATAL, at crash time)
50-backup-restore	assert_ok	K-#64b that de-cluster remedy is COPY-PASTE-RUNNABLE: real substituted nkeys (--account-issuer A… / --broker-nkey U…), not the <…> placeholders it prints when secrets are unreadable
50-backup-restore	assert_ok	K1+K2 the broker becomes reachable after the restore (bounded self-heal off brk2's replica — NOT a de-cluster; a real total loss has no such peer, which is drill 51's territory)
50-backup-restore	not_covered	50-K-#64c the predicted lone-N=1 refusal was not observed in the broker slog this run
50-backup-restore	assert_ok	K3 status is DEGRADED / NOT-HA with exactly ONE voter and NO force_single (exit 1 alone has no discriminating power)
50-backup-restore	assert_ok	L1+L2 IDENTITY: X (lab, pre-backup) is present AND Z (zed, post-backup) RE-CONVERGED from the surviving peer (POLLED — re-convergence is timing-variable under load)
50-backup-restore	not_covered	50-L3 history survival via the replica
50-backup-restore	assert_ok	L4a agt1 comes back ONLINE after the restore
50-backup-restore	assert_ok	L4b expose 'live' kept its public port $PX (polled: restore -> re-home -> agent redial is ASYNC and queues behind #64's measured ~73s crash-loop; a one-shot read races it)
50-backup-restore	assert_ok	L4c DATA PLANE RESTORED: ctl1 curls brk1:$PX and gets the EXACT pre-disaster sentinel back
50-backup-restore	assert_ok	L4d the port is homed to the restored node (the epoch does NOT bump — measured: the home was already brk1, so restore's re-home-to-self is a no-op; the plan's '+1 epoch' prediction was wrong and is dropped rather than forced)
51-full-dr	assert_setup	grow_to_3 (N=3 HA)
51-full-dr	assert_setup	vault init (S0-backup-vault: survives rm_node --vols, dies with nuke — created AFTER grow so grow_to_3's retry-nuke cannot reap it)
51-full-dr	assert_setup	ensure brk1 is the leader (grow inits brk1, so this is usually a no-op — a redundant transfer to self would error 70)
51-full-dr	assert_setup	leader is brk1 (required: online backup is leader-gated AND #29 means the expose only serves when home==brk1)
51-full-dr	assert_setup	session $SID + ctl login
51-full-dr	assert_setup	agent-join agt1
51-full-dr	assert_setup	provision agt1 agent.yaml (tunnel_addr -> brk1, S0-tunnel)
51-full-dr	assert_setup	expose agt1:8091 --on-broker brk1 --name dr
51-full-dr	assert_ok	A-base1 three VOTER before the disaster
51-full-dr	assert_ok	A-base2 session $SID exists
51-full-dr	assert_ok	A-base3 PRE-DISASTER DATA PLANE: ctl1 curls brk1:$P and gets the exact sentinel (real tunneled bytes)
51-full-dr	assert_ok	A-base4 seed an exec row into history
51-full-dr	assert_ok	B-vault1 online backup on the LEADER
51-full-dr	assert_ok	B-vault1b it self-reports as a LEADER-sourced online bundle
51-full-dr	assert_ok	B-vault1c (#53-silence) the BACKUP end warns the bundle is state.db-only: names 'BUNDLE SCOPE', says JetStream is NOT in it, and prints the runnable \`nats stream backup\` alternative — the belief 'I have a backup' can no longer form silently over a control-plane-only bundle
51-full-dr	assert_ok	B-vault2 pull the bundle into the off-cluster vault
51-full-dr	assert_ok	B-vault3 the bundle is a leader snapshot of the full 3-node roster
51-full-dr	assert_ok	B-vault4 the bundle carries NO key material
51-full-dr	assert_ok	C-disaster1 DESTROY all three brokers and their volumes (total cluster loss)
51-full-dr	assert_ok	C-disaster2 no broker container survives
51-full-dr	assert_ok	C-disaster3 all six named volumes are gone (the disaster is real, not just stopped containers)
51-full-dr	assert_ok	C-disaster4 agt1's container is STILL RUNNING (runbook §5.2 step 4 is about it reconnecting)
51-full-dr	assert_ok	C-disaster5 the data plane is really dead after total loss (no sentinel — brk1 unresolvable/refused)
51-full-dr	assert_ok	C-vault-oracle THE BUNDLE SURVIVED OFF-CLUSTER: byte-identical sha256 after the disaster that destroyed every volume
51-full-dr	assert_ok	C-vault-oracle2 and it still parses as a 3-node leader bundle
51-full-dr	assert_ok	D-freshbox1 bring up a fresh brk1 (real install.sh path)
51-full-dr	assert_ok	D-fresh-gate1 the new box has NO tether.db
51-full-dr	assert_ok	D-fresh-gate2 the new box has NO raft state
51-full-dr	assert_ok	D-fresh-gate3 broker.yaml has NO cluster seam (install.sh:548-556 comments it out)
51-full-dr	assert_ok	D-fresh-gate4 nats.conf has NO auth_callout (install.sh:690-704's stock standalone conf)
51-full-dr	assert_ok	D-freshbox2 restore brk1's ORIGINAL secrets from the operator key vault [operator per runbook §5.2 step 1]
51-full-dr	assert_ok	D-freshbox3 the restored tunnel CERT is byte-identical to the pre-disaster one
51-full-dr	assert_ok	D-freshbox4 the restored tunnel KEY is byte-identical too (init.go:312-336 reads both)
51-full-dr	assert_ok	D-freshbox5 enable nats-server on the fresh box
51-full-dr	assert_ok	D-freshbox6 push the vaulted bundle back onto the box [operator per runbook §5.2 step 2]
51-full-dr	assert_ok	E-remint-prep mint a NEW node identity (new tunnel cert = new fingerprint)
51-full-dr	assert_ok	E-remint-prep2 stage the foreign secrets on the box
51-full-dr	assert_refuses	E-remint-neg RE-MINTED secrets are REFUSED: the tunnel-cert fingerprint is restore's un-forgeable provenance anchor (so D-freshbox2 restoring the ORIGINAL material is a RESTORE, not a shortcut)
51-full-dr	assert_ok	E-remint-neg2 the refusal wrote nothing (the gate is before any disk write, restore.go:104-107 << :112)
51-full-dr	assert_refuses	F-a a malformed --raft-addr is rejected (needs the pty: the confirm runs BEFORE validation)
51-full-dr	assert_ok	F-b1 the DR restore succeeds [operator per runbook §5.2 step 2] — rc=0 means the seam WAS written (restore fails CLOSED if it cannot)
51-full-dr	assert_ok	F-b2 it pruned BOTH dead peers and reset the cursor
51-full-dr	assert_ok	F-b3 'prior DB preserved at: (none — no prior DB on this host)' — the fresh-box-exclusive discriminator
51-full-dr	assert_ok	F-b4 the roster is now EXACTLY {brk1} (#49-hardening: a resurrected pruned peer would be a restore-side defect)
51-full-dr	assert_ok	F-b5 the applied cursor was reset to 0
51-full-dr	assert_ok	F-b6 no restore_in_progress / force_single_active markers linger
51-full-dr	assert_ok	F-b7 (#51 FIXED) restore --config APPLIED the broker.cluster seam itself — the old drill had to HAND-WRITE it because restore had no --config flag at all (cluster_backup.go pre-R10)
51-full-dr	assert_ok	F-b8 (H2-flip) the seam on disk carries ALL FIVE fields (data_dir/raft_addr/secrets_dir/nats_conf_path/nats_server_bin) and NOT the old hand-write's invalid \`nats_route\` key — serve keys cluster mode on data_dir, so a partial seam re-lands the #51 boot FATAL
51-full-dr	assert_ok	F-b9 (R10 P4) restore printed the ORDERED next-step list with a ✓ seam confirmation, not the single 'NEXT: start tether-broker' line #52/#64 were made of
51-full-dr	assert_ok	F-c1 a SECOND restore with --raft-addr 127.0.0.1:7400 succeeds (address override on a box that now has a DB; --config \"\" opts out of a seam re-write that would mismatch F-b's)
51-full-dr	assert_ok	F-c2 it preserved the prior DB to tether.db.pre-restore.bak (unlike F-b's fresh box, there now IS a prior DB — 'preserved at' is honest)
51-full-dr	assert_ok	F-c3 the .pre-restore.bak file really exists on disk
51-full-dr	assert_ok	F-c4 the self node's raft_addr was really re-stamped to 127.0.0.1:7400 (restore.go:308 sqlite readback — not just the CLI's word)
51-full-dr	assert_ok	F-d1 a THIRD restore (back to --raft-addr brk1:7400) succeeds
51-full-dr	assert_ok	F-d2 it LADDERED the prior-DB backup to .pre-restore.1.bak (O_EXCL → never overwrites .pre-restore.bak)
51-full-dr	assert_ok	F-d3 BOTH .pre-restore.bak AND .pre-restore.1.bak exist — the ladder, not an overwrite
51-full-dr	assert_ok	F-d4 the raft_addr is back to brk1:7400 (the override is not sticky across restores) — leaving the DB in the same effective state F-b produced, matching the seam on disk, so the #51 tail below is unaffected
51-full-dr	assert_ok	G-own1 the ROOT restore left root-owned data (the #6 hazard P2 forces: root was REQUIRED to write the root-owned seam, and sudo -u tether fails CLOSED on it — measured)
51-full-dr	assert_ok	G-own2 [GAP #6-chown] apply the documented broker-ops #6 remedy so the daemon can read its data dir
51-full-dr	assert_setup	G-nats0 restore printed a runnable reconcile command to copy verbatim [operator per runbook §5.2 step 3]
51-full-dr	assert_ok	G-nats1 (#52) run restore's OWN printed reconcile step to render the standalone auth_callout conf — no drill-authored render
51-full-dr	assert_ok	G-nats2 restart nats-server to load the rendered conf (runbook §5.2 step 3)
51-full-dr	assert_ok	G-start reset-failed + start tether-broker
51-full-dr	not_covered	51 DR-completion (G3/H/J/I): the documented DR procedure did not compose into a live broker on the real stack
51-full-dr	assert_ok	G3a the broker is finally ready — the DR completed by following runbook §5.2 with product verbs (restore --config seam, printed reconcile render) + the one #6 chown, NOT a drill-authored seam
51-full-dr	assert_ok	G3b exactly one voter
51-full-dr	assert_ok	G3c status is DEGRADED/NOT-HA (runbook §5.1 step 3's own words) and NOT force_single
51-full-dr	not_covered	51 H1a offline-window (transient offline state not observable in-sim — a drill coverage hole, not a re-run valve)
51-full-dr	assert_ok	H1b agt1 comes back ONLINE with ZERO operator action on the agent
51-full-dr	assert_ok	H2 THE DR TERMINUS: the pre-disaster expose serves the EXACT original sentinel again on the same public port $P (never 'ps'/'expose explain'/'cluster status')
51-full-dr	assert_ok	J1 (#53-silence CLOSED, restore end) restore WARNED that history/audit did NOT come back and printed the runnable \`nats stream restore\` inverse — paired with B-vault1c (backup end), NEITHER end is silent
51-full-dr	not_covered	51 #53-scope reader liveness
51-full-dr	not_covered	51 #53-scope (history/audit NOT recoverable from a state.db-only bundle after total loss)
51-full-dr	assert_ok	I-prep bring up a second broker container
51-full-dr	assert_ok	I2 the data plane STILL serves the original sentinel after the re-grow
51-full-dr	product_red	[#GROW-ONTO-RECOVERED] re-growing after a DR restore is blocked — the DR spine completed at N=1 (survivor de-clustered to standalone, NOT force_single, JS healthy per G3c above) yet 'cluster add brk2' cannot complete: the grow cutover re-clusters the lone survivor and the 1→2 clustered-JS meta never forms for the joiner, stalling the join op. This is the grow-onto-RECOVERED-broker family — sibling of 42's #GROW-ONTO-FORCE-SINGLE (grow onto a force-single survivor) and the pc732 grow-onto-resnapshot'd-broker incident (memory project_cluster_ha_realmachine_test). Runbook §5.2 step 3b cannot be finished; disclosed as a deep HA-path defect owed a dedicated batch (R15 did NOT rush a fix in the clustered-JS meta path). NOTE: the SEPARATE #31/#45 mid-grow-bundle residue (a leaked grow lock / stalled retire op carried into the restored origin when the bundle was captured mid-membership-op) IS closed by the R15 restore-side fix (normalizeRestoreStaging now clears grow/upgrade markers+leases+non-terminal ops; hermetic verifier TestRestoreClearsStaleGrowUpgradeAndOpResidue) — but THIS drill's bundle is from a healthy N=3, so that residue path is not what blocks here. Captured: $(printf '%s' 
51-full-dr	not_covered	51 re-grow after DR (arm I)
52-credential-rotation	assert_setup	grow_to_2 (N=2 VOTER + JS meta FORMED at size 2 + leader pinned brk1)
52-credential-rotation	assert_setup	session $SID + ctl login
52-credential-rotation	assert_setup	agent-join agt1
52-credential-rotation	assert_setup	provision agt1 agent.yaml (tunnel_addr, S0-tunnel)
52-credential-rotation	assert_setup	expose agt1:8092 --on-broker brk1 --name rot
52-credential-rotation	assert_setup	FG-guard 2: pre-rotation data plane serves the exact sentinel
52-credential-rotation	assert_setup	FG-guard 3a: the ctl identity connects via brk1 (B5d's control baseline)
52-credential-rotation	assert_setup	FG-guard 3b: the SAME identity connects via brk2 (B5d's control baseline)
52-credential-rotation	assert_refuses	A1 rotate-tunnel-cert without --cert-fp is refused
52-credential-rotation	assert_ok	A3 a wrong --cert-fp is refused AND the fp tether reports for the on-disk cert equals what secrets_tunnel_fp computes (harness self-check: a mismatch here would invalidate every later fp oracle)
52-credential-rotation	assert_ok	A4a #56 FIXED: on the FOLLOWER, rotating SELF gives self-only guidance ('this IS the target broker, but it is a FOLLOWER — transfer leadership to it, then re-run here'), and NEVER the OLD wrong 'run on the leader host' (which sent the operator to the leader to rotate a follower — still fails, because the TARGET must BE the leader)
52-credential-rotation	assert_ok	A4b #56 FIXED: on the LEADER for a REMOTE target, the guidance points to running it ON the target ('must run ON the target broker brk2 … Re-run it on brk2'), NOT a circular 'transfer leadership' loop and NOT 'run on the leader host'
52-credential-rotation	assert_refuses	A4c control: a GENERIC mutating verb (cluster drain) on the follower STILL says 're-run on the leader host' — proving the self-only bypass A4a/A4b assert is rotate-tunnel-cert-specific, not a global redirect removal
52-credential-rotation	assert_ok	A5a mint a NEW tunnel leaf for brk1 (operator PKI — [by design: rejection #2])
52-credential-rotation	assert_ok	A5b the new leaf really is a different identity
52-credential-rotation	assert_ok	A5c push the new leaf onto brk1 (0600, tether-owned — SecretsPreflight hard-refuses 0077 bits)
52-credential-rotation	assert_ok	A5d push the matching key
52-credential-rotation	assert_ok	A5 rotate-tunnel-cert commits and hot-swaps the LIVE certificate
52-credential-rotation	assert_ok	A6 the rotation WINDOW is observable: brk1's node carries the new pin, the previous pin, and an expiry (probed, not a guessed path)
52-credential-rotation	assert_ok	A7a inject: blackhole brk1's tunnel LISTENING port 7000 (silent DROP on brk1) — long enough to outlast the 30s yamux keepalive so agt1's OLD session actually dies (the only way to force a genuine redial); only 7000 is cut, so NATS/route/raft stay up and the cluster is otherwise healthy
52-credential-rotation	assert_ok	A7b PROOF the block took: agt1's connect to brk1:7000 HANGS (rc=124, not a refusal) — impossible unless the tunnel port is dropped, so it is the down-edge's proven fault source
52-credential-rotation	assert_ok	A7c heal the partition
52-credential-rotation	assert_ok	A7d GENUINE re-pin (KEPT invariant, #63 REFUTED): after the OLD session provably DIED (down-edge past the 30s keepalive), a FRESH yamux redial re-serves the exact sentinel against brk1's ROTATED cert (up-edge) — NOT surviving-session traffic. A failure here is a re-pin REGRESSION, not a known defect
52-credential-rotation	assert_ok	A7c heal the partition (down-edge not observed)
52-credential-rotation	not_covered	52 A7 re-pin (no session drop)
52-credential-rotation	assert_ok	A8-pre snapshot brk2's currently-pinned tunnel leaf so recovery can restore the exact old generation (not the new unpinned brick)
52-credential-rotation	assert_ok	A8a mint a new tunnel leaf for brk2 and land it WITHOUT rotating the pin
52-credential-rotation	assert_ok	A8b restart brk2's broker (it must now refuse to start: the on-disk cert matches neither pin)
52-credential-rotation	assert_ok	A8c brk2's broker does NOT reach active (fail-closed, as designed)
52-credential-rotation	assert_ok	A8d the broker's BOOT stream (journald — main()'s exit error, printed before any logger exists) carries the EXACT pin-mismatch refusal (not merely 'the unit failed' — that would eat any crash as green)
52-credential-rotation	assert_ok	A8e DOC-23 FIXED: the pin-mismatch refusal in the broker's BOOT stream now guides FILE-level recovery ('FILE-level restore', 'put the PREVIOUS tunnel-cert.pem + tunnel-key.pem back', 'restart the broker') and NEVER points at the unreachable 'rotate-tunnel-cert' command it used to dead-end the operator with
52-credential-rotation	assert_ok	A8f restore brk2's SAVED OLD leaf (pre-A8 snapshot, fp==original pin — NOT the new unpinned leaf) and bring it back; the fail-closed broker reaching active is itself proof the restored cert matches the pin (reset-failed first: StartLimitBurst=5/10s is deliberately NOT disabled)
52-credential-rotation	assert_ok	A8g N=2 VOTER + JS meta back at size 2 before arm group B (proves the cluster fully re-converged on the recovered old pin, not just that the unit started)
52-credential-rotation	assert_ok	C1a keygen mints a key on real disk at 0600
52-credential-rotation	assert_ok	C1b the public key tether prints matches what the independent \'nk\' tool derives from the seed (never verify tether with tether)
52-credential-rotation	assert_ok	C2a keygen is Hidden: it does not appear in \'cluster --help\'
52-credential-rotation	assert_ok	C2b but it is runnable and self-documents
52-credential-rotation	assert_ok	C3 without --out, keygen writes nothing to disk
52-credential-rotation	assert_ok	B1 baseline: both brokers' nats.conf carry the SAME account issuer
52-credential-rotation	assert_ok	B0 CONTROL: the reconcilers are ALIVE — 'reconcile nats --all --wait' blocks on every broker's reconciler reporting convergence, so exit 0 proves none is dead (a dead one would hang the wait). This is what makes B2's 'issuer unchanged' mean 'alive reconciler chose not to act', not 'reconciler never ran'
52-credential-rotation	assert_ok	B2a mint generation-2 trust material (new account.nk + new CA), leaving _shared/gen1 untouched
52-credential-rotation	assert_ok	B2b swap ONLY the account.nk (the auth_callout issuer's source) on BOTH brokers — NOT the route leaf or CA (that would break the route mesh and mask the skew behind an UNREACHABLE; the account.nk skew alone constructs the issuer skew while the route mesh stays up)
52-credential-rotation	assert_refuses	B2 #54 facet 1 FIXED: with account.nk swapped-but-not-re-rendered, 'reconcile nats --all --wait' now FAILS-CLOSED naming the auth_callout issuer skew (was a FALSE all-clear: 'converged' + exit 0). The runbook's VERIFY command no longer certifies an un-adopted issuer as converged
52-credential-rotation	assert_ok	B2c the fail-closed reconcile did NOT mutate the conf (VERIFY-only, not a fixer): both brokers' nats.conf md5 are unchanged and brk1's issuer is still OLD
52-credential-rotation	assert_ok	B3 #54 facet 2 FIXED: 'cluster doctor --offline --json' on the swapped-but-not-re-rendered brk1 now FATALs auth-issuer-skew and EXITS non-zero (was blind: 0 fatal). The JSON carries a FATAL auth-issuer-skew check and summary.fatal>=1 — a RUNNING cluster finally has a verb that surfaces the skew
52-credential-rotation	assert_setup	55-restart: restart ONLY brk1's broker so its reconciler adopts the gen-2 issuer (brk2 left un-rolled — this IS the #55 partial-rotation window)
52-credential-rotation	assert_ok	55a brk1 CONVERGES: its in-broker reconciler re-renders nats.conf to the gen-2 issuer within ~20s (proves R6's constructible-skew mechanism — a plain restart with a new seed adopts it, no membership change needed)
52-credential-rotation	assert_ok	55b NON-VACUITY control: with brk1 converged (on-disk gen-2 == rendered gen-2), doctor --offline on brk1 no longer flags auth-issuer-skew — the skew FATAL is NOT a blanket always-on, it clears on the broker that actually adopted the new issuer
52-credential-rotation	assert_ok	55c #55 CONSTRUCTED + VISIBLE: the UN-rolled brk2 (on-disk gen-2 vs its OLD-issuer conf) is now caught — doctor --offline on brk2 FATALs auth-issuer-skew and exits non-zero. Before R11 this partial-rotation divergence was SILENT (the 'not constructible' premise #55 was parked on); the danger an operator would otherwise miss is now detectable
52-credential-rotation	not_covered	52 D-group (C7 guided rotation)
52-credential-rotation	assert_ok	D0 CLEAN BASELINE: no manual:credrot:* alert exists before we retire anything (a transient one from setup would make D6 pass for the wrong reason)
52-credential-rotation	assert_refuses	D1 --require-credential-rotation without --compromised is a usage error
52-credential-rotation	assert_refuses	D3 --yes on retire is an UNKNOWN FLAG (not a Tier-2 refusal like restore's): retire registers no yes-rejector — the #36 divergence, not a new number
52-credential-rotation	assert_ok	D2a the WARNING precedes the confirm (the operator is told retire != revocation BEFORE being asked)
52-credential-rotation	assert_ok	D2b non-interactive is refused at the real TTY gate (the branch hermetic tests structurally cannot reach)
52-credential-rotation	assert_ok	D2c the refusal had ZERO side effects: no NEW removal (retire/drain) op was opened for brk2 and no credrot alert was raised (brk2's healthy VOTER membership row — kind=add/state=done — is NOT a side effect)
52-credential-rotation	product_red	#31 the leaked \'cluster add\' grow lock BLOCKS the C7 guided rotation spine — and it does so AFTER the operator has already been made to type the node id (cluster_retire.go:50's callAdmin runs after :47's typed confirm), which is a new facet of #31's blast radius. Captured: $(printf '%s' 
52-credential-rotation	not_covered	52 D4-D8: the C7 guided-rotation alert lifecycle (guide + severe manual:credrot alert raise -> read from ctl -> clear)
52-credential-rotation	assert_ok	D4a the retire op was created
52-credential-rotation	assert_ok	D4b the guided rotation checklist was printed
52-credential-rotation	assert_ok	D4c the guide states the alert was RAISED (the single discriminator — the checklist body itself is hermetically covered)
52-credential-rotation	assert_ok	D5 the NOT-SAFE banner is printed and contains no 'done/safe/complete' wording (rejection #5's floor)
52-credential-rotation	assert_ok	D6 THE SIGNATURE ARM: ctl reads the severe manual:credrot:brk2 alert over REAL NATS (hermetic only ever proved the raise request's SHAPE against a stub socket — never that it persists and is readable)
52-credential-rotation	assert_ok	D7 with a severe alert live, stdout stays parseable (the banner goes to stderr)
52-credential-rotation	assert_ok	D8a alert clear removes it (operator-only admin-socket verb — run ON brk1 via the broker exec channel, not ctl)
52-credential-rotation	assert_ok	D8b it is really gone
52-credential-rotation	assert_ok	D8c clear is idempotent (same broker admin-socket channel as D8a)
60-user-journey	assert_ok	up 1 broker + 2 agents + 1 ctl
60-user-journey	assert_ok	init brk1 (standalone → N=1 cluster)
60-user-journey	assert_ok	N=1 leader floor (leader=brk1)
60-user-journey	assert_ok	session $SID + ctl login
60-user-journey	assert_ok	agent-join agt1
60-user-journey	assert_ok	agent-join agt2
60-user-journey	assert_ok	both agents ONLINE (2-agent baseline)
60-user-journey	assert_ok	J1 ctx shows the active session ($SID)
60-user-journey	assert_ok	J-G.3a-1 logout clears current_session
60-user-journey	assert_refuses	J-G.3a-2 node ls REFUSES with no active session (negative control)
60-user-journey	assert_ok	J-G.3b stop agt2 daemon (state change during the logout window)
60-user-journey	assert_ok	J-G.3b2 broker admin view shows agt2 STALE/OFFLINE DURING the logout window (authoritative, no ctl session)
60-user-journey	assert_ok	J-G.3c-1 re-login (fresh CONNECT + auth_callout re-auth)
60-user-journey	assert_ok	J-G.3c-2 FIRST post-login node ls -a already reflects agt2 gone (reconnected read = current state; single check, no poll)
60-user-journey	assert_ok	J-G.3c-3 default node ls (ONLINE-only) drops agt2, keeps agt1
60-user-journey	assert_ok	J-G.3c-4 default node ls no longer lists agt2
60-user-journey	assert_ok	J2 node ls columns: agt1 KIND, ONLINE, PROTO int, RELEASE non-empty
60-user-journey	assert_ok	J3 exec exit-0 passthrough
60-user-journey	assert_ok	J4 exec non-zero exit passthrough (EXACT 7, not just !=0)
60-user-journey	assert_ok	J5 exec stdout multi-chunk stream (256 KiB, ordered HEAD..TAILxyz)
60-user-journey	assert_ok	J6 exec --cwd /tmp (flag BEFORE node; SetInterspersed)
60-user-journey	assert_ok	J7 exec signal-kill → FLAT 128 (not 128+signo)
60-user-journey	assert_ok	J7b exec signal-kill stderr note 'terminated by signal'
60-user-journey	assert_ok	J8 run real PTY size 40x132 (cross-container winsize)
60-user-journey	assert_ok	J9 run interactive round-trip (shell-computed marker ROK)
60-user-journey	assert_ok	J10 run resize propagates (24x80 → remote stty size)
60-user-journey	assert_ok	J11-base foreground sleep RUNNING on agt1 (pre-injection baseline)
60-user-journey	assert_ok	J11 Ctrl-C interrupted the foreground sleep (prompt returned ≤8s)
60-user-journey	assert_ok	J11b sleep pgroup gone after Ctrl-C (poll async teardown)
60-user-journey	assert_ok	J13a ps shows the sleep RUNNING (default view)
60-user-journey	assert_ok	J13b ps -a shows the sleep EXITED (transition; -a gates it)
60-user-journey	assert_ok	J14 ps PORTS section: exact 6-col header (no HOME) + empty (none) body
60-user-journey	assert_ok	J15 history -n 5 bounds output (1..5 audit rows)
60-user-journey	assert_ok	J16a history --kind proc lists ONLY PROC rows (real JS audit replay)
60-user-journey	assert_refuses	J16b history --kind bogus rejected (validator: must be one of)
60-user-journey	assert_ok	J17 history --follow smoke (bounded; a new record tails in)
60-user-journey	assert_ok	J18a version prints a semver
60-user-journey	assert_ok	J18b completion bash renders
61-transfer-edges	assert_ok	up 1 broker + 1 agent + 1 ctl
61-transfer-edges	assert_ok	init brk1 (N=1)
61-transfer-edges	assert_ok	session $SID + ctl login
61-transfer-edges	assert_ok	agent-join agt1
61-transfer-edges	assert_ok	seed ctl-local files (sim-owned): a.bin/u.bin/o.bin
61-transfer-edges	assert_ok	G0 agent_provision_yaml FAILS on a malformed yaml (guard self-test: catches a bad reprovision)
61-transfer-edges	assert_ok	A0 reprovision agt1 agent.yaml = OPEN (flagless unit, yaml-driven; ONLINE poll)
61-transfer-edges	assert_ok	A1 push OK (open, ctl→agt1 /home/sim/a.bin)
61-transfer-edges	assert_ok	A1b pushed file landed on agt1
61-transfer-edges	assert_ok	A2 pull OK (open, agt1→ctl)
61-transfer-edges	assert_ok	A2b pull round-trip sha256 matches
61-transfer-edges	assert_ok	B3-seed real symlink leaf on agt1 disk
61-transfer-edges	assert_ok	B5-seed regular dst on agt1
61-transfer-edges	assert_ok	B5b push --force succeeds (rc=0)
61-transfer-edges	assert_ok	B5c --force dst content == source SHA-256 (real overwrite, not just rc=0)
61-transfer-edges	assert_ok	B6-seed >2 GiB sparse file on agt1 (3G, sim-owned)
61-transfer-edges	assert_ok	C1 ~500 KiB → tier A (512000 < 523264 boundary; push OK, not just the banner)
61-transfer-edges	assert_ok	C1b tier-A file landed on agt1 (below-boundary transfer completed, not just tier chosen)
61-transfer-edges	assert_ok	C2 1 MiB → tier B (real max_payload/2 clamp, not the 8 MiB static path)
61-transfer-edges	assert_ok	C2b tier-B file landed on agt1 (banner prints pre-Put → anchor on the result)
61-transfer-edges	assert_ok	C2c pull o.bin back + sha matches (tier-B round-trip completed)
61-transfer-edges	assert_ok	D0 reprovision agt1 = NARROW /srv/data (helper installs the dir sim-owned pre-restart)
61-transfer-edges	assert_ok	D-F1 push INSIDE narrow root OK (positive control)
61-transfer-edges	assert_ok	D-F1b in-narrow file landed on agt1
61-transfer-edges	assert_ok	E0 reprovision agt1 = DISABLED (allow_roots: [] → modeDisabled)
61-transfer-edges	assert_ok	E3 restore agt1 = OPEN
61-transfer-edges	assert_ok	E4 push works again after restore (reversibility control)
61-transfer-edges	assert_ok	E4b pull recover.bin back to ctl
61-transfer-edges	assert_ok	E4c recover round-trip sha256 == source (real round-trip, not just test -s)
61-transfer-edges	assert_ok	F-complete: history --kind transfer has a complete row for /home/sim/a.bin
61-transfer-edges	assert_ok	F-failed: history --kind transfer has a failed row for the outside.bin refusal
62-remote-fs-safe	assert_ok	up 1 broker + 1 agent + 1 ctl
62-remote-fs-safe	assert_ok	init brk1 (N=1)
62-remote-fs-safe	assert_ok	session $SID + ctl login
62-remote-fs-safe	assert_ok	agent-join agt1
62-remote-fs-safe	assert_ok	probe: mount fuse.hangfs at $MNT (healthy)
62-remote-fs-safe	assert_ok	probe: mount fstype is fuse.hangfs (spawnsafe classifies fuse.* as a hangable network mount)
62-remote-fs-safe	assert_ok	probe: statfs healthy while the daemon runs
62-remote-fs-safe	assert_ok	Arm1 setup: reprovision agt1 = default (auto), agent boots with the hangable mount present
62-remote-fs-safe	assert_ok	Arm1 control: exec works normally under auto with a HEALTHY mount (auto is inert)
62-remote-fs-safe	assert_ok	Arm1 inject: SIGSTOP the fuse daemon → mount wedged
62-remote-fs-safe	assert_ok	Arm1 discriminator: fuse daemon is T/S-state, NOT uninterruptible-D (reapable approximation)
62-remote-fs-safe	assert_ok	Arm1 discriminator: statfs on the mount now BLOCKS (bounded probe)
62-remote-fs-safe	assert_refuses	Arm1a auto: exec --cwd wedged-mount fast-fails remote_fs_unsafe_cwd (flag BEFORE node)
62-remote-fs-safe	assert_ok	Arm1 alive-control: a NORMAL exec STILL works after the wedge (bounded probe didn't wedge the agent)
62-remote-fs-safe	assert_refuses	Arm1b auto: exec abs-argv0 on wedged mount fast-fails remote_fs_unhealthy
62-remote-fs-safe	assert_ok	Arm1 heal: SIGCONT the daemon (restore healthy mount for Arm 3)
62-remote-fs-safe	assert_ok	1S prime discriminator: mount still statfs-healthy after priming
62-remote-fs-safe	assert_ok	1S inject: SIGSTOP hangfs → the mount dies AFTER it was judged healthy
62-remote-fs-safe	assert_ok	1S discriminator: still T/S-state, not uninterruptible-D (reapable approximation)
62-remote-fs-safe	assert_ok	1S discriminator: statfs on the mount now BLOCKS
62-remote-fs-safe	assert_ok	1S-2 run: first abs-argv0 after the mount died (captures rc + output)
62-remote-fs-safe	assert_ok	1S-2 oracle A (transport): ctl got a terminal state back inside the wrapper
62-remote-fs-safe	assert_ok	1S-2 oracle B (delivery): the broker forwarded it and the agent received it
62-remote-fs-safe	assert_ok	1S-2 oracle C (product): the code is remote_fs_spawn_timeout (proves the verdict was cached-healthy going in)
62-remote-fs-safe	assert_ok	1S-3 run: second abs-argv0 after the stall (captures rc + output)
62-remote-fs-safe	assert_ok	1S-3 oracle A (transport): ctl got a terminal state back inside the wrapper
62-remote-fs-safe	assert_ok	1S-3 oracle B (delivery): the broker forwarded it and the agent received it
62-remote-fs-safe	assert_ok	1S-3 oracle C ★ (product): the code is remote_fs_unhealthy (evidence-driven re-probe happened)
62-remote-fs-safe	assert_ok	1S heal (re-arm the scenario for the --safe half)
62-remote-fs-safe	assert_ok	1S re-prime discriminator: the verdict really is HEALTHY again (a dead one would refuse --cwd)
62-remote-fs-safe	assert_ok	1S re-wedge
62-remote-fs-safe	assert_refuses	1S --safe discards cached verdicts: FIRST --safe call fast-fails remote_fs_unhealthy
62-remote-fs-safe	assert_ok	1S alive-control: a normal exec still works (the abandoned execve did not wedge the agent)
62-remote-fs-safe	assert_ok	1S heal: SIGCONT the daemon (hand a healthy mount to Arm 3)
62-remote-fs-safe	assert_ok	Arm3 setup: reprovision agt1 = remote_fs.mode off (agent reboots with healthy mount)
62-remote-fs-safe	assert_ok	Arm3 inject: SIGSTOP the fuse daemon → mount wedged again
62-remote-fs-safe	assert_ok	Arm3 discriminator: still T/S-state (reapable approximation)
62-remote-fs-safe	assert_refuses	Arm3 off+--safe: exec --safe --cwd wedged-mount fast-fails remote_fs_unsafe_cwd (escalation, flags BEFORE node)
62-remote-fs-safe	assert_ok	Arm3 alive-control: normal exec still works (agent not wedged)
62-remote-fs-safe	assert_ok	Arm3 heal: SIGCONT the daemon
62-remote-fs-safe	not_covered	62 Arm 2 (measured): true uninterruptible-D + mode:off-without-safe legacy hang
62-remote-fs-safe	assert_ok	Arm2 true-D + mode:off-without-safe NOT-COVERED (live: wedge was SIGCONT-reversible = reapable approx, not true-D)
67-transient-js-refusal	assert_ok	up 2 brokers + 1 agent + 1 ctl
67-transient-js-refusal	assert_ok	init brk1 (N=1)
67-transient-js-refusal	assert_ok	grow brk2 (N=2 clustered JS)
67-transient-js-refusal	assert_ok	session + ctl login
67-transient-js-refusal	assert_ok	agent-join agt1
67-transient-js-refusal	assert_ok	JS meta FORMED at N=2 (cluster_size==2)
67-transient-js-refusal	assert_ok	a 12 MB payload exists on ctl1 (> the 8 MiB tier-A ceiling => tier B is forced)
67-transient-js-refusal	not_covered	#67 sub-face 4 (grow reports success before the JS meta can place assets)
67-transient-js-refusal	product_red	#67 STILL reproduces with NO INJECTION AT ALL (the G67 fix did not close this case) — the FIRST tier-B push after a plain \`cluster add\` grow hard-failed on a HEALTHY N=2: [$(printf '%s' 
67-transient-js-refusal	assert_ok	CONTROL(before) ESTABLISHED: tier-B provably works on this healthy N=2 — first attempt, or a plain retry after a #67 refusal (that retry is recorded as a product_red above, never silently absorbed). Without this, a refusal below proves nothing
67-transient-js-refusal	assert_ok	G69 (#67 sub-face 4): the grow reached terminal SERVING having PROVEN JetStream placement — no 'WITHOUT proving' degrade entry in any op timeline
67-transient-js-refusal	not_covered	G69 positive oracle (#67 sub-face 4)
67-transient-js-refusal	assert_ok	INJECT: stop brk2's nats-server cleanly (JS quorum is lost; brk1 stays reachable; nothing is destroyed)
67-transient-js-refusal	assert_ok	RESTORE: start brk2's nats-server again
67-transient-js-refusal	assert_ok	the 2-node JS meta re-forms once the peer is back
67-transient-js-refusal	assert_ok	CONTROL(after): the SAME tier-B push SUCCEEDS once the peer is back, with NO operator action — this is what makes the refusal above provably transient (a real capability absence would persist)
67-transient-js-refusal	product_red	#84 (post-recovery face) the FIRST tier-B push after the JS meta re-formed sat ${_G67_AFTER_FIRST_S}s — the whole --timeout — on the Put leg (first attempt said: '${_g67_first_face:-?}'), and the next attempt succeeded at once: the stall is not bounded by the ctl's watchdog (≈30 s) nor by the cluster's recovery, whatever the refusal is worded as
67-transient-js-refusal	product_red	#84 (post-recovery face, slow success) the FIRST tier-B push after the JS meta re-formed SUCCEEDED but took ${_G67_AFTER_FIRST_S}s (said: '$(printf '%s' 
67-transient-js-refusal	not_covered	#67 regression (transient JS-quorum loss must be reported honestly)
67-transient-js-refusal	not_covered	#67 regression
67-transient-js-refusal	product_red	#84 tier-B Put on a JS that has lost quorum sat ${_G67_INJ_S}s (rc=$_G67_RC, said: '$(printf '%s' 
67-transient-js-refusal	product_red	#84 tier-B Put on a JS that has lost quorum sits the FULL size budget (or fails instantly with no responders) and reports a bare '$_g67_put_face' — no transient code, no retry hint, no bound the operator can plan around: #67's operator-facing defect, moved from the prepare leg to the Put leg by 0b204b5 (prepare now resolves the bucket locally and succeeds without a JS round-trip, so the G67 refusal wording (a)–(d) is unreachable from this injection)
67-transient-js-refusal	not_covered	#67 regression
67-transient-js-refusal	assert_ok	#67 the refusal carries the TRANSIENT code jetstream_not_ready (pre-G67 this was the permanent bucket_create_failed)
67-transient-js-refusal	assert_ok	#67 the refusal TELLS THE OPERATOR it is transient and worth retrying
67-transient-js-refusal	assert_ok	#67 the refusal does NOT assert a permanent capability absence, and does NOT offer the unrelated max_payload advice
67-transient-js-refusal	not_covered	#67 non-vacuity tooth
67-transient-js-refusal	not_covered	#67 non-vacuity tooth
67-transient-js-refusal	not_covered	#67 face B (swallowed caps-probe error -> invented permanent capability claim)
67-transient-js-refusal	assert_ok	the recovery-run file really landed on agt1 (round-trip, not a queued no-op)
70-expose-journey	assert_ok	up 1 broker + 1 agent + 1 ctl
70-expose-journey	assert_ok	init brk1 (N=1 cluster)
70-expose-journey	assert_ok	session lab + ctl login (owner)
70-expose-journey	assert_ok	agent-join agt1
70-expose-journey	assert_ok	agent_provision_yaml agt1 (agent.yaml tunnel_addr → expose data plane live; S0-隧道)
70-expose-journey	assert_ok	J1a expose agt1 --local 8080 --name web
70-expose-journey	assert_ok	J1b [DATA-PLANE IRON PROOF] curl brk1:$P returns the EXACT sentinel over the reverse tunnel
70-expose-journey	assert_ok	J2 ps -a PORTS: web row w/ PUBLIC :$P + HOME brk1 (N=1 cluster home — pinned to live, not absent)
70-expose-journey	assert_ok	J2json ps -a --json .ports[web]: port==$P AND home_broker==brk1
70-expose-journey	assert_ok	J3 explain web: rebuild on + B5 'not yet recorded' footer present (values deferred, not asserted)
70-expose-journey	assert_ok	J3json explain --json: rebuild==true ∧ moved==false ∧ home_broker==brk1
70-expose-journey	assert_ok	J4a --remote-port 14500 (in-band, free)
70-expose-journey	assert_ok	J4b curl brk1:14500 returns the sentinel (explicit port carries real bytes)
70-expose-journey	assert_refuses	J5 port_taken (--remote-port on an already-allocated port)
70-expose-journey	assert_refuses	J6 port_out_of_band (--remote-port 15500 outside 14000-14999)
70-expose-journey	assert_refuses	J7 name_taken (duplicate --name web)
70-expose-journey	assert_ok	J8 --on-broker brk1 (self = eligible VOTER in N=1 cluster) SUCCEEDS — pinned to live truth
70-expose-journey	assert_ok	J9-baseline curl web:$P alive BEFORE rm (prove the data plane is live first)
70-expose-journey	assert_ok	J9a expose rm agt1 --name web
70-expose-journey	assert_ok	J9b [SEMANTIC] curl brk1:$P now connection-REFUSED (exit 7 — NOT !curl-sf; R1 half-open guard)
70-expose-journey	assert_ok	J9c port $P immediately REUSABLE (--remote-port $P re-expose)
70-expose-journey	assert_ok	J9d curl brk1:$P = sentinel after reuse (real bytes, not expose exit-0 — bound≠forwarding)
70-expose-journey	assert_ok	J10-baseline curl web-reuse:$RP alive + record port
70-expose-journey	assert_ok	J10a systemctl STOP tether-agent (precise: agent unit, NOT broker)
70-expose-journey	assert_ok	J10b [SEMANTIC] curl brk1:$RP REFUSED while agent down (proves the tunnel is agent-driven)
70-expose-journey	assert_ok	J10c systemctl START tether-agent
70-expose-journey	assert_ok	J10d [P6] curl brk1:$RP = SAME sentinel after reconnect (state.json token; port $RP unchanged)
70-expose-journey	assert_ok	J10e explain moved==false ∧ public_port==$RP (reconnected SAME expose, not re-created; epoch unobservable)
71-expose-rehome-failover	assert_ok	grow_to_3 + 1 agent + ctl (N=3 HA cluster; GROW-ATTEMPTS logged above)
71-expose-rehome-failover	assert_ok	session lab + ctl login (owner)
71-expose-rehome-failover	assert_ok	agent-join agt1 (tunnel broker = brk3, a non-leader)
71-expose-rehome-failover	assert_ok	agent_provision_yaml agt1 (tunnel_addr=brk3)
71-expose-rehome-failover	assert_refuses	A --on-broker <bogus> → on_broker_unknown
71-expose-rehome-failover	assert_ok	A2 no 'web-bogus' row after refusal (not just an error string)
71-expose-rehome-failover	assert_ok	GATE brk3 is a LIVE NON-leader (crash target valid: killing it keeps the leader $LDR0 alive as admin)
71-expose-rehome-failover	assert_ok	FIXTURE agt→brk3 + a brk3-homed expose ESTABLISHED (required to test the locked crash-strand core) — RED if the intermittent agent-tunnel-to-non-leader gap prevented it (R6-M2: not a silent GREEN NOT-COVERED)
71-expose-rehome-failover	assert_ok	FIXTURE rebuild-ON wstrand ($PC) + rebuild-OFF wnr ($PD) both serve on brk3 (agt tunnel established; home data plane live pre-crash)
71-expose-rehome-failover	assert_ok	D-rebuild-off wnr explain shows rebuild:false (--no-rebuild PERSISTED — expose.go:217 rebuild = !RebuildOff)
71-expose-rehome-failover	assert_ok	C-kill node_kill brk3 (CRASH the home broker; quorum kept: brk1 leader + brk2)
71-expose-rehome-failover	assert_ok	C-detect leader $LDR0 marks brk3 DOWN/unreachable (readable proof the crash was SEEN)
71-expose-rehome-failover	assert_ok	C-strand [#29] rebuild-ON wstrand is dead on EVERY live voter's :$PC (curl exit 7 cluster-wide — NOT auto-rehomed to any survivor; F5)
71-expose-rehome-failover	assert_ok	D-strand [#29] rebuild-OFF wnr is dead on EVERY live voter's :$PD (curl exit 7 cluster-wide — rebuild-OFF goes DOWN with its crashed home, IDENTICAL to rebuild-ON on a crash — the rebuild distinction is DRAIN-only)
71-expose-rehome-failover	assert_ok	C-strand-epoch wstrand epoch UNCHANGED ($EC0) + not moved — confirms NO rehome (a rehome would ++ epoch)
71-expose-rehome-failover	assert_ok	D-strand-explain wnr explain is HONEST post-crash: still rebuild:false + NOT moved (no home_reassign) — rebuild-OFF never rehomes
71-expose-rehome-failover	assert_ok	C-return node_start brk3 (the crashed home RETURNS)
71-expose-rehome-failover	assert_ok	C-recover rebuild-ON wstrand recovers on the SAME brk3:$PC (agt1 re-tunnels to its returned home; SAME port)
71-expose-rehome-failover	assert_ok	C-recover-epoch wstrand epoch STILL UNCHANGED ($EC0) after return — recovery was a home-RETURN, not a rehome
71-expose-rehome-failover	assert_ok	D-recover rebuild-OFF wnr recovers on the SAME brk3:$PD after return (same-home recovery, like rebuild-ON — both strand-then-return on a crash)
71-expose-rehome-failover	assert_refuses	E [rebuild-OFF DRAIN REFUSAL] cluster drain brk3 (wnr rebuild-off + wstrand both live) MUST refuse with rc≠0 + the EXACT rebuild-OFF signature 'will NOT be auto-migrated' (clusterdrain.go:665, the locked control) — fail-closed exact oracle (R8-M3); RED if #31 intercepts first (exact refusal unreachable) or if it does not refuse
71-expose-rehome-failover	not_covered	71 Arm E THIS RUN
71-expose-rehome-failover	assert_ok	B-cmd [R8 rc] cluster drain brk3 returned rc=0 — under R8 that is a STRONG claim, not a bare exit: DrainNode only reaches its rc=0 tail once awaitHomeConvergence has seen every migrated expose's agent ACK the new home epoch (home_convergence.go:122-142). The pre-R8 rc=0 (raft written, nobody told) is the exact lie this arm exists to catch
71-expose-rehome-failover	assert_ok	B-cmd [R8 rc] cluster drain brk3 returned the DOCUMENTED rc=75 dataplane_not_converged with its authored signature — CORRECT behaviour, NOT a refusal and NOT a transport failure: the rehome IS committed and the broker keeps re-delivering, so the CLI declines to report success the data plane has not earned yet. Pre-R9-D this drill classified the product's own P1 fix as 'a wrong refusal' and went RED on it
71-expose-rehome-failover	assert_ok	B-migrate [P1 TERMINAL STATE — the core of this drill] wstrand is homed on a SURVIVOR voter AND that broker's public port returns the exact sentinel over the real tunnel, within 240s of the drain. This is the END state, never an intermediate 'a home directive was published' artifact — a directive nobody applied IS the P1 bug. RED here means the control plane moved the row and the data plane never followed; the terminal explain + old/new-home curl forensics logged above say WHICH of the two it was (H9)
71-expose-rehome-failover	assert_ok	B-noreexec agt1's tether-agent MainPID is UNCHANGED across the drain (was [${AGT_PID0:-?}], now [${AGT_PID1:-?}]) — the rehome was NOT carried by an agent restart, whose boot register would have delivered the new home the pre-R8 way
71-expose-rehome-failover	assert_ok	B-silent [P1 PREMISE] the migration above was delivered while agt1 stayed SILENT: its own journal for the drain window contains >=1 ACTIVE home-delivery line (home directives pushed / home applied-ack) and ZERO re-register/rebuild lines. Without this, one incidental NATS reconnect would let B-migrate pass in BOTH the fixed and the unfixed world — the reconnect, not the R8 push, would have carried the directive
71-expose-rehome-failover	assert_ok	B-event [#30] anchor: the operator reader (admin events) reads a KNOWN-fired non-rehome event (agent_registered sid=lab/nid=agt1) — proves the reader WORKS in this drill before pinning the rehome kind (non-vacuity)
71-expose-rehome-failover	assert_ok	B-event [#30] admin events reads the drain's expose_rehomed for wstrand OFF brk3 (from_broker=brk3, name=wstrand, sid=lab) — the migration B-migrate just proved on the data plane ALSO surfaces as an operator-readable rehome event; #30 added this reader (was NOT-COVERED: raw sys.events had no operator reader)
71-expose-rehome-failover	assert_ok	B-event [#30] the expose_rehomed payload is SECRET-FREE (body keys ⊆ {v,type,ts,port,name,sid,from_broker,to_broker}; the reader relays only topology fields — never a token/psk)
71-expose-rehome-failover	not_covered	71 B-silent [P1 premise] THIS RUN
71-expose-rehome-failover	assert_ok	B-rerun [R8 remedy] the re-run the rc=75 message tells the operator to do reaches rc=0 (the documented loop TERMINATES; it does not tell operators to retry something that can never succeed)
71-expose-rehome-failover	assert_ok	B-rerun-effect brk3 is actually in phase DRAINING after that rc=0 (the terminal control-plane state the exit code claims), read back from the leader's roster — not inferred from the exit code alone
71-expose-rehome-failover	product_red	#31 acquire-lock window (owner: R7b lease+TTL / 'cluster unlock') — cluster drain brk3 was FENCED by a lingering in-flight membership op (rc=$_dmrc, signature NATS_ROLLED_OUT/in-flight/membership-operation). R7a fixed the release-failure shape and R7b put the marker under a 15m lease with an explicit operator clear, so this is no longer the expected steady state; reproducing it here is the registered defect surfacing, not a new one
71-expose-rehome-failover	not_covered	71 Arm B [P1 drain-migrate + the R8 rc semantics] THIS RUN
71-expose-rehome-failover	assert_ok	B-cmd cluster drain brk3 failed for an UNDOCUMENTED reason (rc=$_dmrc, matching NEITHER the R8 dataplane_not_converged signature NOR the #31 in-flight fence: [$(printf '%s' 
71-expose-rehome-failover	assert_ok	B-migrate [P1 TERMINAL STATE] UNREACHABLE — the drain failed for an undocumented reason above, so the migration never happens (RED, release-blocking)
71-expose-rehome-failover	not_covered	71 Arm B [DRAIN-MIGRATE] THIS RUN
71-expose-rehome-failover	not_covered	71 crash-strand core + drain arms THIS RUN
71-expose-rehome-failover	not_covered	71 [G/F only — Arm E is now DIRECTLY EXECUTED above (R7-M2)]: the home-return stickiness arm (G) and rehome_stalled{no_eligible_target} (F)
72-proxy-subscription	assert_ok	up 1 broker + 2 agents + 1 ctl
72-proxy-subscription	assert_ok	init brk1 (N=1 cluster)
72-proxy-subscription	assert_ok	session lab + ctl login (owner)
72-proxy-subscription	assert_ok	agent-join agt1
72-proxy-subscription	assert_ok	agent-join agt2
72-proxy-subscription	assert_ok	agent_provision_yaml agt1 proxyprivate (POSITIVE SS leg: allow_private_destinations)
72-proxy-subscription	assert_ok	agent_provision_yaml agt2 open (NEGATIVE leg: default private-dest deny)
72-proxy-subscription	assert_ok	SETUP ingress (mint leaf + same-netns TLS front /sub + trust inject on ctl1)
72-proxy-subscription	assert_ok	O-G3 proxy on WITHOUT --yes (non-PTY) → aborted (confirmProxyOn gate)
72-proxy-subscription	assert_ok	O1 proxy on --yes (owner) → PROXY ON
72-proxy-subscription	assert_ok	O1b both agents reach proxy-ready (SS server up; /sub populates only when ready)
72-proxy-subscription	assert_ok	O0 member login (2nd identity joins session lab via pin)
72-proxy-subscription	assert_refuses	O2 member proxy on → not_owner (owner-only)
72-proxy-subscription	assert_ok	O3 member proxy status readable + no PSK/token leak
72-proxy-subscription	assert_ok	C1 sub create alice → token minted (URL printed once)
72-proxy-subscription	assert_ok	C2 duplicate sub create alice → conflict pinned + first token still valid
72-proxy-subscription	assert_ok	SUB1 /sub loopback body: 2 ss proxies (agt1+agt2) w/ real exit host:port
72-proxy-subscription	assert_ok	SUB2 /sub cross-container via ingress = same 2 ss proxies (loopback stays loopback-only)
72-proxy-subscription	assert_ok	SUB3 forged /sub token → 404 (no info leak; tether's only outward public HTTP door)
72-proxy-subscription	assert_ok	SS0 fetch /sub to ctl1 via ingress (for ss-local parse)
72-proxy-subscription	assert_ok	SS0b /sub has exactly 2 ss proxies (agt1+agt2)
72-proxy-subscription	assert_ok	SUB2-neg cross-container DIRECT to brk1:8090 (bypass ingress) → connection refused 000 (product /sub is loopback-only, NOT dev-no-auth; Stage-C dp-3)
72-proxy-subscription	assert_ok	SS-pos agt1 (allow_private) exit → curl RFC1918 sink returns bytes (POSITIVE leg)
72-proxy-subscription	assert_ok	SS-neg-privdest agt2 (default deny) exit → sink blocked + journald blocked-dest (NEGATIVE leg)
72-proxy-subscription	assert_ok	SS-aead wrong-PSK to the agt1 (allow_private) exit → NO sink bytes, WHILE the correct PSK (SS-pos) DOES flow to the same sink — no bytes on an exit that never dest-blocks = pure AEAD trial-decrypt failure (Stage-C harness-safety-2)
72-proxy-subscription	assert_ok	REV-baseline alice+bob both /sub 200 (prove both live before revoke)
72-proxy-subscription	assert_ok	REV-ss0 bring up alice(1080)+bob(1085) SS legs via agt1 (each with its own PSK)
72-proxy-subscription	assert_ok	REV-ss-base-a alice SS leg (PSK_alice) flows bytes to the sink (pre-revoke LIVE baseline, REQUIRED)
72-proxy-subscription	assert_ok	REV-ss-base-b bob SS leg (PSK_bob) flows bytes to the sink (pre-revoke LIVE baseline, independent control, REQUIRED)
72-proxy-subscription	assert_ok	REV-hold-base BOTH held-open SS streams are ESTABLISHED + ACTIVELY TRANSFERRING bytes pre-revoke (received-byte count STRICTLY GROWING on each; not merely 'no exit marker' — R5-M1)
72-proxy-subscription	assert_ok	REV1 proxy sub revoke alice
72-proxy-subscription	assert_ok	REV-hold-alice [IN-FLIGHT FORCE-CLOSE] alice's ESTABLISHED byte stream is FORCE-CLOSED within ~30s of the revoke — its curl EXITS EARLY (marker written; NOT the ~120s natural end) with its bytes FROZEN. Revoke cuts an in-flight, byte-transferring stream, not merely new connections
72-proxy-subscription	assert_ok	REV-hold-bob [IN-FLIGHT SURVIVES] bob's HELD-OPEN stream KEEPS TRANSFERRING (not exited AND its byte count still GROWING after the revoke) — the force-close is PER-SUB, not a blanket cut
72-proxy-subscription	assert_ok	REV2 [SEMANTIC] alice /sub → 404 (revoked) WHILE bob /sub → 200 (control unaffected)
72-proxy-subscription	assert_ok	REV2-dp [DATA-PLANE] alice SS leg BLACK-HOLES after revoke (PSK_alice dropped from the agent keyset; new curl through 1080 no longer reaches the sink)
72-proxy-subscription	assert_ok	REV2-dp+ [DATA-PLANE] bob SS leg (PSK_bob still live) STILL flows bytes — revoke is per-sub, NOT a blanket outage
72-proxy-subscription	assert_ok	REV3 recover: sub create alice2 → new token minted
72-proxy-subscription	assert_ok	REV3b alice2 /sub → 200 (recovered token resolves; revoke did not break the proxy)
72-proxy-subscription	assert_ok	REV3-dp [DATA-PLANE] alice2 SS leg (new PSK_alice2) flows bytes to the sink — the data plane RECOVERED, not merely a /sub 200
72-proxy-subscription	assert_ok	OFF-port-pre brk1 LISTENs on the __proxy__ exit port $EXIT1_PORT (pre-off baseline — the exit's public listener is up)
72-proxy-subscription	assert_ok	OFF-alloc-pre the __proxy__ exit has >=1 ALLOCATED row in port_allocations (authoritative allocation-source baseline, not just the listener — R5-M1)
72-proxy-subscription	assert_ok	OFF1 proxy off
72-proxy-subscription	assert_ok	OFF2-dp [DATA-PLANE] the alice2 SS leg (1086) BLACK-HOLES after proxy off — the exit is torn down (SS server stopped + tunnel dropped), not just a /sub cosmetic change
72-proxy-subscription	assert_ok	OFF-port-reclaim [PORT RECLAIM] brk1 RELEASES the __proxy__ exit port $EXIT1_PORT after proxy off (the public LISTENER is GONE — ss -ltn no longer shows it). FAILS if tether leaks the listener.
72-proxy-subscription	assert_ok	OFF-alloc-reclaim [ALLOCATION RECLAIM] the __proxy__ exit's port_allocations rows drop to 0 after proxy off (the authoritative allocation SOURCE is released, not merely the OS listener — R5-M1)
72-proxy-subscription	assert_ok	OFF-reuse [SAFE REUSE] a fresh regular expose can claim the exact reclaimed port $EXIT1_PORT (no port_taken) and SERVE a sentinel through it — proves the allocation was genuinely released + the port is safely reusable, not a leaked half-state (R5-M1)
72-proxy-subscription	assert_ok	OFF3 [SEMANTIC] proxy status OFF + /sub still RESOLVES 200 with 0 ss nodes (DIRECT fallback; HTTP-200 gated, NOT a vacuous 404/empty)
73-proxy-cluster-ha	assert_ok	grow_to_3 + 2 agents + ctl (N=3 HA cluster; GROW-ATTEMPTS logged above)
73-proxy-cluster-ha	assert_ok	session lab + ctl login (owner)
73-proxy-cluster-ha	assert_ok	agent-join $_a (tunnel=brk1)
73-proxy-cluster-ha	assert_ok	agent_provision_yaml $_a proxyprivate
73-proxy-cluster-ha	assert_ok	SETUP ingress on all 3 brokers + trust ctl1
73-proxy-cluster-ha	assert_ok	proxy on --ha-policy freeze-on-quorum-loss (owner)
73-proxy-cluster-ha	assert_ok	both agent exits reach proxy-ready
73-proxy-cluster-ha	assert_ok	CV proxy status --cluster: CLUSTER line + per-agent HOME/REASON, member-readable, no secret
73-proxy-cluster-ha	assert_ok	SUB1 sub create alice → token minted
73-proxy-cluster-ha	assert_ok	SUB2 /sub loopback 200 (signed subscription doc)
73-proxy-cluster-ha	assert_ok	SUB3 /sub cross-container via ingress (loopback stays loopback-only)
73-proxy-cluster-ha	assert_ok	SUB4 forged /sub token → 404 (no info leak)
73-proxy-cluster-ha	assert_ok	SS-construct rebalance until >=1 exit homes OFF the tunnel/leader broker brk1 (now that >=2 voters are proxy-eligible, the spread is achievable; M1/R3-M1)
73-proxy-cluster-ha	assert_ok	SS-spread >=1 __proxy__ exit homes OFF brk1 (NT_A=$NT_A on $NT_HB), provably NOT all-on-tunnel — CONSTRUCTED, not assumed
73-proxy-cluster-ha	assert_ok	SS-guard NT_HB=$NT_HB is a NON-leader (the REHOME kill must not take the leader — Stage-C cluster-4)
73-proxy-cluster-ha	assert_ok	SS [DATA-PLANE] egress via $NT_A (home=$NT_HB != tunnel brk1) → curl RFC1918 sink returns bytes — proxy dials HomeBrokerAddr+pins, IMMUNE to expose #29 (steady state)
73-proxy-cluster-ha	assert_ok	REHOME-baseline SS leg via $NT_A (home=$NT_HB) flows bytes pre-kill (live destructive baseline; the SAME verified-ready client observes the crash black-hole below — anti-vacuity)
73-proxy-cluster-ha	assert_ok	REHOME-precond-live the kill target $NT_HB is still RUNNING (killing an already-dead container is a vacuous injection)
73-proxy-cluster-ha	assert_ok	REHOME-precond-home $NT_HB STILL homes $NT_A at kill time — the 1/1/1 spread constructed by 'cluster rebalance proxy' HELD long enough to be used (a KEPT invariant, and the causal precondition of every #33 claim below)
73-proxy-cluster-ha	product_red	#34 proxy home distribution does not STAY put (owner: R8 product side; docs/deploy-tier-gotchas.md:231) — the spread this drill CONSTRUCTED with 'cluster rebalance proxy' had already drifted by kill time: $NT_A is homed on [${_np_home:-none}], not the constructed $NT_HB. Same signature the 2026-07-18 full-suite run recorded (a constructed 1/1/1 collapsing back onto one broker after a disturbance). Operationally: proxy HA capacity spread is not durable — an operator must re-run 'cluster rebalance proxy' after every disturbance or the spread is decorative
73-proxy-cluster-ha	assert_ok	REHOME kill $NT_A's home broker $NT_HB (non-leader; quorum kept 2/3 so the rehome raft-write can commit)
73-proxy-cluster-ha	assert_ok	REHOME [#33-a] the SS data plane via $NT_A BLACK-HOLES right after its home broker $NT_HB crashes — the crash severed the tunnel to the dead home; the SAME verified-ready client that flowed pre-kill now black-holes (the exit, not the client)
73-proxy-cluster-ha	assert_ok	REHOME [CONTROL] $NT_A's __proxy__ home leaves the dead $NT_HB (auto-rehome to a survivor voter — a raft rehome write)
73-proxy-cluster-ha	assert_ok	REHOME [CONTROL] $NT_A reaches proxy-ready post-rehome at least once (ready = the agent re-ACKed after OpenHome re-dialed the new home; NB it may then FLAP — that is part of #33 below, not a die condition)
73-proxy-cluster-ha	assert_ok	REHOME [#33 FIXED] the SS DATA plane AUTO-recovers from a crash-rehome — the exit via $NT_A flows again within 90 s of killing its home $NT_HB (observed 16–29 s over 7 samples; a bound from observation, not an SLA) with no manual 'proxy off/on'
73-proxy-cluster-ha	assert_ok	REHOME [#33 mechanism: pool reconnect] $NT_A's NATS connection WAS on the killed $NT_HB and moved to a survivor, and its slog shows the re-register after reconnect — the path the 2026-09-19 samples exercised (nats.go pool reconnect + re-register; NOT the stuck-disconnect session rebuild #80 decoupled)
73-proxy-cluster-ha	not_covered	REHOME [#33 mechanism] the agent's NATS connection was on a survivor at kill time (pre=${_agt_conn_pre:-none}, killed=$NT_HB)
73-proxy-cluster-ha	not_covered	73 Arm REHOME (#33 crash-rehome control-plane + data-plane measurement) THIS RUN
73-proxy-cluster-ha	assert_setup	REHOME-skip [env] kill $NT_HB as PURE QUORUM-arm PROVISIONING (2 of 3 brokers must be down before the quorum-loss separation below; this kill asserts NOTHING about #33 — see the NOT-COVERED above)
73-proxy-cluster-ha	assert_ok	REVOKE carol minted + /sub 200 before revoke
73-proxy-cluster-ha	assert_ok	REVOKE-event [#30] anchor: the operator reader (admin events) reads the earlier sub-create proxy_keyset_changed events (>=2 for sid=lab) — proves the reader WORKS in this drill before pinning the revoke's delta (non-vacuity)
73-proxy-cluster-ha	assert_ok	REVOKE proxy sub revoke carol
73-proxy-cluster-ha	assert_ok	REVOKE-effect carol /sub → 404 WHILE alice /sub still 200 (precise, not a blanket outage)
73-proxy-cluster-ha	assert_ok	REVOKE-event [#30] admin events reads the CLUSTER-MODE revoke's proxy_keyset_changed: the sid=lab count grew past the pre-revoke baseline ${_PKC_BEFORE:-?} (DELTA pins the revoke's emit a7b88cb, not the sub-create emits) — operator reader over the root admin socket, no NATS member cred (closes the old #30 no-reader gap)
73-proxy-cluster-ha	assert_ok	REVOKE-event [#30] the proxy_keyset_changed payload is SECRET-FREE (body keys ⊆ {v,type,ts,sid}; the reader relays only sid — never a psk/token/PSK material)
73-proxy-cluster-ha	assert_ok	Q-heal-off proxy off succeeds (exit 0; raft write at 2/3, quorum intact)
73-proxy-cluster-ha	assert_ok	Q-heal-on proxy on succeeds → FRESH keyset + re-establishes all exits on the 2 survivors (sidestepping #33's crash-rehome recovery timing)
73-proxy-cluster-ha	assert_ok	Q-heal2 both surviving exits reach proxy-ready after the heal
73-proxy-cluster-ha	assert_ok	Q-construct rebalance until BOTH leader $LDR and K2 $K2 each home >=1 exit (DETERMINISTIC 1+1 over the 2 live voters — both baselines fresh+healthy after the heal; survivor NOT optional; waits out K2's post-reconfiguration proxy-eligibility)
73-proxy-cluster-ha	assert_ok	Q-legup-dead dead-homed SS leg via $DEAD_A (home $DEAD_HB) renders+flows bytes pre-kill (baseline for the separation)
73-proxy-cluster-ha	assert_ok	Q-legup-surv survivor-homed SS leg via $SURV_A (home leader $LDR) renders+flows bytes pre-kill (independent live control source)
73-proxy-cluster-ha	assert_ok	Q-baseline /sub 200 before quorum loss
73-proxy-cluster-ha	assert_ok	Q-xcheck the dead-homed exit $DEAD_A's /sub-VENDED server is EXACTLY the broker about to be killed ($DEAD_HB) — control-plane home_broker AND the vended data-plane endpoint AGREE (polled up to 60 s past the re-dial window), so a post-kill black-hole is the dead home, not a stale/other endpoint (R5-M6)
73-proxy-cluster-ha	assert_ok	Q-kill kill dead-homed exit's broker $DEAD_HB (→ 1/3 with the REHOME-killed $NT_HB; committable quorum lost; leader $LDR survives)
73-proxy-cluster-ha	assert_ok	Q-dead $DEAD_HB is genuinely DOWN after node_kill (container not running — its tunnel/public ports are gone) so the black-hole below is the DEAD home, not a stale endpoint (R5-M6)
73-proxy-cluster-ha	assert_ok	Q-freeze [DATA-PLANE] dead-homed SS via $DEAD_A (home=$DEAD_HB killed) BLACK-HOLES under quorum loss (SS rides the home broker's tunnel; home dead → leg dead; no rehome possible — that is a raft write, quorum is gone)
73-proxy-cluster-ha	assert_ok	Q-freeze [DATA-PLANE+] survivor-homed SS via $SURV_A (home=leader $LDR alive) STILL flows bytes — the REQUIRED independent live control source proves this is a HOME-scoped black-hole, not a whole-proxy outage
73-proxy-cluster-ha	assert_ok	Q-freeze [SEPARATION] the dead SS leg is PROVEN dead (black-hole held above) WHILE /sub STILL vends 200 (frozen READ gate) — proves /sub-200 ≠ data-plane liveness (#20). CAUSALLY GATED on the black-hole: a dead leg that kept serving FAILS this, never a false 'DEAD while 200' conjunction (R5-M6)
73-proxy-cluster-ha	assert_ok	Q-freeze [corrob] a control WRITE is FENCED (proxy sub create does NOT mint under quorum loss)
73-proxy-cluster-ha	not_covered	73 QUORUM separation proof THIS RUN — Q ENDPOINT MISMATCH (R7-M3 exposed defect)
73-proxy-cluster-ha	not_covered	73 QUORUM data-plane separation THIS RUN
74-rebalance-on-return._shared	not_covered	74 #34 REGISTERED-OPEN: the constructed one-per-voter proxy-home spread is NOT verified to STAY put (face 1 reproduced 1/13 concurrent samples on 2026-09-19, unattributed)
74-rebalance-on-return._shared	assert_ok	grow_to_3 + 3 agents + ctl (N=3 HA cluster; GROW-ATTEMPTS logged above)
74-rebalance-on-return._shared	assert_ok	session lab + ctl login (owner)
74-rebalance-on-return._shared	assert_ok	agent-join $a
74-rebalance-on-return._shared	assert_ok	agent_provision_yaml $a proxyprivate
74-rebalance-on-return._shared	assert_ok	SETUP ingress on all 3 brokers + trust ctl1 (for /sub cross-container SS legs)
74-rebalance-on-return._shared	assert_ok	proxy on --yes (owner)
74-rebalance-on-return._shared	assert_ok	3 agent exits reach proxy-ready
74-rebalance-on-return._shared	assert_ok	SETUP-111 construct one-per-voter via cluster rebalance proxy (spread==0 — the locked 1/1/1 baseline; the non-deterministic initial reconcile can pile exits, so this is CONSTRUCTED not assumed)
74-rebalance-on-return.SRAB	assert_ok	SKEW-reconstruct re-build 1/1/1 (spread==0, rc-checked rebalance) right before the destructive skew — RED if the proxy distribution can't be re-balanced (the #34 non-tunnel-voter proxy-eligibility instability). GATES the destructive arms (R7-M1: a failed foundation must NOT flow into misleading destructive PASS lines)
74-rebalance-on-return.SRAB	assert_ok	SKEW-flow exit $_rnid flows bytes over the RECONSTRUCTED 1/1/1 baseline ADJACENT to the skew (the locked 'one-per-voter + every exit flowing' SKEW baseline over a VALIDATED snapshot, plan §3-74 ①; per-nid via _ss_via_agent — GATES the destructive arms, R8-M1/R9-M1)
74-rebalance-on-return.SRAB	assert_ok	SKEW-flow-snapshot the SKEW baseline needs a VALIDATED proxy-status snapshot (exactly $EXPECT_EXITS distinct nids, all homes real voters) — RED + fail-closed when the status is empty/partial/malformed (R9-M1: a zero-iteration enumeration must NOT leave RECON_FLOW=1)
74-rebalance-on-return.SRAB	not_covered	74 destructive arms (SKEW/RETURN/A/B) THIS RUN
74-rebalance-on-return.SRAB	assert_ok	SKEW-precond KTGT=$KTGT started with ≥1 __proxy__ home (else the _ktgt_empty rehome-away oracle is vacuously satisfied; Stage-C pin-6 + self-review finding-10)
74-rebalance-on-return.SRAB	assert_ok	SKEW kill $KTGT → its exits rehome AWAY to survivors ($KTGT home count → 0)
74-rebalance-on-return.SRAB	assert_ok	RETURN restart $KTGT → rejoins as VOTER (cluster back to 3 voters)
74-rebalance-on-return.SRAB	assert_ok	A-elig returned $KTGT becomes proxy-ELIGIBLE within 240s (rebalance --dry-run PLANS a move onto it → auto-rebalance COULD fire here if it were on; the dry-run mutates nothing). RED if the returned voter's proxy-eligibility recovery does NOT complete in a GENEROUS window — a real returned-voter-eligibility gap, not a too-short poll (R6)
74-rebalance-on-return.SRAB	assert_ok	A default-off: $KTGT stays at 0 homes across a 30s quiet window (STABLE no auto-move-back without TETHER_AUTO_REBALANCE — not merely 'not yet'; M6)
74-rebalance-on-return.SRAB	assert_ok	B-settle post-return proxy distribution stops drifting before measuring dry-run
74-rebalance-on-return.SRAB	assert_ok	B-flow-pre exit $_pnid flows bytes before the manual rebalance (per-nid SEQUENTIAL probe over a VALIDATED snapshot, ~90s each; the fresh adjacent snapshot re-validates attribution right before B-real; whichever exit B moves onto $KTGT is proven live — GATES the WHOLE B injection, R8-M1/R9-M1/R10-M1)
74-rebalance-on-return.SRAB	assert_ok	B-flow-pre-snapshot the B pre-flow baseline needs a VALIDATED proxy-status snapshot (exactly $EXPECT_EXITS distinct nids, all homes real voters) — RED + fail-closed when the status is empty/partial/malformed (R9-M1: a zero-iteration enumeration must NOT leave B_PREFLOW=1)
74-rebalance-on-return.SRAB	not_covered	74 B injection (dry-run / real rebalance / B-move / B-dp / negative control) THIS RUN
74-rebalance-on-return.SRAB	assert_ok	B-snapshot the fresh attribution snapshot adjacent to the injection is VALIDATED (rc=0 + non-empty exact nid=home, captured WITHOUT a rc-masking pipeline) — R10-M1
74-rebalance-on-return.SRAB	assert_ok	B-dry cluster rebalance proxy --dry-run: preview + ZERO change (home counts byte-identical)
74-rebalance-on-return.SRAB	assert_ok	B-real cluster rebalance proxy: evens the distribution (spread<=1)
74-rebalance-on-return.SRAB	assert_ok	B-real-effect $KTGT now carries >=1 home again (rebalance moved exits BACK onto the returned voter)
74-rebalance-on-return.SRAB	assert_ok	B-real-nonone NO exit left un-homed (home=none) after rebalance — spread<=1 must not hide a silently un-homed agent; Stage-C cluster-3
74-rebalance-on-return.SRAB	assert_ok	B-move a REAL exit MOVED onto $KTGT by THIS manual rebalance (exact nid $DP_A non-empty, old home ${DP_BEFORE:-?} != $KTGT, new home == $KTGT) — the attribution GATE for B-dp (R8-M1)
74-rebalance-on-return.SRAB	not_covered	74 B-dp THIS RUN
74-rebalance-on-return.SRAB	assert_ok	B-dp [DATA-PLANE, HARD] an SS leg MUST flow bytes through the RECORDED moved exit $DP_A (was on $DP_BEFORE, proven flowing pre-B, now on $KTGT) within 240s — tests THAT exact nid via _ss_via_agent, not whatever is first on $KTGT (R8-M1). RED if it STRANDS with the local SS client PROVEN READY (stage=$_bdps) = the #33-family moved-exit stranding EXPOSED as release-blocking (R6-M1; the harness-vs-product split is H10)
74-rebalance-on-return.SRAB	not_covered	74 B-dp THIS RUN
74-rebalance-on-return.SRAB	assert_ok	B-negctrl-create ordinary expose reg create command succeeded (rc=0)
74-rebalance-on-return.SRAB	assert_ok	B-negctrl-pre reg becomes VALIDLY homed on a real broker + SERVING before the rebalance (POLLED — the data plane takes a few seconds; a NON-VACUOUS negative control, R6-M1: not an empty-explain 'single' compare) — GATES the negative-control transaction (R8-M1)
74-rebalance-on-return.SRAB	assert_ok	B-negctrl-rc the rebalance command SUCCEEDED (rc=0) so 'reg unchanged' is a REAL negative control, not a no-op (R6-M1)
74-rebalance-on-return.SRAB	assert_ok	B-negctrl the co-homed ORDINARY expose reg is NOT moved by the SUCCESSFUL __proxy__-only rebalance — home STILL $EXH0 AND still SERVING (plan inv-5; RED if it moved or stopped serving)
74-rebalance-on-return.SRAB	not_covered	74 B-negctrl THIS RUN
74-rebalance-on-return.SRAB	not_covered	74 B-negctrl THIS RUN
74-rebalance-on-return.SRAB	assert_ok	B-snapshot the FRESH attribution snapshot adjacent to the injection is VALIDATED (rc=0 + non-empty exact nid=home) — RED + fail-closed skip of the WHOLE B injection when the status is empty/partial/malformed (R10-M1: a _snap_nidhome|tr pipeline previously masked the helper rc; captured WITHOUT a pipeline now)
74-rebalance-on-return.SRAB	not_covered	74 B injection (dry-run / real / B-move / B-dp / negative control) THIS RUN
74-rebalance-on-return.C	assert_ok	C-negctrl-fixture ordinary expose reg create command succeeded (rc=0) — the pre-control C builds for itself now that it runs on its own cluster (it used to inherit B's)
74-rebalance-on-return.C	assert_ok	C-negctrl-fixture-pre reg becomes VALIDLY homed on a real broker + SERVING before Arm C (POLLED; a NON-VACUOUS negative control, R6-M1) — GATES C-negctrl
74-rebalance-on-return.C	not_covered	74 C-negctrl THIS RUN
74-rebalance-on-return.C	assert_ok	C-setup TETHER_AUTO_REBALANCE=on on all broker units (drop-in + daemon-reload + restart; rejoin 3-VOTER) — GATES Arm C (R8-M2)
74-rebalance-on-return.C	assert_ok	C-verify env live on every broker (systemctl show -p Environment) — GATES Arm C (R8-M2)
74-rebalance-on-return.C	assert_ok	C-settle post-restart __proxy__ distribution STOPS drifting BEFORE the Arm-C KTGT snapshot (C-setup just restarted every broker; sampling mid-reconcile read all candidates at 0 homes and picked a 0-home KTGT, making _skew's rehome-away oracle vacuous — H11) — GATES the auto edge
74-rebalance-on-return.C	assert_ok	C-SKEW-precond KTGT=$KTGT carries >=1 __proxy__ home BEFORE the C kill (else _skew's _ktgt_empty rehome-away oracle is vacuously satisfied from the start — R8-M2, the retry hit KTGT with 0 homes)
74-rebalance-on-return.C	not_covered	74 C-ss-pre THIS RUN via $_cnid
74-rebalance-on-return.C	assert_ok	C-ss-pre exit $_cnid flows bytes over the POST-RESTART baseline before the C kill (per-nid SEQUENTIAL probe, ALL exits over a VALIDATED snapshot, ~90s each — R9-M2; the harness-vs-product split is H10, stage=$_cfs) — GATES the auto edge
74-rebalance-on-return.C	assert_ok	C-ss-pre-snapshot the post-restart baseline needs a VALIDATED proxy-status snapshot (exactly $EXPECT_EXITS distinct nids, all homes real voters) — RED + fail-closed when the status is empty/partial/malformed (R9-M2)
74-rebalance-on-return.C	assert_ok	C-skew-adjacent a non-leader non-tunnel broker STILL carries >=1 __proxy__ home on a VALIDATED snapshot ADJACENT to the kill (KTGT reselected=$KTGT) — R10-M1: the ~270s all-nid pre-flow loop drifts the distribution, so re-validate a NON-VACUOUS kill boundary right before _skew (else _skew kills a 0-home broker and _ktgt_empty is true from the start)
74-rebalance-on-return.C	assert_ok	C-skew kill $KTGT → its exits rehome AWAY ($KTGT count→0) — GATES the auto edge (R8-M2)
74-rebalance-on-return.C	assert_ok	C-return restart $KTGT → rejoins VOTER — GATES the auto edge (R8-M2)
74-rebalance-on-return.C	assert_ok	C-still-skewed $KTGT is STILL at 0 homes immediately before the auto window, OR the auto path already fired inside the return (proxy_auto_rebalanced advanced past its baseline — no manual verb can emit that) — GATES the auto EFFECT (R8-M2)
74-rebalance-on-return.C	assert_ok	C-auto [AUTO-EFFECT, HARD] distribution AUTO-evens (spread<=1) within the locked 180s window WITHOUT the manual verb OVER A VALID skew+return edge — TETHER_AUTO_REBALANCE=on MUST auto-rebalance-on-return (locked plan §3-74 Arm C). RED if it does NOT fire = the auto-rebalance gap EXPOSED as release-blocking; likely root = the #31 lingering in-flight op closing the 'no in-flight op' fire-gate (proxy_auto_rebalance.go:57), the same op that blocks drain/upgrade (R6-M1)
74-rebalance-on-return.C	assert_ok	C-event [#30] admin events reads EXACTLY ONE proxy_auto_rebalanced for the single Arm-C return edge (anti-flap count==1; delta=$_par_delta) — #30's operator reader makes the plan's exact-one EVENT assertion PINNABLE (was carved out UN-PINNABLE: no operator read command); the auto EFFECT itself stays the HARD C-auto assertion above
74-rebalance-on-return.C	assert_ok	C-event [#30] the proxy_auto_rebalanced payload is SECRET-FREE (body keys ⊆ {v,type,ts,reason,returned,voters,planned}; carries only node ids + scalar counts — never a psk/token)
74-rebalance-on-return.C	assert_ok	C-move a REAL exit AUTO-moved onto $KTGT (nid $C_DP_A non-empty, pre-auto home ${C_DP_BEFORE:-?} != $KTGT, AND $C_DP_A was proven flowing pre-kill) — the attribution gate for C-dp (R8-M2/R9-M2)
74-rebalance-on-return.C	assert_ok	C-dp [DATA-PLANE] SS leg flows bytes through the RECORDED auto-moved exit $C_DP_A (pre-auto home $C_DP_BEFORE, proven flowing pre-kill, now on $KTGT) after the auto rebalance — tests THAT exact nid via _ss_via_agent (R8-M2/R9-M2)
74-rebalance-on-return.C	not_covered	74 C-dp THIS RUN
74-rebalance-on-return.C	not_covered	74 C-dp THIS RUN
74-rebalance-on-return.C	not_covered	74 C-auto + C-dp THIS RUN
74-rebalance-on-return.C	assert_ok	C-negctrl the co-homed ORDINARY expose reg is NOT moved by the Arm-C return transaction — home STILL $EXH0 AND still SERVING — the __proxy__-only negative control ACROSS THE AUTOMATIC return transaction (R7-M1 unconditional-on-C_AUTO; R8-M1 gated-on-established-pre-control; plan inv-5)
78-proxy-dial-backoff	assert_ok	up 1 broker + 1 agent + 1 ctl
78-proxy-dial-backoff	assert_ok	init brk1 (N=1: auth_callout + JS bootstrap)
78-proxy-dial-backoff	assert_ok	session $SID + ctl login (owner)
78-proxy-dial-backoff	assert_ok	agent-join agt1 (onboards + tunnel to brk1)
78-proxy-dial-backoff	assert_ok	A0 inject: agt1 -> :$TUNNEL_PORT REJECTs (dial can never establish)
78-proxy-dial-backoff	assert_ok	A1 proxy on (owner, confirmed)
78-proxy-dial-backoff	assert_ok	A2 anti-vacuous: the agent really was dialing (bucket-1 attempts >= 2)
78-proxy-dial-backoff	assert_ok	A3 backoff cut the total well below the no-backoff baseline (total <= 20 vs ~39 at a fixed 5s)
78-proxy-dial-backoff	assert_ok	A4 backoff trend: bucket-3 < bucket-1 (strictly fewer attempts as the schedule deepens)
78-proxy-dial-backoff	assert_ok	A5 suppressed node stays unready in proxy status
78-proxy-dial-backoff	assert_ok	B0 heal the tunnel path
78-proxy-dial-backoff	assert_ok	B1 proxy off (owner)
78-proxy-dial-backoff	assert_ok	B2 proxy on again (owner, confirmed)
78-proxy-dial-backoff	assert_ok	B3 the new epoch establishes within 60s (no backoff hangover from arm A)
78-proxy-dial-backoff	not_covered	78 C broker read-REGISTER WARN damping (#78 face ②)
78-proxy-dial-backoff	assert_ok	D0 provision agt1 with proxy.participate=false (real agent.yaml + flagless restart, ONLINE again)
78-proxy-dial-backoff	assert_ok	D1 re-arm the dial counter
78-proxy-dial-backoff	assert_ok	D2 opted-out node makes ZERO dial attempts across 60s of live repair loop
78-proxy-dial-backoff	assert_ok	D3 proxy status marks agt1 opted-out (won't, not can't)
78-proxy-dial-backoff	assert_ok	D4 the __proxy__ allocation is freed (agt1 status row carries no public port, read from real product state)
78-proxy-dial-backoff	assert_ok	D5 heal the tunnel path for the recovery leg
78-proxy-dial-backoff	assert_ok	D6 provision agt1 back with proxy.participate=true (ONLINE again)
78-proxy-dial-backoff	assert_ok	D7 the node re-enters the pool and serves again within 90s
80-session-isolation	assert_ok	up 1 broker + 2 agents + 1 ctl
80-session-isolation	assert_ok	init brk1 (standalone → N=1 cluster)
80-session-isolation	assert_ok	N=1 leader floor (leader=brk1)
80-session-isolation	assert_ok	operator admits A as a session creator
80-session-isolation	assert_ok	operator admits B as a session creator
80-session-isolation	assert_ok	A creates session lab
80-session-isolation	assert_ok	A activates lab (--broker persists broker_url)
80-session-isolation	assert_ok	B creates session ops
80-session-isolation	assert_ok	B activates ops (--broker persists broker_url)
80-session-isolation	assert_ok	bind agt1 → lab
80-session-isolation	assert_ok	bind agt2 → ops
80-session-isolation	assert_ok	T joins lab (non-owner member)
80-session-isolation	assert_ok	T joins ops (non-owner member)
80-session-isolation	assert_ok	nats_as pre-flight: A pub on its OWN lab subject (impersonation valid)
80-session-isolation	assert_ok	I1-pos: A activates its own lab (positive control)
80-session-isolation	assert_refuses	I1-neg: A activating ops (non-member, no PIN) REFUSED at CONNECT
80-session-isolation	assert_ok	I1-persist: failed activation left ctx=lab (not ops)
80-session-isolation	assert_ok	I1-broker: --broker alias persisted broker_url (node ls with no --nats-url works)
80-session-isolation	assert_ok	I2-posA: A pub own lab subject (control)
80-session-isolation	assert_ok	I2-posB: B pub own ops subject (control)
80-session-isolation	assert_ok	I2-posA-sub: A CAN sub its own lab audit (R4 control — member Sub.Allow non-empty)
80-session-isolation	assert_ok	I2-posB-sub: B CAN sub its own ops audit (R4 control, symmetric)
80-session-isolation	assert_refuses	I2-AB-pub: A(lab)→ops PUB refused (Permissions Violation)
80-session-isolation	assert_ok	I2-AB-sub: A(lab)→ops SUB refused (Permissions Violation, verify OUTPUT)
80-session-isolation	assert_refuses	I2-BA-pub: B(ops)→lab PUB refused (symmetric)
80-session-isolation	assert_ok	I2-BA-sub: B(ops)→lab SUB refused (symmetric, verify OUTPUT)
80-session-isolation	assert_ok	I2c-posA: A sees its own tenant node agt1 (control)
80-session-isolation	assert_refuses	I2c-AB: A(lab) cannot reach agt2 (in ops) → node_not_found
80-session-isolation	assert_refuses	I2c-BA: B(ops) cannot reach agt1 (in lab) → node_not_found (symmetric)
80-session-isolation	assert_ok	I3-pos: T is a live lab MEMBER (control; so not_owner is app-layer)
80-session-isolation	assert_refuses	I3-rm: non-owner session rm → not_owner
80-session-isolation	assert_refuses	I3-up: non-owner node upgrade → not_owner (before url/sha validation)
80-session-isolation	assert_ok	E-sub: authoritative sys.events observer live
80-session-isolation	assert_refuses	E-neg: W wrong-PIN join REFUSED at CONNECT
80-session-isolation	assert_ok	E-pinfailed: pin_failed sys.event captured (sid=lab, role=ctl; the client-hidden reason)
80-session-isolation	assert_ok	E-pos: same identity correct-PIN join succeeds (no false lockout)
80-session-isolation	assert_ok	E-joined: member_joined sys.event captured (sid=lab, via=pin; W is the only post-sub join)
80-session-isolation	assert_ok	R-sub: fresh sys.events observer live
80-session-isolation	assert_refuses	R-11th [#25 CLOSED] after $RF same-IP wrong-PIN attempts, the 11th same-IP CORRECT-PIN join is REFUSED at CONNECT — the per-IP PIN rate limit now shuts the source out (a correct PIN would otherwise join, so this refusal is the limiter, not a bad credential)
80-session-isolation	assert_ok	R-fails: all $RF same-IP storm attempts were refused (wrong PIN or rate-limited)
80-session-isolation	assert_ok	R-pinfailed: the wrong-PIN storm reached the Argon2-failure path (pin_failed captured — the §E.6 counter's real trigger)
80-session-isolation	assert_ok	R-ratelimit-sig [#25]: broker log shows the per-IP PIN rate limit fired ('… PIN attempt rate-limited')
80-session-isolation	assert_ok	R-2ndsrc: a correct-PIN join from a DIFFERENT source IP still succeeds while ctl1 is throttled (per-IP scope, no collateral lockout)
80-session-isolation	assert_ok	TS-lab: shell pinned to lab sees agt1, NOT agt2
80-session-isolation	assert_ok	TS-ops: shell pinned to ops sees agt2, NOT agt1 (symmetric)
80-session-isolation	assert_ok	TS-file: env unset falls back to file (last login = ops)
81-admin-evict-session-rm	assert_ok	up 1 broker + 1 agent + 1 ctl
81-admin-evict-session-rm	assert_ok	init brk1 (standalone → N=1 cluster)
81-admin-evict-session-rm	assert_ok	N=1 leader floor (leader=brk1)
81-admin-evict-session-rm	assert_ok	session lab + ctl login (owner)
81-admin-evict-session-rm	assert_ok	agent-join agt1 (bind nkey)
81-admin-evict-session-rm	assert_ok	provision agt1 agent.yaml (tunnel_addr wired, S0-隧道)
81-admin-evict-session-rm	assert_ok	A1 admin sessions (authorized) lists lab ACTIVE
81-admin-evict-session-rm	assert_ok	A2 admin nodes (authorized) shows agt1 ONLINE
81-admin-evict-session-rm	assert_ok	A3 admin audit (authorized, history-lab EXISTS)
81-admin-evict-session-rm	assert_refuses	A4 non-authorized OS user (nobody) → OS EACCES connect: permission denied
81-admin-evict-session-rm	assert_ok	B0 baseline: agt1 ONLINE (pre-injection)
81-admin-evict-session-rm	assert_ok	B1 admin evict (2-arg) deletes + broadcasts
81-admin-evict-session-rm	assert_ok	B2a agent daemon self-exits ≤~1s (PROCESS gone, not heartbeat expiry)
81-admin-evict-session-rm	assert_ok	B2b roster row deleted (FK)
81-admin-evict-session-rm	assert_ok	B3 reconnect refused (provisioning gone) for the agent-specific auth-callout rejection, read from the agent's BOOT stream written by this attempt (distinguishes auth-deny from unreachable: an unreachable broker prints nothing and retries; a success is red too)
81-admin-evict-session-rm	assert_ok	D1 evicted nkey (byte-identical, md5==D0) re-provisions with same session PIN → ONLINE (eviction ≠ revocation, no denylist; DOC-6)
81-admin-evict-session-rm	assert_ok	D1-reprovision agt1 agent.yaml (tunnel_addr, for Arm C)
81-admin-evict-session-rm	assert_ok	C-setup stop systemd unit
81-admin-evict-session-rm	assert_ok	C-setup agt1 ONLINE again (setsid-nohup deploy)
81-admin-evict-session-rm	assert_ok	C-base-proc managed child RUNNING (pre-injection baseline, agent-host pgrep)
81-admin-evict-session-rm	assert_ok	C-base-proc-db broker 'ps' shows the child RUNNING (pre-injection DB baseline for the divergence)
81-admin-evict-session-rm	assert_ok	C-base-expose active expose curl-able (SENTINEL body cross-container; pre-injection baseline)
81-admin-evict-session-rm	assert_ok	C1 admin evict at active state
81-admin-evict-session-rm	assert_ok	C-brk broker DB proc+node rows GONE (FK cascade — evict contract)
81-admin-evict-session-rm	assert_ok	C-exit agent daemon exited
81-admin-evict-session-rm	assert_ok	C-reap [#26 CLOSED] evict reaps the managed OS child on a setsid-nohup host: daemon EXITED and the managed child is GONE from the host process table (agent SIGKILLs the child's process group on agent_evicted; NOT relying on a systemd cgroup)
81-admin-evict-session-rm	assert_ok	C-port public expose port CLEANED: transport CONNECTION-REFUSED (curl exit 7, NOT a 4xx/5xx leaked listener; F3) + broker port-alloc row gone (FK cascade); mechanism = tunnel drop on daemon exit
81-admin-evict-session-rm	assert_ok	C-sysd-setup re-join agt1 under SYSTEMD unit
81-admin-evict-session-rm	assert_ok	C-sysd-base systemd-deployed managed child RUNNING
81-admin-evict-session-rm	assert_ok	C-sysd-evict evict the systemd-deployed agent
81-admin-evict-session-rm	assert_ok	C-sysd-reap [#26 CLOSED] under systemd, evict reaps the managed child too (agent SIGKILL + cgroup teardown both apply): child GONE from the host process table
81-admin-evict-session-rm	assert_ok	E-sub2 sys.events observer live (owner is a lab member; 24h JWT survives destroy)
81-admin-evict-session-rm	assert_ok	E-base baseline: lab ACTIVE + history-lab exists (pre-deletion)
81-admin-evict-session-rm	assert_ok	E1 session rm (owner)
81-admin-evict-session-rm	assert_ok	E2a phase②: history-lab stream deleted (RESULT)
81-admin-evict-session-rm	assert_ok	E2b phase③: SQLite session row gone (RESULT)
81-admin-evict-session-rm	assert_ok	E2c phase④: session_destroyed sys.event captured (direct oracle)
81-admin-evict-session-rm	assert_refuses	E3a session-scoped exec on the removed sid → refused broker-side at CONNECT (no agent broadcast; DOC-11)
81-admin-evict-session-rm	assert_refuses	E3b same refusal on expose (independent CLI path → same broker-enforced refusal)
81-admin-evict-session-rm	assert_refuses	E3c 2nd session rm on the absent sid → also CONNECT-denied (rm-API not_found is hermetic-only, unreachable post-rm)
82-agent-onboarding-invite	assert_ok	up 2 brokers + 2 fresh agents + 1 ctl
82-agent-onboarding-invite	assert_ok	init brk1 (standalone → N=1 cluster)
82-agent-onboarding-invite	assert_ok	N=1 leader floor (leader=brk1) — broker alive, so the #27 bound/refused checks reflect the manifest listener, not a dead broker
82-agent-onboarding-invite	assert_ok	SETUP-27-control a genuinely-unbound port yields curl 000 (proves the connection-refused oracle discriminates — the bound check below is not a vacuous 'curl always returns non-000')
82-agent-onboarding-invite	assert_ok	SETUP-27 [#27 CLOSED] after cluster init the well-known manifest listener is BOUND by default (curl to 127.0.0.1:7480 answers, NOT connection-refused) — no operator manifest_listen step required
82-agent-onboarding-invite	assert_ok	SETUP-ingress mint ingress leaf for brk1
82-agent-onboarding-invite	assert_ok	SETUP-ingress start same-netns HTTPS front on brk1:443
82-agent-onboarding-invite	assert_ok	SETUP-ingress inject instance CA into agt1 trust store
82-agent-onboarding-invite	assert_ok	session lab + ctl login (owner)
82-agent-onboarding-invite	assert_ok	P0 no seed bundle before first publish (first publish is manual — product semantic)
82-agent-onboarding-invite	assert_ok	P1 seeds publish minted an agent-join invite (tether-invite:v1?...&sid=lab)
82-agent-onboarding-invite	assert_ok	M1 broker-local loopback manifest verifies (signed JSON with roster+seeds, not 503)
82-agent-onboarding-invite	assert_ok	M2 cross-container https via S0-ingress + cert-verify (same signed JSON; loopback stays loopback-only)
82-agent-onboarding-invite	assert_ok	J1 fresh agent join --start → ONLINE (real auth_callout first-connect provision)
82-agent-onboarding-invite	assert_ok	J1-online agt1 reaches ONLINE (first-ever ONLINE = real registration)
82-agent-onboarding-invite	assert_ok	J2 agent.yaml at real path, 0600, correct fields
82-agent-onboarding-invite	assert_ok	J3 roster cache pre-warmed (S0-ingress manifest fetch + AdoptDecision succeeded)
82-agent-onboarding-invite	assert_ok	J4 config refresh --once = hard TLS positive oracle (real Go-x509 fetch through trusted front)
82-agent-onboarding-invite	assert_ok	J5 agent doctor: exit 0 AND 'manifest verifies against the pin' AND no FATAL (R8: not bare exit-0)
82-agent-onboarding-invite	assert_ok	I-NEG-SAN wrong-SAN cert refused (CA trusted, hostname mismatch)
82-agent-onboarding-invite	assert_ok	I-NEG-CA untrusted CA refused (agt2: fresh container, CA NOT injected)
82-agent-onboarding-invite	assert_ok	C1-grow grow brk2 (N=2)
82-agent-onboarding-invite	assert_ok	C1 agent roster converges: roster_gen strictly grew after grow (polled past 30s throttle)
82-agent-onboarding-invite	assert_refuses	T1 forged pin + --expect-account-pub → refused BEFORE any write (no residue)
82-agent-onboarding-invite	assert_ok	T1-noresidue no agent.yaml written under the forge home
82-agent-onboarding-invite	assert_ok	T2 forged pin + inline seed, no --expect → agent.yaml RESIDUE + cache absent + doctor exit77+manifest-FATAL (DOC-7)
82-agent-onboarding-invite	assert_refuses	T3 tampered: unknown param → parse refused before any write
82-agent-onboarding-invite	assert_refuses	T4 tampered: wrong scheme/version → parse refused
82-agent-onboarding-invite	assert_refuses	T5 tampered: non-https bootstrap → parse refused (TETHER_DEV_NO_AUTH explicitly unset)
82-agent-onboarding-invite	assert_ok	U1 linger + user manager active
82-agent-onboarding-invite	assert_ok	U2 --install-user-service writes unit (install ≠ start)
82-agent-onboarding-invite	assert_ok	U3 user-unit enable --now → same agent back ONLINE
82-agent-onboarding-invite	not_covered	U3 (in-sim)
82-agent-onboarding-invite	assert_ok	U4 --uninstall removes the user unit
82-agent-onboarding-invite	not_covered	U1-U4 (in-sim)
83-cloned-image-instances	assert_setup	up 1 broker + 2 agents + 1 ctl
83-cloned-image-instances	assert_setup	init brk1 (standalone -> N=1 cluster)
83-cloned-image-instances	assert_setup	session $SID + ctl login
83-cloned-image-instances	assert_setup	agent-join agt1 (the machine the image is baked FROM)
83-cloned-image-instances	assert_setup	provision agt1 agent.yaml (real install.sh shape, flagless daemon)
83-cloned-image-instances	assert_setup	agt1 ONLINE before the clone is introduced
83-cloned-image-instances	assert_setup	clone agt1's ENTIRE ~/.tether onto agt2 (nkey + agent.yaml + state.json + roster cache)
83-cloned-image-instances	assert_setup	run agt2's daemon under agt1's baked nid
83-cloned-image-instances	assert_ok	B1 both instances are ONLINE as two distinct rows: the incumbent keeps '$BAKED', the clone is leased '$LEASE'
83-cloned-image-instances	assert_ok	B1b the incumbent's row is NOT suffixed (a rename of the existing device would be the worse failure)
83-cloned-image-instances	assert_ok	B2a exec a marker command on the baked name
83-cloned-image-instances	assert_ok	B2b exactly ONE start row for that command (two would mean both instances ran it)
83-cloned-image-instances	assert_ok	B2c exactly ONE exit row for that command
83-cloned-image-instances	assert_ok	B3 the leased instance can be targeted by name, like any other device
83-cloned-image-instances	assert_ok	B4 no reconciled_closed row was produced by the clone's arrival
83-cloned-image-instances	assert_ok	B4b no killed_orphan row either
84-shared-home-instances	assert_setup	up 1 broker + 2 agents + 1 ctl, agents sharing ONE home volume
84-shared-home-instances	assert_setup	init brk1 (standalone -> N=1 cluster)
84-shared-home-instances	assert_setup	session $SID + ctl login
84-shared-home-instances	assert_setup	both agents see the SAME .tether directory AND tether binary inode (not copies)
84-shared-home-instances	assert_setup	agent-join agt1 (writes the credential into the shared home)
84-shared-home-instances	assert_setup	provision agt1 agent.yaml (real install.sh shape, flagless daemon)
84-shared-home-instances	assert_setup	agt1 ONLINE before the second instance starts
84-shared-home-instances	assert_ok	S1 incumbent can expose (its ports live in the shared state.json)
84-shared-home-instances	assert_ok	S1b the incumbent's port token is persisted in the shared state.json
84-shared-home-instances	assert_setup	start agt2's daemon under agt1's nid, on the SHARED home
84-shared-home-instances	assert_ok	S2 both instances are ONLINE as two distinct rows ('$BAKED' + leased '$LEASE')
84-shared-home-instances	assert_ok	S2b the incumbent's row is NOT suffixed
84-shared-home-instances	assert_ok	S3 the incumbent's port token SURVIVED the leased instance's arrival (it must not write the shared state.json)
84-shared-home-instances	assert_ok	S4 exec a marker command on the baked name
84-shared-home-instances	assert_ok	S4b exactly ONE start row (two would mean both instances ran it)
84-shared-home-instances	assert_ok	S4c exactly ONE exit row
84-shared-home-instances	assert_ok	S5 the leased instance can be targeted by name
84-shared-home-instances	assert_refuses	S6 leased instance remote upgrade is refused on the shared binary
84-shared-home-instances	assert_refuses	S6b basename remote upgrade is also refused while the clone family exists
90-alerts-lifecycle	assert_setup	grow_to_3 (N=3 HA baseline; single attempt)
90-alerts-lifecycle	assert_setup	join agt1 for ps/banner coverage
90-alerts-lifecycle	assert_ok	clean baseline: NO active 'manual' alert before M1
90-alerts-lifecycle	assert_ok	clean baseline: NO active broker_down before M5
90-alerts-lifecycle	assert_ok	M1 raise manual/severe on the leader admin socket ($SENT)
90-alerts-lifecycle	assert_ok	M1 member alert ls (ctl-over-NATS) sees dedup_key=manual severity=severe msg=$SENT
90-alerts-lifecycle	assert_ok	M2 severe banner on 'ps' STDERR (‼ ALERT [manual] …$SENT)
90-alerts-lifecycle	assert_ok	M2 'ps' STDOUT stays parseable (no ‼ banner on stdout)
90-alerts-lifecycle	assert_ok	M2 severe banner also on 'node ls' STDERR
90-alerts-lifecycle	assert_ok	M2 --json suppresses the banner (no ‼ in ps --json either stream)
90-alerts-lifecycle	assert_ok	M3 alert ack manual (ctl-over-NATS, store-backed)
90-alerts-lifecycle	assert_ok	M3 acked_by committed on a fresh re-read (length>0)
90-alerts-lifecycle	assert_ok	M3 FG: severe banner STILL renders on ps stderr AFTER ack (ack≠clear)
90-alerts-lifecycle	assert_ok	M4 alert clear manual (admin socket)
90-alerts-lifecycle	assert_ok	M4 manual no longer active in member view
90-alerts-lifecycle	assert_ok	M4 second clear is idempotent (exit 0 again)
90-alerts-lifecycle	assert_ok	M4 post-clear: NO ‼ banner on ps stderr
90-alerts-lifecycle	assert_ok	M5① baseline: $FOLL_DOWN is a live VOTER
90-alerts-lifecycle	assert_ok	M5③ $FOLL_DOWN raft :7400 connection-refused (provably dead)
90-alerts-lifecycle	assert_ok	M5④ broker_down:$FOLL_DOWN raised (info) in member alert ls within 45s
90-alerts-lifecycle	assert_refuses	M7 alert ack quorum_lost → live-condition refuse (NOT a store-backed dedup_key)
90-alerts-lifecycle	assert_refuses	M7 alert ack force_single_active → same live-condition refuse
90-alerts-lifecycle	assert_ok	M5⑤ $FOLL_DOWN returns to VOTER + broker_down:$FOLL_DOWN clears before M9
90-alerts-lifecycle	assert_setup	M9 install follower-only ingress raft drop on $LAG_FOLL (NATS remains reachable)
90-alerts-lifecycle	assert_setup	M9 commit 72 real raft alert transitions while $LAG_FOLL cannot receive raft
90-alerts-lifecycle	assert_ok	M9 $LAG_FOLL still answers health but trails the leader by >64 entries
90-alerts-lifecycle	assert_ok	M9 raft_lag:$LAG_FOLL raises (not broker_down)
90-alerts-lifecycle	assert_ok	M9 lag fixture did not misclassify the live responder as broker_down
90-alerts-lifecycle	assert_setup	M9 remove follower raft drop
90-alerts-lifecycle	assert_ok	M9 follower catches up and raft_lag:$LAG_FOLL clears
90-alerts-lifecycle	assert_setup	M6 reset prior topology
90-alerts-lifecycle	assert_setup	M6 build capped 3 GiB N=2 fixture
90-alerts-lifecycle	assert_setup	M6 initialize capped fixture
90-alerts-lifecycle	assert_setup	M6 grow brk2 so disk pressure must forward across brokers
90-alerts-lifecycle	assert_setup	M6 create member session for alert reads
90-alerts-lifecycle	assert_ok	M6① clean baseline: NO disk_pressure:$DISK_FOLL before fill
90-alerts-lifecycle	assert_ok	M6③ allocate ballast and prove the SAME JS filesystem is above 80%
90-alerts-lifecycle	assert_ok	M6③ restart broker successfully to trigger the documented startup disk sample
90-alerts-lifecycle	product_red	#39 disk_pressure startup sample failed to raise exact disk_pressure:$DISK_FOLL within 90s after a proven >80% fill and successful follower restart
90-alerts-lifecycle	assert_ok	M6⑤ remove ballast and restart successfully for the clear sample
90-alerts-lifecycle	assert_ok	M6⑤ exact disk_pressure:$DISK_FOLL clears after recovery
90-alerts-lifecycle	assert_setup	M8 reset before the below_quorum N=2 leg
90-alerts-lifecycle	assert_setup	M8 provision three brokers (grow first to N=2, then N=3 clear control)
90-alerts-lifecycle	assert_setup	M8 initialize brk1
90-alerts-lifecycle	assert_setup	M8 grow brk2 to form exactly N=2
90-alerts-lifecycle	assert_setup	M8 create member session for N=2 alert reads
90-alerts-lifecycle	assert_ok	M8 below_quorum raises at exactly N=2
90-alerts-lifecycle	assert_setup	M8 grow brk3 to restore N=3 fault tolerance
90-alerts-lifecycle	assert_ok	M8 below_quorum clears after the same topology reaches N=3
91-client-converge	assert_setup	up 3 brokers + agent + ctl
91-client-converge	assert_setup	init brk1 (N=1 leader)
91-client-converge	assert_setup	create + activate ctl session
91-client-converge	assert_ok	A1 seeds publish (bootstrap + tls://brk1:443) → published (seed_generation set)
91-client-converge	assert_ok	A1 seeds show lists tls://brk1:443
91-client-converge	assert_setup	A2 grow brk2 reaches VOTER
91-client-converge	assert_ok	A2 grow brk2 → seeds show AUTO-includes brk2 (NO manual publish) within 60s
91-client-converge	assert_ok	A2 seed_generation ADVANCED past $GEN0 after grow (change-gated auto-publish)
91-client-converge	assert_setup	A2 grow brk3 reaches VOTER
91-client-converge	assert_ok	A2 grow brk3 → seeds show AUTO-includes brk3 within 120s (#46 CLOSED — leader periodic re-converge; NO manual publish)
91-client-converge	not_covered	A2 grow brk3 → seeds auto-include (brk3 never reached VOTER this run — grow flake)
91-client-converge	assert_setup	D persist ctl broker_url on non-leader floor $D_FLOOR
91-client-converge	assert_setup	D pin account trust + OOB survivor $D_SURV
91-client-converge	assert_setup	D warm signed roster/seed cache over the persisted floor
91-client-converge	assert_ok	D cache belongs to floor and contains the OOB survivor
91-client-converge	assert_setup	D save exact signed endpoint cache
91-client-converge	assert_setup	D construct valid OOB-only recovery cache (same pinned account + invite)
91-client-converge	assert_setup	D corrupt signed roster+seed cache without corrupting JSON
91-client-converge	assert_setup	D kill non-leader floor $D_FLOOR
91-client-converge	assert_setup	D floor is provably dead (no election injected)
91-client-converge	assert_refuses	D trust anchor: torn signed cache is ignored (dead floor cannot escape via untrusted endpoints)
91-client-converge	assert_setup	D restore exact valid pinned cache
91-client-converge	assert_ok	D live failover: node ls succeeds via survivor while persisted floor remains dead
91-client-converge	assert_ok	D failover did not silently rewrite the operator's dead floor
91-client-converge	assert_setup	D restart the killed floor container
91-client-converge	assert_setup	D restarted floor returns as a VOTER
91-client-converge	assert_ok	A3 retired endpoint auto-disappears and seed generation advances (NO publish)
91-client-converge	product_red	#31 leaked grow lock blocks A3 retire — captured output matched membership operation/grow in flight
91-client-converge	assert_setup	C kill $_d
91-client-converge	assert_setup	C prove $_d dead
91-client-converge	assert_setup	C stop survivor broker for offline recovery
91-client-converge	assert_setup	C offline force-single --reset-js commits with every non-self peer explicitly confirmed dead (runs to completion, rc=0)
91-client-converge	assert_ok	C force-single de-clustered nats.conf to standalone (no cluster{} block — #20 post-condition, not a log line)
91-client-converge	assert_ok	C force-single --reset-js MOVED the clustered JS store aside ITSELF (jetstream.force-single-bak.* present)
91-client-converge	assert_setup	C restart both standalone services (the JS reset was the product's job above)
91-client-converge	assert_ok	C survivor is SETTLED after the restart (admin socket answers; not exit-70 crash-looping)
91-client-converge	assert_ok	C seeds converge to survivor-ONLY: $SURV present + [$_deadre] dropped from endpoints (no manual publish) within 90s
92-js503-remote-alert	assert_setup	baseline ctl login
92-js503-remote-alert	assert_setup	create an independent destructive-gate session
92-js503-remote-alert	assert_setup	restore baseline ctl session before quorum-loss leg
92-js503-remote-alert	assert_ok	BASE: healthy --remote has NO 'DATA-PLANE DEGRADED' banner
92-js503-remote-alert	assert_ok	a③ brk2 :7400 refused (peer provably dead; quorum lost 1/2)
92-js503-remote-alert	product_red	#42 --remote quorum-loss verdict did NOT self-correct to READ-ONLY within 90s [#42-stronger] — the misleading 'transient' window persists far beyond the ~10s TFence estimate; the operator is misled far longer than documented
92-js503-remote-alert	assert_setup	leg-b prerequisite: N=2 fully recovers
92-js503-remote-alert	assert_setup	leg-b restore ctl login to the untouched baseline session
92-js503-remote-alert	assert_ok	leg-b brk2 is provably dead
92-js503-remote-alert	assert_setup	leg-b online force-single dwell becomes eligible
92-js503-remote-alert	assert_setup	leg-b online force-single commit succeeds
92-js503-remote-alert	assert_ok	b: --remote force_single exit code == 3 (#16)
92-js503-remote-alert	assert_refuses	M17 force_single_active blocks destructive session rm by default
92-js503-remote-alert	assert_ok	M17 --ack-alerts bypasses force_single_active and commits the delete
92-js503-remote-alert	assert_refuses	M17 committed write is observable: removed session cannot be activated
92-js503-remote-alert	assert_ok	b: the banner CARRIES the 'reconcile nats --to-standalone' remedy (same JetStreamUnavailable block)
92-js503-remote-alert	assert_ok	b: build a payload strictly larger than 8 MiB
92-js503-remote-alert	product_red	#41 dedicated JS-503 banner failed to fire within 90s in a control-healthy online-force-single fixture with clustered JS 1/2
92-js503-remote-alert	assert_ok	leg-b recovery GUARD precondition: the conf is STILL clustered (the already-standalone refusal also mentions --reset-js, so without this the guard could pass for the wrong reason)
92-js503-remote-alert	assert_refuses	leg-b recovery GUARD: bare --to-standalone REFUSES on a data-bearing JS store and names --reset-js + the data impact (R16 A4 acknowledgement gate)
92-js503-remote-alert	assert_ok	leg-b recovery: reconcile nats --to-standalone --reset-js succeeds (ONE product verb: conf swap + store move-aside; no hand-rolled mv)
92-js503-remote-alert	assert_ok	leg-b recovery: the product MOVED the store aside (never deleted) — a standalone-bak.* is on disk
92-js503-remote-alert	assert_ok	leg-b recovery: service restart (the step the product tells the operator to run)
92-js503-remote-alert	assert_ok	leg-b recovery: ctl re-login succeeds after broker auth responder is ready
92-js503-remote-alert	assert_ok	leg-b recovery: --remote banner clears on the recovered control plane
92-js503-remote-alert	assert_ok	leg-b recovery: build a >8MiB tier-B terminus payload
92-js503-remote-alert	assert_ok	REMOTE-homes: cluster status --homes --remote runs from ctl
92-js503-remote-alert	assert_refuses	REMOTE-mutex: --remote + --offline is an exact usage/mutex refusal
93-metrics-observability	assert_setup	grow_to_3 (N=3 HA baseline; single attempt)
93-metrics-observability	assert_setup	create member session for remote observability
93-metrics-observability	assert_ok	[env] webhook receiver up on ctl1:$WPORT
93-metrics-observability	assert_setup	enable observability on $b and restart successfully
93-metrics-observability	assert_setup	PROOF the seam really landed on $b (3 keys, exactly once each — a duplicate observability map is parser-defined)
93-metrics-observability	assert_setup	N=3 healthy after observability rolling restart
93-metrics-observability	assert_ok	MET: leader /metrics tether_broker_cluster_mode==1
93-metrics-observability	assert_ok	MET: leader /metrics tether_broker_is_leader==1
93-metrics-observability	assert_ok	MET: leader /metrics tether_broker_voters==3
93-metrics-observability	assert_ok	MET: leader /metrics has peer_reachable{node=…} DATA rows (leader-gated)
93-metrics-observability	assert_ok	MET FG: follower /metrics has is_leader==0 + NO peer_applied_lag{node=…} DATA rows (leader-gated)
93-metrics-observability	assert_ok	MET: POST to /metrics → 405
93-metrics-observability	assert_ok	TOPO-ACTION: cluster status --json carries topo_action on EVERY reporting voter (self row AND peer rows)
93-metrics-observability	assert_ok	TOPO-ACTION: a converged cluster reports the converged actions, never a failure one
93-metrics-observability	assert_ok	setup: TOPO-STUCK wedge $FOLL's nats.conf with an unrecognized directive
93-metrics-observability	assert_ok	TOPO-STUCK: the wedged broker's topo_action reaches the ctl as the closed-enum value (not a free-text reason)
93-metrics-observability	assert_ok	TOPO-STUCK: the TOPO column renders STUCK for it — NOT the catching-up marker the pre-batch-C ctl showed for a render/apply failure
93-metrics-observability	assert_ok	TOPO-STUCK: the health verdict is DEGRADED and its banner names STUCK (the wedge is at the CONVERGED generation, which the pre-batch-C gate reported HEALTHY_HA)
93-metrics-observability	assert_ok	TOPO-STUCK: cluster doctor reports the topology check FATAL and names the wedged broker
93-metrics-observability	not_covered	93 TOPO-STUCK (the reconciler did not report unknown_directive within 45s of the conf edit)
93-metrics-observability	assert_ok	setup: TOPO-STUCK restore $FOLL's nats.conf
93-metrics-observability	assert_ok	TOPO-STUCK: the wedge CLEARS once the conf is fixed (no sticky STUCK — the classification is derived per pass, not latched)
93-metrics-observability	assert_ok	TOPO-ACTION: the human TOPO column agrees — all 3 voters render the converged marker, none renders catching-up or STUCK
93-metrics-observability	assert_ok	ADMINRT: admin runtime --json emits the stable schema with a positive live goroutine count, thread count and uptime (the process-introspection surface exists and is sane)
93-metrics-observability	assert_ok	ADMINRT: the per-reconciler last_tick surface is populated (>=1 registered R7 reconciler — the stalled-reconciler diagnostic has rows to age)
93-metrics-observability	assert_ok	ADMINRT FRESHNESS: the leader's max reconciler last_tick ADVANCES across two spaced samples (a live pass is TICKING, not merely present — a stalled reconciler's last_tick would stop moving; node-states runs every ReconcileInterval=1s)
93-metrics-observability	assert_ok	HEALTH: one /healthz response has HTTP 200 and exact body ok
93-metrics-observability	assert_ok	HEALTH: one /readyz response has HTTP 200 and positive ready body
93-metrics-observability	assert_setup	READYZ-503: start draining the follower
93-metrics-observability	assert_ok	READYZ-503: one follower /readyz response has HTTP 503 and not-ready body
93-metrics-observability	assert_setup	READYZ-503: abort drain
93-metrics-observability	assert_ok	WEBHOOK precondition: STABLE cluster leadership before the delta-timing-sensitive webhook arm (the reconciler re-seeds + fires NOTHING on any leadership move — alert_reconcile.go:120-123,177 — which intermittently swallowed the raise delta)
93-metrics-observability	assert_setup	WEBHOOK warmup: raise a unique baseline manual alert
93-metrics-observability	assert_setup	WEBHOOK warmup: clear after one complete reconcile poll
93-metrics-observability	assert_ok	WEBHOOK warmup: leader-side reconciler/poster emitted the unique alert
93-metrics-observability	assert_setup	WEBHOOK truncate receiver log
93-metrics-observability	assert_setup	WEBHOOK raise a unique manual alert
93-metrics-observability	assert_ok	WEBHOOK raised: exact schema, transition, alert identity, leader and key whitelist
93-metrics-observability	not_covered	93 WEBHOOK raised (NONE — the committed alert delta was never generated: raft-lease-jitter timing race)
93-metrics-observability	assert_setup	WEBHOOK clear the same manual alert
93-metrics-observability	assert_ok	WEBHOOK cleared: second exact POST is transition=cleared for the same alert and same leader
93-metrics-observability	assert_setup	WEBHOOK-URL negative: install a forbidden ftp:// URL on $BAD
93-metrics-observability	assert_ok	WEBHOOK-URL negative: startup fails loudly on non-http(s), not a later POST timeout
93-metrics-observability	assert_setup	WEBHOOK-URL negative: restore valid receiver URL and restart $BAD
93-metrics-observability	assert_setup	WEBHOOK-URL negative: restored follower returns VOTER
93-metrics-observability	assert_ok	CARD/JSON-0 NON-VACUITY: --json yielded a non-empty .health and .exit_code to compare against (an empty needle would make the two grep clauses below match any output)
93-metrics-observability	assert_ok	CARD/JSON-1: the --json sample's process rc equals its own .exit_code field
93-metrics-observability	assert_ok	CARD/JSON-2: the --card sample's process rc equals the --json sample's .exit_code (adjacent stable samples agree)
93-metrics-observability	assert_ok	CARD/JSON-3: the card text carries the SAME health label the --json sample reported ($STATUS_HEALTH)
93-metrics-observability	assert_ok	CARD/JSON-4: the card text carries the exit-code footer '(exit $STATUS_EXIT)' matching the --json sample
93-metrics-observability	assert_ok	EXIT: online cluster status returns a valid B2 verdict + code (HEALTHY/NOT-HA, exit 0/1) from a broker
93-metrics-observability	assert_ok	WATCH: docker PTY renders at least two status frames before the bounded timeout
93-metrics-observability	assert_setup	MET-degraded: stop follower broker $MET_DOWN
93-metrics-observability	assert_ok	MET-degraded: same leader sample reports voters=3 and live quorum_margin=0
93-metrics-observability	assert_setup	MET-degraded: restart follower broker $MET_DOWN
93-metrics-observability	assert_ok	MET-degraded: recovery sample returns voters=3 and quorum_margin=1
93-metrics-observability	assert_ok	#75 healthy start emits the log-sink breadcrumb to journal (sink-visibility: config took effect)
93-metrics-observability	assert_setup	#75 inject a ROOT-level (mis-nested) observability block on $N75
93-metrics-observability	assert_ok	#75 mis-nested broker.yaml → broker REFUSES to start, diagnostic names the key (pre-#75: silently ignored)
93-metrics-observability	assert_setup	#75 restore $N75's broker.yaml (remove the root-level block) and restart
93-metrics-observability	assert_setup	#75 restored follower returns VOTER
93-metrics-observability	product_red	all-down remote status incorrectly returns rc=0 with 'no cluster detected/no broker answered' instead of exact ROSTER_UNREACHABLE rc=2
93-metrics-observability	not_covered	93 #42 quorum-loss ~TFence(10s) cluster status --remote transient window
94-agent-reconcile	assert_setup	up 1 broker + 2 agents + 1 ctl
94-agent-reconcile	assert_setup	init brk1 (standalone -> N=1 cluster)
94-agent-reconcile	assert_setup	vault init (AFTER cluster setup: any nuke on the way here would reap it)
94-agent-reconcile	assert_setup	session $SID + ctl login
94-agent-reconcile	assert_setup	agent-join agt1
94-agent-reconcile	assert_setup	agent-join agt2
94-agent-reconcile	assert_setup	provision agt1 agent.yaml (S0-tunnel)
94-agent-reconcile	assert_setup	provision agt2 agent.yaml (S0-tunnel)
94-agent-reconcile	assert_setup	sys.events observer up (member core-sub — permissions.go:147 allows it; 80/81 have done this for two batches)
94-agent-reconcile	assert_setup	the event subscription is really established (a missed event must not be confusable with a late sub)
94-agent-reconcile	assert_ok	A0a exec sleep 9481 on agt1 as a FOREGROUND process (ctl-side backgrounded — tether tracks it RUNNING)
94-agent-reconcile	assert_ok	A0b exec sleep 9482 on agt1 (#2)
94-agent-reconcile	assert_ok	A0c exec sleep 9483 on agt2 (THE CONTROL: it must survive agt1's reconciliation untouched)
94-agent-reconcile	assert_ok	A0d the BROKER sees both agt1 procs RUNNING (tracked, not orphaned outside its view — this is what makes a missed-exit possible)
94-agent-reconcile	assert_ok	A0e and agt2's child is RUNNING in the broker's view too
94-agent-reconcile	assert_ok	A1a docker kill agt1 (power-loss class: the whole container dies)
94-agent-reconcile	assert_ok	A1b agt1 leaves ONLINE (heartbeat stops)
94-agent-reconcile	assert_ok	A1c agt1 reaches OFFLINE
94-agent-reconcile	assert_ok	A1d ps LOST: agt1's first exec child is DERIVED LOST now that agt1 is OFFLINE (storage-RUNNING row + OFFLINE owning node -> LOST at read time, exec.go:326-345 — NOT persisted)
94-agent-reconcile	assert_ok	A1e ps LOST: agt1's second exec child is DERIVED LOST too
94-agent-reconcile	assert_ok	A1f DISCRIMINATOR: agt2's child is STILL RUNNING (agt2 is ONLINE) — LOST is per-owning-node-status, not a table-wide relabel; without this the LOST above could be a blanket mislabel of every RUNNING row
94-agent-reconcile	assert_ok	B0a bring agt1 back
94-agent-reconcile	assert_ok	B0b agt1 is ONLINE again
94-agent-reconcile	assert_ok	A2a G.1: agt1's two processes are reconciled to EXITED after re-registration
94-agent-reconcile	assert_ok	A2b they carry exit_code -1 (the synthetic 'we never saw it exit' marker)
94-agent-reconcile	assert_ok	A2c agt2's process is STILL RUNNING — reconciliation is node-scoped, not a table-wide sweep
94-agent-reconcile	assert_ok	A2d the agent_registered event was captured (inventory row 30, delivered by the inventory's own oracle)
94-agent-reconcile	assert_ok	A3a the audit reader itself works (absence below is only evidence if the reader is alive)
94-agent-reconcile	assert_ok	A3b G.5: history --kind proc carries kind=reconciled_closed rc=-1 for agt1's process
94-agent-reconcile	assert_ok	A3c and NOT for agt2's untouched process (paired with A3b: node-scoped, not table-wide)
94-agent-reconcile	assert_ok	B1a take a backup BEFORE the orphan process exists (as User=tether — broker-ops.md:621-626 #6)
94-agent-reconcile	assert_ok	B2a start a PTY-managed process via 'tether run' (exec CANNOT produce an orphan — agent.go:1133-1136)
94-agent-reconcile	assert_ok	B2b the broker sees it RUNNING
94-agent-reconcile	assert_ok	B2c and the OS really is running it (broker view + OS truth, double-proven — this one has to SURVIVE the injection, unlike the double-fault arm)
94-agent-reconcile	assert_ok	B2d expose a SECOND port after the backup (its row will be absent from the restored DB)
94-agent-reconcile	assert_ok	B3a stop the broker (an OPERATOR stop: Restart=always does not fight us)
94-agent-reconcile	assert_ok	B3b restore the OLDER bundle as User=tether (a product-level rollback to an earlier committed state)
94-agent-reconcile	assert_ok	B3c the restore happened well inside the 15-min port-revoke timer (expose.go:484-485) — otherwise B7 would be contaminated
94-agent-reconcile	assert_ok	B3d start the broker again
94-agent-reconcile	assert_ok	B3e the broker is ready
94-agent-reconcile	assert_ok	B3f the orphan process is STILL ALIVE after the broker rollback (it has to survive the injection for the arm to mean anything)
94-agent-reconcile	assert_ok	B3g INVARIANT: restarting the BROKER did NOT make the agent re-register (they are independent processes — which is exactly why the nats-server restart below is not optional)
94-agent-reconcile	assert_ok	B4a restart nats-server (NEVER 'systemctl restart tether-agent': systemd's cgroup would kill the very child under test)
94-agent-reconcile	assert_ok	B1-timeout the broker is reachable again
94-agent-reconcile	assert_ok	B2-timeout PRIMARY: the agent RE-REGISTERED (proxy.go:486's exact line, gated on a journal cursor). 'node ls' going ONLINE is explicitly NOT accepted: the restored bundle still holds agt1's node row, so heartbeats alone flip it back with zero register calls
94-agent-reconcile	assert_ok	B3-timeout the orphan was KILLED (the broker returned a drop directive for a process it has no record of)
94-agent-reconcile	assert_ok	B5 the agent logged the orphan kill (this line IS the evidence the drop directive was received AND acted on; DOC-25 closed 2026-09-19 — the reconnect path now prints reconciled/drop_procs/revoke_ports like the first register, which is how #87 was finally attributed)
94-agent-reconcile	assert_ok	B4a G.5: the killed_orphan audit row exists for the orphan's ULID
94-agent-reconcile	assert_ok	B4b and it carries NO rc (product guarantee: exec.go:451-454 sets RC only for exit|reconciled_closed) — asserted in BOTH directions
94-agent-reconcile	assert_ok	B6a the session from the bundle is intact
94-agent-reconcile	assert_ok	B6b the reconciled EXITED rows from arm group A are still there
94-agent-reconcile	assert_ok	B6c #49-hardening: the roster is exactly {brk1} — a resurrected pruned peer would be a restore-side defect
94-agent-reconcile	assert_ok	B7a the port-reconcile audit row exists (kind=reconciled — note: 'reconciled' is AuditPort's kind; AuditProc's is 'reconciled_closed', schema/audit.go:36/:51)
94-agent-reconcile	assert_ok	B7b DATA PLANE: web2's public port is really dead (curl exit 7)
95-broker-selfheal	assert_setup	grow_to_2 (N=2 VOTER + JS meta formed + leader pinned brk1)
95-broker-selfheal	assert_setup	session $SID + ctl login
95-broker-selfheal	assert_setup	agent-join agt1
95-broker-selfheal	assert_setup	provision agt1 agent.yaml (S0-tunnel)
95-broker-selfheal	assert_setup	expose agt1:8095 --on-broker brk1 --name heal (home PINNED — #29 makes it a coin flip otherwise)
95-broker-selfheal	assert_setup	pre-injection data plane serves the exact sentinel
95-broker-selfheal	assert_setup	seed a process + an exec row for the G.2 survival oracles
95-broker-selfheal	assert_setup	sys.events observer up (member core-sub — permissions.go:147)
95-broker-selfheal	assert_setup	the event subscription is really established
95-broker-selfheal	assert_ok	T0-drift the real unit really says Restart=always (T1's inference is only valid if its premise holds)
95-broker-selfheal	assert_ok	T1a SIGTERM straight to MainPID $PID0 (BYPASSING systemctl: a systemd-initiated stop makes Restart= inapplicable and would destroy the discriminator)
95-broker-selfheal	assert_ok	T1b journal says 'Deactivated successfully' — the exit was CLEAN (serve.go:241-247: SIGTERM -> NotifyContext -> context.Canceled -> RunE nil -> exit 0)
95-broker-selfheal	assert_ok	T1c and NOT 'Main process exited, code=' — this is T2's exact counterpart and is what makes #23 testable at all
95-broker-selfheal	assert_ok	T1d systemd revived it (NRestarts grew past $NR0) — with SuccessExitStatus unset (grep -c = 0), exit 0 is the ONLY success, so Restart=on-failure would have left it inactive-not-failed. THIS is #23's first behavioural proof
95-broker-selfheal	assert_ok	T1e a NEW MainPID replaced $PID0
95-broker-selfheal	assert_ok	T1f the broker is REALLY ready — 'broker: ready' line count grew in the broker slog (NOT ActiveState==active: Type=simple is 'active' the instant exec happens, long before it can serve)
95-broker-selfheal	assert_ok	T1g FUNCTION really recovered: the data plane serves the exact sentinel again
95-broker-selfheal	assert_ok	T1h no start-limit was hit (install.sh:752-753 deliberately leaves StartLimitBurst=5/10s on — a leftover would kill a later arm and get blamed on the product)
95-broker-selfheal	assert_ok	T2a SIGKILL MainPID $PID1 (unclean — the OTHER exit semantics)
95-broker-selfheal	assert_ok	T2b journal says 'code=killed, status=9' — T1 asserted this string's ABSENCE, T2 asserts its PRESENCE: together they prove we really produced two DIFFERENT exit semantics rather than one
95-broker-selfheal	assert_ok	T2c the broker really came back (new 'broker: ready' line)
95-broker-selfheal	assert_ok	T2d G.2: the session survived the crash
95-broker-selfheal	assert_ok	T2e G.2: the ALLOCATED port survived and the data plane recovered (real bytes)
95-broker-selfheal	assert_ok	T2f G.2: the pre-crash exec row is still in history
95-broker-selfheal	assert_ok	T2g the agent was NOT disturbed at all (broker and agent are independent processes — a broker crash does not drop the agent's NATS link; the roadmap's 'the agent reconnects' is wrong here). Paired with a same-window positive control below
95-broker-selfheal	assert_ok	T2h POSITIVE CONTROL for T2g (without it, T2g would be a vacuous PASS if the journal reader or the agent were simply dead): agt1 is ONLINE (polled — a kill-9 of the broker can briefly STALE the heartbeat view)
95-broker-selfheal	assert_ok	T3a restart nats-server underneath the broker
95-broker-selfheal	assert_ok	T3b HARD: the broker is active AND Result=success after the nats restart. 'inactive (dead)' or 'failed' here is EXACTLY #23's symptom and a regression of G1's Restart=always fix — a release blocker, never a shrug
95-broker-selfheal	assert_ok	T3c HARD: real read/write recovered (data plane)
95-broker-selfheal	assert_ok	T3d HARD: exec really works again
95-broker-selfheal	assert_ok	EV the tetherd_restarted event was emitted and is readable by a member over real NATS (inventory row 29; core-sub per permissions.go:147). Corroboration only — hung off T1/T2 because T3's nats restart drops the subscription itself
95-broker-selfheal	assert_ok	D-neg1 the tightened _d_raft_ok returns FALSE when NO leader is visible (existence half can RED — the predicate is not permanently true)
95-broker-selfheal	assert_ok	D-neg2 the tightened _d_raft_ok returns FALSE when the leader CHURNS across the two spaced reads (stability half can RED — a mere this-instant leader is rejected, and the OLD 'must be brk1' semantics is gone)
95-broker-selfheal	assert_ok	D0 create a session to doom
95-broker-selfheal	assert_ok	D0b switch the ctl back to $SID (R-CTX: session create ACTIVATES the new session, and a ctl left on a session we are about to destroy fails every later call with an Authorization Violation)
95-broker-selfheal	assert_ok	D1 stop ONLY brk2's nats-server (kills the JS meta quorum; brk2's tether-broker and raft stay up)
95-broker-selfheal	not_covered	95-D DELETING boot resume: could not decouple raft from JS at N=2
95-broker-selfheal	assert_ok	D4 restore brk2's nats-server so JS can form again
95-broker-selfheal	assert_ok	D4b JS meta re-formed after restoring brk2's nats (a JS-backed history read works again) — the boot resumer's documented precondition (broker.go:1023-1038); without it D5's 1s boot probe races JS re-election and the resume is silently skipped
95-broker-selfheal	assert_ok	D5 restart brk1's broker — the boot resumer must finish the interrupted delete (audit.go:161 / sessions.go:161,180)
95-broker-selfheal	assert_ok	D6a the broker is live again
95-broker-selfheal	assert_ok	D6b G.2(1)b: the DELETING session is really gone after the boot resume
95-broker-selfheal	assert_ok	D6c UNRELATED ROWS INTACT (the control source: the resume finished ONE session, it did not sweep the table)
95-broker-selfheal	assert_ok	D6d and the data plane still serves the exact sentinel
95-broker-selfheal	not_covered	95-D DELETING boot resume: could not park a session in DELETING on the real stack (session rm rc=$_D_RM_RC; the JS-meta-down window did not leave the row in DELETING)
96-mid-flight-chaos._shared	not_covered	96 arm B (run-PTY kill-broker → DOC-28) + arm C (expose-crash RETURN + home_reassign_failed event)
96-mid-flight-chaos._shared	assert_setup	grow_to_3 (N=3 HA)
96-mid-flight-chaos._shared	assert_setup	ensure brk1 is the leader (grow inits brk1, so this is usually a no-op — a redundant transfer to self would error 70)
96-mid-flight-chaos._shared	assert_setup	PRECONDITION leader is brk1 (so the victim brk2 is a NON-leader: #58's reaperMayDelete()==false is then guaranteed by source, not by luck)
96-mid-flight-chaos._shared	assert_setup	session $SID + ctl login
96-mid-flight-chaos._shared	assert_setup	agent-join agt1
96-mid-flight-chaos._shared	assert_setup	agent-join agt2 (the untouched control node)
96-mid-flight-chaos._shared	assert_setup	provision agt1 agent.yaml with tunnel/NATS on brk2 (the NON-leader victim)
96-mid-flight-chaos._shared	assert_setup	provision agt2 agent.yaml (control, on brk1)
96-mid-flight-chaos.A	assert_setup	R15 #58: set broker.cluster.xfer_reap_interval=8s on the victim home broker brk2 (so its home-authoritative periodic reap is OBSERVABLE, not a 5m wait — the reap itself is tether's, this only tunes cadence)
96-mid-flight-chaos.A	not_covered	#58 cross-home GC reap (deploy-tier observation)
96-mid-flight-chaos.A	assert_setup	R16 #58: set broker.cluster.xfer_reap_interval=8s on the LEADER brk1 (CADENCE only — the age floor is no longer compressible, see above)
96-mid-flight-chaos.A	assert_setup	R16 #58: restart brk1 so it loads the cross-home GC knobs
96-mid-flight-chaos.A	assert_setup	R16 #58: brk1 is the leader again after the knob-loading restart (the cross-home GC is leader-only, so the compressed knobs must be on the LEADER)
96-mid-flight-chaos.A	assert_setup	R16 #58: PRECONDITION re-verified — leader is brk1 (post-restart)
96-mid-flight-chaos.A	assert_ok	A0a create a 12 MiB payload (> the 8 MiB transferTierAMaxBytes constant => tier B is forced)
96-mid-flight-chaos.A	assert_ok	A0b CONTROL: a full tier-B pull completes cleanly before any injection (proves the audit read path pairs start+complete when nothing is wrong — without this, a missing terminal row later means nothing)
96-mid-flight-chaos.A	assert_ok	A0c CONTROL: history shows that transfer's START and COMPLETE as a pair
96-mid-flight-chaos.A	assert_ok	A1a start a tier-B pull in the BACKGROUND and let it get in flight
96-mid-flight-chaos.A	assert_ok	A1b the transfer is REALLY in flight (a 'start' row is visible) — otherwise 'no terminal row' would be true for the wrong reason
96-mid-flight-chaos.A	assert_ok	A1c INJECT: docker kill brk2 the instant the start row lands — the 1 GiB upload is still streaming, so it is caught in-flight (the agent's home AND a guaranteed NON-leader, so #58's reaper gate is false by source)
96-mid-flight-chaos.A	not_covered	96-A #57 in-flight interruption (audit unreadable — home broker down carries it away)
96-mid-flight-chaos.A	not_covered	96-A #57 in-flight interruption (transfer completed before the crash — in-sim interruption not reliably constructable)
96-mid-flight-chaos.A	assert_ok	A1f-pre bring brk2 back so the finalize-on-recovery pass can run BEFORE #57 is judged (R3-F3: the old verdict fired while the home was still down)
96-mid-flight-chaos.A	assert_ok	A1f-pre brk2's broker is active again (recovery cannot run on a dead broker)
96-mid-flight-chaos.A	product_red	#57 an in-flight tier-B transfer whose home broker crashes leaves a DANGLING start row and NO terminal audit, forever: the watchdog hangs off the broker's runCtx (transfer.go:593/:704) so it dies with the process, transferTracker is an in-memory map rebuilt EMPTY on restart (transfer.go:99-104 + broker.go:602), and any late finalization from the agent is silently dropped by handleEvTransfer's 'preview == nil -> return' (:816-819). Operators auditing transfers see a transfer that never ended.
96-mid-flight-chaos.A	assert_ok	A1e CONTROL after the injection: a fresh small (tier-A) transfer still pairs start+complete — so the audit face works and it is specifically the crashed transfer's terminal row that is missing
96-mid-flight-chaos.A	not_covered	96-A2 (#58) OBJ_xfer object count is unreadable via /jsz (baseline=$_B58, orphan-probe=$_C_ORPHAN)
96-mid-flight-chaos.A	not_covered	96-A2 (#58) no orphan object was manufactured (count $_C_ORPHAN <= clean baseline $_B58)
96-mid-flight-chaos.A	assert_ok	A2a bring brk2 back so its HOME-AUTHORITATIVE periodic reap runs (orphan object count $_C_ORPHAN > baseline $_B58 while it was down)
96-mid-flight-chaos.A	assert_ok	A2b brk2's broker is up again
96-mid-flight-chaos.A	not_covered	96-A2 (#58) object count became unreadable after the restart
96-mid-flight-chaos.A	not_covered	96-A2 (#58) the A-arm produced NO orphan set this run (peak orphan count $_C_ORPHAN <= tombstone floor $_reap_floor), so neither the home-authoritative reap nor the R16 leader cross-home GC had anything to reclaim
96-mid-flight-chaos.A	assert_ok	B0a 'run' is gated by the severe alert state left by the kill (the refusal names --ack-alerts)
96-mid-flight-chaos.A	assert_ok	B0b CONTROL: the SAME command with --ack-alerts is allowed through (proves the gate was BYPASSED, not that the command merely works)
96-mid-flight-chaos.A	not_covered	96-B0 run --ack-alerts gate (inventory row 122's S9 cell)
96-mid-flight-chaos.D	assert_ok	D0a BASELINE: 3 VOTER (poll — a bare check races the last joiner's raft catch-up)
96-mid-flight-chaos.D	assert_ok	D0b BASELINE: leader is brk1
96-mid-flight-chaos.D	assert_ok	D0c BASELINE: a real WRITE through brk1 succeeds (this is what must move to the survivors)
96-mid-flight-chaos.D	assert_ok	D0d BASELINE: switch the ctl back to $SID (R-CTX)
96-mid-flight-chaos.D	assert_ok	D0e BASELINE: brk2 can reach brk1's route port
96-mid-flight-chaos.D	assert_ok	D0f PREMISE (#89): every broker's own nats-server hosts exactly ONE tetherd client and it comes from loopback — brk1's broker IS on brk1's NATS, so the isolation below isolates the process it claims to (a broker roamed onto a peer's NATS forwarded around the cut and was read as a minority commit three times on 2026-09-19)
96-mid-flight-chaos.D	assert_ok	D1a INJECT: silently blackhole brk1's route (6222) + raft (7400) — 4222 is DELIBERATELY LEFT UP
96-mid-flight-chaos.D	assert_ok	D1b SELF-PROOF: brk2->brk1:6222 now HANGS (rc=124) — a silent DROP. An immediate failure would mean an OUTAGE, which is a different (easier) fault
96-mid-flight-chaos.D	assert_ok	D1c SELF-PROOF: brk2->brk1:7400 also hangs
96-mid-flight-chaos.D	assert_ok	D1d SELECTIVE CONTROL: ctl->brk1:4222 STILL CONNECTS — this is what makes 'the minority is read-only' a claim about TETHER. A broker cut off from everything obviously cannot serve; that would prove nothing
96-mid-flight-chaos.D	assert_ok	D2 the SURVIVORS elected a new leader — read from brk2/brk3, never from brk1 (whose view is stale by construction)
96-mid-flight-chaos.D	assert_ok	D3 (Q4) THE MAJORITY IS REALLY ALIVE: a real WRITE on the survivor side returns SUCCESS (rc=0) — the only legitimate proof of quorum (not a status field), and now a deterministic positive after the R14 best-effort-success fix (was the apply-lag non-idempotent flake)
96-mid-flight-chaos.D	assert_ok	D4a ANTI-VACUOUS: brk1's admin socket still answers (it is alive, just partitioned — otherwise D4b would be about a dead process)
96-mid-flight-chaos.D	assert_ok	D4c brk1 did NOT crash or restart: same MainPID, NRestarts unchanged
96-mid-flight-chaos.D	assert_ok	D5a HEAL the partition
96-mid-flight-chaos.D	assert_ok	D5b all three nodes converge on ONE leader (sort -u == 1)
96-mid-flight-chaos.D	assert_ok	D6 NO SPLIT-BRAIN, proven at the RESULT level: the row written on the majority during the partition is readable FROM brk1 (the ex-minority) after healing — a status field agreeing would not prove the data agrees
96-mid-flight-chaos.D	not_covered	96-D6b #65 NOT judgeable: brk1's own broker.log names canary3 pre-heal, but D0f found a broker off its own NATS (#89 shape) — the line is a roamed broker's forward through a peer, not committer attribution
96-mid-flight-chaos.D	product_red	#65 a partitioned-minority stale-leader write became DURABLE: canary3 was COMMITTED BY the isolated minority brk1 WHILE STILL PARTITIONED (the pre-heal committer artifact — brk1's own broker.log named canary3 BEFORE D5a healed the partition${_C3_COMMIT_PREHEAL_LINE:+: $_C3_COMMIT_PREHEAL_LINE} — is committer attribution taken during isolation, not just a --nats-url dial and not a post-heal delayed commit; D0f proved brk1's broker was on brk1's own NATS, so this is not the #89 roam) during the partition (D4b=${_D4B_REC:-n/a}, pre-heal-on-brk1=${_C3_PRE:-unknown}) AND is visible via the MAJORITY after heal (brk1=$_C3_B1 brk2=$_C3_B2 brk3=$_C3_B3) — a partitioned minority's committed write must never survive (raft safety). CANDIDATE: reproduce in a dedicated single run with the full D4/D6/D6b + pre-heal committer artifact before treating it as characterised, not asserted from a chaos drill alone
96-mid-flight-chaos.D	not_covered	96-D6b #71 AMBIGUOUS: brk1's canary3 commit-success line first observed after the pre-heal snapshot
96-mid-flight-chaos.D	not_covered	96-D6b canary3 durable via a LEGITIMATE majority commit (NOT #65 — committer attribution shows brk1 did NOT commit it)
96-mid-flight-chaos.D	not_covered	96-D6b minority stale-write rollback — minority-write variant STRUCTURALLY unreachable in-sim (needs condition Y: a long-lived pre-partition client)
96-mid-flight-chaos.D	not_covered	96-D6b minority stale-write rollback (brk1=$_C3_B1 brk2=$_C3_B2 brk3=$_C3_B3)
96-mid-flight-chaos.D	assert_ok	D7 R-CTX: switch the ctl back to $SID after the canary2/canary3 creates moved its active session (a fresh login must work on the healed cluster; without it the end-state measure reads canary3's empty roster)
96-mid-flight-chaos.D	not_covered	96-D post-partition full recovery (3 VOTER + agt1 & agt2 ONLINE) not observed within 360s of the heal
96-mid-flight-chaos.F	assert_ok	F0a two HELD seeds on agt1 are RUNNING (tether holds each 'sleep 966N' as its exec child — Q3: a self-backgrounded nohup would leave only an EXITED sh, so RUNNING is real evidence here)
96-mid-flight-chaos.F	assert_ok	F0b HELD control seed on agt2 is RUNNING — the CONTROL that must survive agt1's reconciliation untouched (foreground-held so F4's post-injection RUNNING check is non-vacuous)
96-mid-flight-chaos.F	assert_setup	F0c CONTROL PRECONDITION (GATE): agt2's seeded process is actually running before the double-fault AND its pid was captured for F4's identity comparison
96-mid-flight-chaos.F	assert_ok	F1 INJECT BOTH: kill agt1 AND its home broker brk2 together
96-mid-flight-chaos.F	assert_ok	F2 bring both back
96-mid-flight-chaos.F	assert_ok	F3 G.1xG.2 converge: agt1's processes are reconciled to EXITED(-1)
96-mid-flight-chaos.F	assert_ok	F4 THE DISCRIMINATOR: the EXACT pre-injection agt2 process (pid ${_F0C_PID:-?}) is STILL RUNNING and no seed row on agt2 was closed out — reconciliation is node-scoped, not a table-wide sweep. H14: this now asks a STRICTLY DIFFERENT question from the F0c gate (identity + no-terminal-row after the injection, vs 'is anything running' before it), so a sweep and an absent control can no longer produce the same signal
96-mid-flight-chaos.F	assert_ok	F5 G.5: the audit says kind=reconciled_closed (AuditProc's kind; 'reconciled' is AuditPort's — schema/audit.go:36 vs :51)
96-mid-flight-chaos.F	assert_ok	F6 the agent is NOT wedged: a NEW process starts and runs after the double fault
97-soak-cycles	assert_setup	grow_to_3 (N=3 HA)
97-soak-cycles	assert_setup	ensure brk1 is the leader (grow inits brk1, so this is usually a no-op — a redundant transfer to self would error 70)
97-soak-cycles	assert_setup	PRECONDITION leader is brk1 (brk1 is a MEASURED process and must never be a victim)
97-soak-cycles	assert_setup	session $SID + ctl login
97-soak-cycles	assert_setup	agent-join agt1 (the VICTIM agent)
97-soak-cycles	assert_setup	agent-join agt2 (a MEASURED process — never a victim)
97-soak-cycles	assert_setup	provision agt1 agent.yaml
97-soak-cycles	assert_setup	provision agt2 agent.yaml
97-soak-cycles	assert_setup	seed a 12 MiB payload on agt1 for the transfer-concurrency cycles (> the 8 MiB tier-A ceiling)
97-soak-cycles	assert_setup	warm-up: real business traffic before the baseline (t0 at boot would catch lazy init and false-RED)
97-soak-cycles	assert_ok	cycle $_c/0 INJECT: docker kill agt1
97-soak-cycles	assert_ok	cycle $_c/0 NON-VACUITY: agt1 really left ONLINE (an injection that did not take would make the whole cycle a no-op)
97-soak-cycles	assert_ok	cycle $_c/0 return agt1
97-soak-cycles	assert_ok	cycle $_c/0 NON-VACUITY: agt1 really came back ONLINE
97-soak-cycles	assert_ok	cycle $_c/1 INJECT: restart brk3's broker
97-soak-cycles	assert_ok	cycle $_c/1 NON-VACUITY: brk3's MainPID really CHANGED (proves the restart happened rather than being a no-op)
97-soak-cycles	assert_ok	cycle $_c/2 INJECT: silently partition brk2's route+raft
97-soak-cycles	assert_ok	cycle $_c/2 NON-VACUITY: the partition really took (rc=124 hang = a silent DROP, not a refusal)
97-soak-cycles	assert_ok	cycle $_c/2 heal
97-soak-cycles	assert_ok	cycle $_c/2 NON-VACUITY: healed — all three nodes agree on ONE leader again
97-soak-cycles	assert_ok	cycle $_c/2 NON-VACUITY: real business runs THROUGH the formerly-partitioned side (without this, 'healed' is unproven)
97-soak-cycles	assert_ok	cycle $_c/3 INJECT: a tier-B transfer runs CONCURRENTLY with a broker restart
97-soak-cycles	assert_ok	cycle $_c/3 NON-VACUITY (restart half): the concurrent broker restart really took (brk3 MainPID changed) — the deterministic proof the restart ran
97-soak-cycles	not_covered	97 cycle $_c/3 transfer-concurrency half
97-soak-cycles	not_covered	97 leak judgement invalid for this run: a MEASURED process restarted at cycle $_c (brk1 broker $PID_B0->$_pb, agt2 agent $PID_A0->$_pa)
97-soak-cycles	not_covered	97 leak judgement invalid: a leak sample failed to read /proc at cycle $_c (brk1 rc=$_rcb, agt2 rc=$_rca)
97-soak-cycles	assert_ok	cycle $_c END: real business still works (every cycle must end proving the system is usable, or 'no leak' would be trivially true of a dead system)
97-soak-cycles	not_covered	97 leak slope judgement skipped: SOAK_CYCLES=$SOAK_CYCLES gives fewer than the $LEAK_MIN_N samples the slope test needs
97-soak-cycles	assert_ok	LEAK brk1's broker fd/Threads/RSS stayed bounded across $SOAK_CYCLES chaos cycles (thresholds [UNCALIBRATED], widened 4x pending the 3-run baseline — a RED here is triaged as jitter-or-leak FIRST; the threshold is never what moves to make it green)
97-soak-cycles	assert_ok	LEAK agt2's agent fd/Threads/RSS stayed bounded across $SOAK_CYCLES chaos cycles
97-soak-cycles	not_covered	97 goroutine gate skipped: brk1's broker restarted during the soak (MainPID $PID_B0->$_pb_now)
97-soak-cycles	not_covered	97 goroutine gate skipped: no pre-load goroutine baseline was captured at t0
97-soak-cycles	not_covered	97 goroutine gate: no post-quiesce goroutine sample
97-soak-cycles	assert_ok	GOROUTINE brk1's NumGoroutine floor returned to the pre-load baseline after the soak+quiesce (post $GOR_POST <= pre $GOR_BASE + tol $GOR_TOL) — a real leak would keep the post floor elevated by the accumulated leaked count; .goroutines is the in-process truth (runtime.NumGoroutine), NOT the /proc Threads proxy
97-soak-cycles	assert_ok	INTEGRITY brk1 has no panic / FK violation / data race / DB corruption (broker slog AND journal)
97-soak-cycles	assert_ok	INTEGRITY brk2 is clean (broker slog AND journal)
97-soak-cycles	assert_ok	INTEGRITY brk3 is clean (broker slog AND journal)
97-soak-cycles	assert_ok	INTEGRITY agt2's agent journal is clean (the agent unit is NOT redirected, so journald is right here)
98-stuck-redial-recovery	assert_ok	up 3 brokers + 1 agent + 1 ctl
98-stuck-redial-recovery	assert_ok	init brk1 + grow to 3 (HA roster)
98-stuck-redial-recovery	assert_ok	session lab + ctl login (owner)
98-stuck-redial-recovery	assert_ok	agent-join agt1
98-stuck-redial-recovery	assert_ok	inject: silent-DROP agt1 -> $CUT_BROKER:4222 ONLY (the other voters stay reachable)
98-stuck-redial-recovery	assert_ok	inject self-proof A: agt1 -> $CUT_BROKER:4222 black-holes (rc 124, a PARTITION not an outage)
98-stuck-redial-recovery	assert_ok	inject self-proof B: agt1 -> $SURVIVOR:4222 is STILL reachable (the cut is one edge, so a failover target exists)
98-stuck-redial-recovery	assert_ok	inject self-proof C: at the watermark instant $CUT_BROKER's /connz STILL holds agt1's client connection (the cut edge is the live one; a connection that had already moved to a survivor would make the recovery conjunction vacuous)
98-stuck-redial-recovery	assert_ok	inject self-proof C′: at the watermark instant (read $_hb_try) $CUT_BROKER's /connz STILL holds agt1's client connection — the retry window did not let a rehome move agt1 before the watermark was taken
98-stuck-redial-recovery	assert_ok	RECOVERY within the SHARED ${RECOVERY_BUDGET}s budget (DROP still armed): agt1's heartbeat advances past the AT-INJECTION watermark AND /connz OBSERVES agt1 on a voter other than $CUT_BROKER — both, so it can only be a re-registration through a survivor, never a socket that exists or a heartbeat that slipped through the dying link
98-stuck-redial-recovery	assert_ok	SERVER-SIDE the cut broker $CUT_BROKER drops the dead client itself: /connz OBSERVES agt1 absent (nats.conf ping_interval×(ping_max+1); an observation error does not count)
98-stuck-redial-recovery	assert_ok	recovery used the ladder's NORMAL rungs (no 'teardown WEDGED' escalation on a single-edge partition)
98-stuck-redial-recovery	assert_ok	heal: lift the DROP
98-stuck-redial-recovery	assert_ok	steady state: agt1 stays ONLINE after heal (no flap loop)
98-stuck-redial-recovery	not_covered	[GAP #72] the WSS write-path black-hole arm (the live incident's actual shape: half-dead wss:// handshake under nats.go's nc.mu)
lib/setup-forcesingle	assert_ok	up 2 brokers + 1 agent + 1 ctl
lib/setup-forcesingle	assert_ok	init brk1 (N=1)
lib/setup-forcesingle	assert_ok	grow brk2 (N=2 clustered JS)
lib/setup-forcesingle	assert_ok	session + ctl login
lib/setup-forcesingle	assert_ok	agent-join agt1
lib/setup-forcesingle	assert_ok	JS meta FORMED at N=2 (cluster_size==2) before kill
lib/setup-forcesingle	assert_ok	baseline: build the 12 MB tier-B payload
lib/setup-forcesingle	product_red	#67 residual: the tier-B refusal said it was transient and told the operator to retry shortly, but the documented retry window (5 attempts over ~25s) NEVER succeeded on a healthy N=2 — the documented remedy does not work here
